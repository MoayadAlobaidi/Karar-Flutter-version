{
  "name": "@karar/financial-connections",
  "version": "0.0.0",
  "private": true,
  "description": "How Karar receives financial data — the subject's connections, and the many-to-many links that say which connection feeds which account. Thirteen rails are named and only MANUAL and USER_FILE_UPLOAD may be written; no credential of any kind is stored, and no state here means connected.",
  "type": "module",
  "main": "dist/public-api.js",
  "types": "dist/public-api.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/financial-accounts": "workspace:*",
    "@karar/platform": "workspace:*",
    "@karar/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@karar/financial-retention-local-fixtures": "workspace:*",
    "@types/node": "^25.9.5",
    "@types/pg": "^8.15.0",
    "pg": "^8.16.0"
  }
}
