{
  "name": "@karar/payment-instruments",
  "version": "0.0.0",
  "private": true,
  "description": "What SPENDS from a balance-bearing financial account — cards and QR payment identities as instruments, never as balances. There is no balance column and there must never be one; no PAN, CVV, token or wallet credential is stored anywhere.",
  "type": "module",
  "main": "dist/public-api.js",
  "types": "dist/public-api.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/financial-accounts": "workspace:*",
    "@karar/platform": "workspace:*",
    "@karar/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@karar/financial-retention-local-fixtures": "workspace:*",
    "@types/node": "^25.9.5",
    "@types/pg": "^8.15.0",
    "pg": "^8.16.0"
  }
}
