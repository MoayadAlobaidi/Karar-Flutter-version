{
  "name": "@karar/consent",
  "version": "0.0.0",
  "private": true,
  "description": "Consent keyed on the (operatingEntity, purpose, jurisdiction) triple: immutable versioned grants, the legal-document version lifecycle with mandatory re-consent classification, and fail-closed consent resolution (ADR-0024).",
  "type": "module",
  "main": "dist/public-api.js",
  "types": "dist/public-api.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/audit": "workspace:*",
    "@karar/operating-entity": "workspace:*",
    "@karar/platform": "workspace:*",
    "@karar/shared-kernel": "workspace:*",
    "@nestjs/common": "^11.2.0"
  },
  "devDependencies": {
    "@karar/consent-local-fixtures": "workspace:*",
    "@types/node": "^25.9.5",
    "@types/pg": "^8.15.0",
    "pg": "^8.16.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  }
}
