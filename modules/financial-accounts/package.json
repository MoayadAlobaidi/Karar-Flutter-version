{
  "name": "@karar/financial-accounts",
  "version": "0.0.0",
  "private": true,
  "description": "Financial accounts as the subject declared them, the source-reported balance snapshots attached to them, and the platform's reviewed institution catalogue. No bank connection exists and nothing here fabricates one.",
  "type": "module",
  "main": "dist/public-api.js",
  "types": "dist/public-api.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/platform": "workspace:*",
    "@karar/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "@types/pg": "^8.15.0",
    "pg": "^8.16.0"
  }
}
