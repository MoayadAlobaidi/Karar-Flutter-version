{
  "name": "@karar/financial-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure financial calculation. Zero framework dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/shared-kernel": "workspace:*"
  }
}
