{
  "name": "@karar/content-trust",
  "version": "0.0.0",
  "private": true,
  "description": "Which inputs may carry instruction authority, and which are only ever data. Zero framework dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  }
}
