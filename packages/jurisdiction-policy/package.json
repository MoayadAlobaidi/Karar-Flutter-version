{
  "name": "@karar/jurisdiction-policy",
  "version": "0.0.0",
  "private": true,
  "description": "Jurisdiction policy resolution. Zero framework dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  }
}
