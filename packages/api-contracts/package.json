{
  "name": "@karar/api-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Authored OpenAPI contract and domain event catalogue (ADR-0009).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
