// Phase 1: the contract surface is intentionally empty. Endpoints are authored
// into openapi/openapi.yaml first (ADR-0009); generated SDK types are exported
// from here once generation exists.
export {};
