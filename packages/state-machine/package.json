{
  "name": "@karar/state-machine",
  "version": "0.0.0",
  "private": true,
  "description": "Explicit-transition state machine. Zero framework dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  }
}
