// @karar/platform — backend platform library (Phase 2).
// Subpath entry points are added by their owning workstreams; this index
// re-exports the stable surface once integration completes.
export {};
