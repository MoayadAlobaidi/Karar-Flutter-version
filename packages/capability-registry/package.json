{
  "name": "@karar/capability-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Compile-time capability registry: ids, descriptors, lifecycle/implementation/deployment states. Zero framework dependencies; wiring stays ordinary module imports.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --exclude \"**/node_modules/**\" --exclude \"**/dist/**\""
  },
  "dependencies": {
    "@karar/jurisdiction-policy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.5"
  }
}
