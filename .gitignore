# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
*.jks
secrets/
**/service-account*.json

# Node
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.npm/
.pnpm-store/

# Flutter / Dart
apps/mobile/.dart_tool/
apps/mobile/.packages
apps/mobile/build/
apps/mobile/.flutter-plugins
apps/mobile/.flutter-plugins-dependencies
apps/mobile/ios/Pods/
apps/mobile/ios/.symlinks/
apps/mobile/android/.gradle/
apps/mobile/android/local.properties
*.iml

# Prisma
prisma/*.db
prisma/*.db-journal

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# Editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs and temp
*.log
logs/
tmp/
.cache/

# Local development data
docker/data/
minio-data/
postgres-data/

# User backup artifacts — never commit (scoped: only the known root archive,
# so legitimate zip fixtures elsewhere are not silently ignored)
/Archive.zip

# Prisma generated client (regenerated via make prisma-generate)
packages/platform/prisma/client/
