gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Gradle writes here when it is invoked directly rather than through the
# Flutter tool, which redirects output to apps/mobile/build. Uncovered, a
# `git add -A` after a direct Gradle run would commit build output.
/app/build/
/build/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
