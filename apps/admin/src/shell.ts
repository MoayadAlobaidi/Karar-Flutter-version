// Phase 1 shell only. Admin features arrive as SDK-backed views in later phases;
// this app never gains a database driver (docs/architecture/backend.md §2).

export const SHELL_TITLE = 'Karar Admin — Phase 1 shell';
