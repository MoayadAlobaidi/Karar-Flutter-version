# Terraform root

`modules/` holds reusable definitions; the three environment directories compose them.

**Production must not be introduced before a separate staging environment exists** — hard gate, Phase 20.

## Import rules

—

---

_Phase 0: this directory is a skeleton. No application code exists yet._
