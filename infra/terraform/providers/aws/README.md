# AWS implementations

AWS modules implementing the contracts. Structure exists so a second provider is an implementation, not a redesign. Built when a deployment needs it — not speculatively.

---

_Phase 0.5: skeleton only. No cloud account exists._
