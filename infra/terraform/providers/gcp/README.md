# GCP implementations

GCP modules implementing the contracts. Used only by compositions whose DeploymentProfile selects GCP. Nothing provisioned at Phase 0.5.

---

_Phase 0.5: skeleton only. No cloud account exists._
