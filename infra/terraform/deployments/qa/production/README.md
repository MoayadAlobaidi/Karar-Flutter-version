# QA production composition

Composition placeholder for the Qatar deployment profile, `production` environment.
Binds provider-neutral contracts (`../../../modules/contracts/`) to one provider
implementation when Phase 17 provisions infrastructure. No `.tf` files exist and
nothing is provisioned; the provider choice is UNVERIFIED in the
[country deployment matrix](../../../../../docs/architecture/country-deployment-matrix.md).
