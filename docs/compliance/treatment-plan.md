# Risk Treatment Plan

**Status:** ACTIVE register · **Owner:** Security Owner · **Version:** 0.7 · **Date:** 2026-08-22 · **Review:** every phase gate

**v0.7 (2026-08-22, Phase 5 gate; extended the same day as the register was):** rows **042–046 and 050 added** with their risks, so a treatment row again exists for every open risk. *(This line said 042–045, which was the set when it was written. The independent reviews and the phase's first CI run added 046–050; 047, 048 and 049 were fixed and closed and so carry their treatment in the register rather than here, but 046 and 050 stayed open and needed rows — a plan that claims coverage of every open risk has to actually have them.)* **KAR-RSK-011's evidence-store treatment slipped for the second time and is recorded as a slip with a hard deadline, a named owner and a written risk acceptance** rather than being re-dated — a passed deadline does not become "ongoing" because nobody wrote it down. No treatment was closed. Three of the new rows record work **delivered** at Phase 5 (044, 045, 050), two record work that **cannot** be delivered here (042 needs a second runtime, 043 needs a deployed environment), and one is not a phase treatment at all (046 is a gate on deployment); the difference is stated in each row rather than left to the reader.

**v0.6 (2026-08-18, Phase 4):** rows **031–041 added** with their risks. **A treatment row exists for every open risk** — rows 001–018, 020, 022–041, with 019 and 021 carrying their closures. Rows 013, 016, and 027 record the Phase 4 steps they had scheduled as **delivered**, and rows 016 and 027 record that delivery did not move the score, because the residual each carries was never the part the client owned.

**One treatment has slipped its phase, and that is a gate finding rather than a note.** **KAR-RSK-011's evidence-store step** — "evidence-store decision (out of interim) by Phase 2 gate" — has now passed the Phase 2 gate, the Phase 3 and Phase 3.5 gates, and the explicit "before Phase 4 or before any external audit" deadline the Phase 2 gate set for it. There are now **112 evidence rows across four phases** in run URLs with finite vendor retention and in-repo references, in one vendor account, with no organizational access control and no reviewer other than the collector. The risk was **re-evaluated with that fact and deliberately not re-scored**: the event it models is vendor loss or compromise, which is no more likely than before — what lapsed is the mitigation, not the exposure. Owner: Compliance Owner. Recorded here because this plan's own rule says a treatment that slipped its phase is a gate finding, and a finding nobody writes down is a finding that did not happen.

**Four Phase 4 treatments cannot be discharged by engineering effort, and each row says so plainly** rather than carrying a step that will never come due: 031 needs real handsets, 032 and 033 need an Apple Developer account and a Team ID that this repository must not invent, and 037 needs a branch-protection settings change outside the compliance workstream's write scope. A treatment whose next step belongs to somebody else is recorded with that owner named, not softened into something an engineer could plausibly do.

**v0.5 (2026-08-16, Phase 3.5 close):** every row re-checked at the gate; **row 021's closure confirmed** (gate record v0.6). No treatment slipped its phase, so there is no slipped-treatment finding at this gate. **A treatment row exists for every open risk** — rows 001–018, 020, 022–030 against the register's open rows, with 019 and 021 carrying their closures. **Phase 3.5 is COMPLETE; Phase 4 is NOT STARTED**, and the several rows naming Phase 4 state when a treatment step next falls due, not that any of it has begun.

**v0.4 (2026-08-16, Phase 3.5):** rows 022–030 added with their risks; row 021 records its closure and hands its surviving residual to row 030.

**v0.3 (2026-08-16, Phase 3):** rows 016–020 added with their risks; row 009 carries its Phase 3 note in the register.

**v0.2 (2026-08-15, Phase 2):** rows 003, 009, 010 updated with delivered Phase 2 steps; rows 014, 015 added with their risks.

Maps each open risk in the [risk register](risk-register.md) to concrete treatment steps and the [roadmap](../roadmap.md) phase where each step lands. Most risks do not close by effort — they close when a named phase delivers the mechanism, or when a headcount/legal trigger fires.

---

| Risk | Treatment steps | Lands at | Closes when |
|---|---|---|---|
| KAR-RSK-001 key person | Keep every decision and procedure canonical in-repo (already the working rule); verify account-recovery paths for SCM and registries; maintain role structure for handover (control-owners.md) | Continuous | First SoD trigger fires and roles actually separate |
| KAR-RSK-002 no independent review | Merge-blocking CI (KAR-CTL-016) + architecture tests (020) as the mechanical reviewer; independent-reviewer-agent step in each phase process; EXC-001 reviewed at every gate | Phase 1 CI; per-phase | Team ≥ 2 and human review becomes mandatory (EXC-001 exit) |
| KAR-RSK-003 no alerting | Observability foundations **delivered at Phase 2** (2026-08-15): classification-aware logs/traces/metrics, truthful readiness, outbox/job metrics (KAR-CTL-058, 062, 063) — emitting, not watched; projections/ops views Phase 8; alert routing, severity model, on-call rotation as Phase 20 gates (KAR-CTL-041) | 2 done; 8, 17–20 | Phase 20 gate passes with alerts live and rotation staffed |
| KAR-RSK-004 supply chain | Lockfiles + reviewed updates (028), SCA per-PR (025 — blocking threshold pending, tracked in `docs/operations/repository-security-settings.md`), SBOM per build (027), secret-scanned CI (026); verify container digest pinning at the Phase 1 gate; registry provenance checks when publishing begins | Phase 1, verified each gate | Never fully — residual re-scored each phase; drops to Moderate once SCA blocking and digest pinning are verified |
| KAR-RSK-005 CI secret leakage | Least-privilege workflow tokens (009); no secrets in workflow logs (036); secret scanning (026); Phase-1-gate review of all workflow files against these rules | Phase 1 gate | Residual accepted at Low after first clean gate review; re-opened on any workflow change adding secrets |
| KAR-RSK-006 residency undetermined | Keep architecture provider-portable (`DeploymentProfile`, jurisdiction-scoped KEKs — already designed); obtain data-residency legal opinion; record determination in `docs/architecture/data-residency.md` | Opinion by Phase 20; posture reviewed at 17 | Legal opinion recorded and architecture confirmed against it |
| KAR-RSK-007 Sharia review absent | Engage external Sharia reviewer for Zakat methodology; hold AC-010 (never a fatwa) true in product copy; launch gate on completed review | Before Zakat launch (Phase 9 gate) | Review completed and its scope recorded |
| KAR-RSK-008 Amanat clearance absent | Per-jurisdiction legal clearance engagement; terminology review; capability ships `PENDING_LEGAL_REVIEW` until cleared | Phase 14 gate | Clearance recorded per launch jurisdiction |
| KAR-RSK-009 sealed key custody | Rotation, custody-model, provenance, and canary **contracts designed and test-pinned at Phase 2** (2026-08-15; KAR-CTL-064, 065 — DESIGNED, test-only in-memory provider, no cloud KMS, no production keys); custody strategy + recovery policy + operating canary built at Phase 13 with the vault; approved strategy, rehearsed recovery, running canary as Phase 20 gates before any production `SEALED` data (ADR-0017) | 2 done (design); 13, 20 | Phase 20 custody gates pass |
| KAR-RSK-010 documentation drift | Docs checks in CI (023) from Phase 1; assurance-claim linkage test 26 ACTIVE from Phase 2 (score re-dropped to 9 on 2026-08-15, as this row predicted); per-gate reconciliation of claims vs. built reality (phase-compliance-gate.md item 6) | 1, 2 done, per-phase | Never fully — held at bay per gate |
| KAR-RSK-011 GitHub dependence | Full-clone recoverability rule (046); per-phase evidence export; evidence-store decision (out of interim) by Phase 2 gate; mirror/second-remote assessment at Phase 17 alongside cloud onboarding. **SLIPPED — recorded as a gate finding at Phase 4, not carried again in silence.** The store decision has now passed the Phase 2, 3, and 3.5 gates and the explicit "before Phase 4 or before any external audit" deadline. **112 evidence rows** now sit in run URLs and in-repo references under one vendor account, with no organizational access control and no reviewer other than the collector — which is the structural reason only one register row is `REVIEWED` — EV-007, the one row whose subject an authority outside this repository can confirm. The recoverability half is in better shape: the Phase 4 clean-clone verification executed at head `8d89328` with all 33 steps exiting 0 (EV-462). **SLIPPED A SECOND TIME AT THE PHASE 5 GATE (2026-08-22), and recorded as a formal slip rather than re-dated.** The Phase 4 gate named it overdue; Phase 5 produced no store, no access design, and no approval, and the evidence population grew again. The Phase 5 gate therefore does three things instead of moving the target quietly: it records the miss as nonconformity **CI-016**; it fixes a **non-ambiguous new deadline — before the Phase 6 gate report is written, and in any case before any evidence row is proposed for `REVIEWED`**; and it states the **risk accepted in the meantime**, by the Platform Owner, in one sentence — *every piece of Karar's compliance evidence remains held in a single vendor account, under a single credential, with no independent custodian, and a loss or compromise of that account would leave the project unable to demonstrate any control it claims.* Next step, unchanged and twice overdue: **a store decision recorded with its access design**, owner Compliance Owner | 2 **(missed)**, 4 **(missed)**, 5 **(missed)**, **6 (hard)**, 17 | Approved evidence store + recoverability check pass. **Until the store exists, no evidence row anywhere can honestly become `REVIEWED`**, which is the practical cost of the slip |
| KAR-RSK-012 accidental commits | Secret scanning (026) and synthetic-only rule (038) from Phase 1; repo-hygiene item on every phase gate; Platform Owner disposition of the pre-existing `Archive.zip` (remove, and decide whether history rewrite is warranted) at Phase 1 close | 1 close, per-phase | Archive dispositioned and two consecutive clean gate checks |
| KAR-RSK-013 no cert pinning | Accepted (EXC-002, threat model §4/C11); revisit when the Flutter network layer is built (Phase 4) and at production readiness (Phase 20). **The Phase 4 revisit happened, on schedule, and moved the score:** the network layer exists, a real refresh token now sits in platform-backed storage, and impact rose 3 → 4 (6 → 8) while likelihood held at 2 because **no endpoint exists for any environment and there is no traffic to intercept**. The acceptance stands on its original reasoning — pinning's bricked-clients-on-rotation failure mode is unchanged — with compensating controls now code rather than design (KAR-CTL-097, 099, 108) | Revisit 4 **(done, re-scored)**; 20 | Superseding decision, or re-acceptance recorded at Phase 20. **Re-examined again the moment a DEV endpoint exists**, because that is when the likelihood argument stops holding |
| KAR-RSK-014 queue exhaustion | Caps and bounded behaviour delivered with the queues at Phase 2 (bounded retries → dead-letter, payload cap, leases, stale-claim recovery — KAR-CTL-058, 059); DLQ-depth and backlog **alerting** with monitoring at Phase 20 (KAR-CTL-041); per-tenant/producer quotas assessed when tenancy exists (Phase 3+) | 2 done (caps); 3+, 20 | Never fully — standing operational risk, re-scored per gate; alerting closes the residual's detection half |
| KAR-RSK-015 schema drift | Checksums, drift detection, forward-only history, and from-zero rebuild delivered with the runner at Phase 2 (KAR-CTL-053, 054); `/readyz` reports migration status (062); re-scored when additional environments exist (Phase 17+), where drift surface multiplies | 2 done; re-score 17+ | Never fully — held by verify-per-run; re-scored per gate |
| KAR-RSK-016 refresh replay | Rotation, families, reuse detection revoking family and session, sha256-only storage delivered with identity at Phase 3 (KAR-CTL-068); client-side token custody rules with the Flutter network layer (Phase 4); reuse-signal alerting with monitoring (Phase 20, KAR-CTL-041). **The Phase 4 step is delivered:** one namespaced secure entry with platform options chosen and reasoned rather than defaulted, single-flight refresh over a separate raw transport with no coordinator or retry policy, terminal failures wiping the credential and transient ones leaving it, and an expired chain ending the session with no network call (KAR-CTL-097, 099; EV-437, 440). **Delivery did not move the score, and the reason is that the residual was never the client's:** a stolen token is valid until rotated or reused, and detection still fires on the second presentation | 3 done (mechanism); **4 done (client custody)**; 20 (alerting) | Never fully — bearer tokens carry inherent theft risk; held by rotation + detection, re-scored per gate |
| KAR-RSK-017 cross-tenant exposure | FORCE RLS with fail-closed principal context, architecture tests 9/22 merge-blocking, and adversarial non-empty-data suites delivered at Phase 3 (KAR-CTL-011, 071); per-module isolation coverage accretes with every new tenant-scoped table; re-score at Phase 17+ when real environments multiply the surface | 3 done; ongoing per table; re-score 17+ | Never fully — standing isolation risk, held by FORCE RLS and merge-blocking tests |
| KAR-RSK-018 privilege escalation | Deny-by-default closed catalogue, peer-delegation rule, RLS-bound assignment writes, audited grants delivered at Phase 3 (KAR-CTL-072, 073); the audited, reason-capturing HTTP admin surface arrives with the control plane (ADR-0021, KAR-CTL-013); access reviews at every gate (KAR-CTL-014) | 3 done; control-plane phase | Never fully — re-scored when an HTTP administration surface exists |
| KAR-RSK-019 unmounted kill-switch guards | Lead mounts `KillSwitchGuard`/`RequireOperationAllowed` on registration/login/refresh/invitation routes at phase integration, before the Phase 3 PR; the Phase 3 gate verifies the mounting (gate checklist item 3) | 3 close | The gate verification records the mounting — closes at the Phase 3 gate, or stays OPEN with reason and owner |
| KAR-RSK-020 fail-closed availability | Failure modes declared per policy in code at Phase 3 (fail-closed pre-auth limits and kill-switch reads; fail-open-with-fallback refresh; local-only providers refusing outside local — KAR-CTL-070, 075, 078); real mail/KMS providers selected at Phase 17+; dependency-outage alerting with monitoring at Phase 20 | 3 done (declared); 17+, 20 | Re-accepted or superseded when real environments and providers exist |
| KAR-RSK-021 tenant-bound surface dormant | Dormancy documented across phase-03, MODULE.md files, and the OpenAPI description (2026-08-16); binding mechanism (bind-at-login or explicit tenant selection) delivered as entry work of the first phase that needs the tenant-bound surface live | 3.5 (owns the mechanism) | **CLOSED 2026-08-16 (Phase 3.5):** the binding/bootstrap mechanism landed with its own adversarial and concurrency tests, exactly as this row required (KAR-CTL-091; EV-411, 412, 420). The residual the mechanism does not remove — a bound session outliving its membership — moves to row 030 rather than closing with it |
| KAR-RSK-022 jurisdiction misassignment | Typed source/verification axes with `USER_DECLARED` structurally `UNVERIFIED`, fail-closed `NONE`/`UNVERIFIED` denial, end-only immutable history, audited assignments — delivered at Phase 3.5 (KAR-CTL-079); an identity-verification provider behind `PROVIDER_VERIFIED`, and the audited operator surface that would make an assignment at all, arrive with the Phase 8 control plane | 3.5 done (mechanism); 8 | Re-scored when `VERIFIED` means an executed check rather than a recorded assertion; never fully closed while jurisdiction drives legal ceilings |
| KAR-RSK-023 unapproved pack in production | Lifecycle gate refusing non-`APPROVED` packs outside `local` and `APPROVED`-without-reference everywhere, structural validation before any write, metadata-only append-only ledger — delivered at Phase 3.5 (KAR-CTL-080, 081, 082); environment identity stays boot-time typed configuration, re-verified when the first non-local environment exists | 3.5 done; 17+ | Re-scored at the first non-local deployment, where the mislabelled-environment residual becomes reachable for the first time; legal approval of any pack is a legal act, not an engineering closure |
| KAR-RSK-024 capability above its ceiling | Compile-time registry as the ceiling, ceiling gates ahead of grant-like input, enablement-incapable settings shape, above-ceiling writes refused and audited, property harness over generated configurations — delivered at Phase 3.5 (KAR-CTL-085, 086, 087); the settings write path is reviewed when Phase 8 opens it | 3.5 done; 8 (settings write path) | Never fully — the shape argument must be re-checked at every schema change to these tables; the Phase 8 review is the named checkpoint |
| KAR-RSK-025 entitlement/availability misconfiguration | FORCE RLS, trigger-written append-only history with version continuity, revocation-as-status, read-time temporal validation, empty tables and unseeded permissions — delivered at Phase 3.5 (KAR-CTL-088, 093); operator surface with audited reason capture at Phase 8; unavailability alerting with monitoring at Phase 20 (KAR-RSK-003) | 3.5 done; 8, 20 | Never fully — configuration risk is standing; the detection half closes with alerting |
| KAR-RSK-026 subject-policy disclosure | CONFIDENTIAL classification, dual-GUC FORCE RLS, immutable supersession, reference-only audit, no events and no HTTP, no staff read path — delivered at Phase 3.5 (KAR-CTL-090); the owning capability protects its own option **content** when it arrives (Phase 9 for Zakat), behind its own audited purpose-limited surface | 3.5 done (metadata); 9+ (content) | Closed for this table when a first owning capability ships with its content protection reviewed; the metadata half is done |
| KAR-RSK-027 binding/rotation abuse | Server-side membership sourcing, bind-time re-verification, atomic session and refresh-family rotation on switch, uniform denial of arbitrary/revoked/expired/disabled targets, audited denials — delivered at Phase 3.5 (KAR-CTL-091); client-side handling of the switch response and the auto-bind contract fact lands with the Flutter network layer (Phase 4). **Delivered:** the client presents choices and sends a selection, never asserts a tenant, takes its binding from the server's own membership read, and explicitly invalidates tenant-scoped providers on a switch so a screen cannot render the previous tenant's data after the session rotated (KAR-CTL-101; EV-443) | 3.5 done; **4 done** | **Re-score commitment discharged at Phase 4 by re-examination: held at 8.** A well-behaved client neither widens nor narrows a server-side race; the window is narrowed by design, not eliminated |
| KAR-RSK-028 bootstrap over-disclosure | Closed-field-set serializer, single-sourced client projection consumed unenriched, omission of hidden capabilities and non-actionable reasons, leak-regression suites with hostile ports — delivered at Phase 3.5 (KAR-CTL-089, 092); response-timing equivalence is deferred with the T9 discipline | 3.5 done; T9 phase (timing) | Never fully while the surface aggregates — held by the closed field set and re-reviewed whenever a field is added; the timing half closes with T9 |
| KAR-RSK-029 caller-settable pin cutoff | Guard triggers on the pin columns, absent DELETE grant, required use-case version input, architecture test 21 merge-blocking — in place at Phase 3.5 (KAR-CTL-084); the real fix is a database-supplied creation witness independent of the caller, on the `recorded_at` pattern the audit store already uses (KAR-CTL-056), which is a schema change for a later phase. The separate open question — whether a subject election survives an operating-entity migration — belongs to the owning module | Later phase (creation witness); 9 (entity-migration question) | Closed when creation time for legal-consequence rows is witnessed by the database rather than supplied by the writer |
| KAR-RSK-030 bound session outliving membership | Membership re-verified on every bootstrap read with revocation on detection, reselect-required rather than a working tenant, status-transition removal the roster policy reads, adversarial proof of the actual data reach — delivered at Phase 3.5 (KAR-CTL-091); shortening the window further (shorter sessions, or per-statement membership checks) is a design and performance decision for the phase with real traffic | 3.5 done (bounding); re-score 4, 17+ | **Never fully — RLS cannot re-check membership.** The plan's job here is to keep the window bounded, measured, and stated, not to claim it away |
| KAR-RSK-031 biometric unverified on a device | Everything provable without hardware is delivered at Phase 4 — fail-closed failure mapping including its default arm, an unlock that grants no session, enable-on-confirmed-write with an unconfirmable disable retaining the safer state, host activity and theme parents asserted by source scan, biometric permissions asserted against a real merged manifest, purpose strings asserted in the packaged bundle (KAR-CTL-100, 111). The step that remains cannot be substituted: **a device pass covering at least one Android API 24–27 handset, one modern Android handset, and one Face ID iOS handset**, each exercising enrolment, a successful unlock, a refused unlock, and the password fallback that this phase found trapped the user | 4 done (everything static); first device-testing pass | Closed when a prompt has been shown, refused, and fallen back from on real hardware in each of the three device classes, with the results recorded. **No amount of further static assertion closes it** |
| KAR-RSK-032 no signed build | Refusal delivered at Phase 4: unsigned release rather than a debug-key fallback, an unsigned-not-debug-signed assertion in CI, iOS built with no code signing and the absence of a team and profile asserted by test (KAR-CTL-114). The treatment from here is **acquisition, not engineering** — an Apple Developer account and Team ID, an Android upload key with its custody decided before it is generated, and only then a first signed build on each platform. Custody, approval, and change-authorship separation are KAR-CTL-115, which activates at that moment rather than at a phase number | 4 done (refusal); pre-release (acquisition and first signed build); 20 (custody gates) | Closed when a signed build exists on each platform, produced under a recorded custody model, and the install and update paths have been exercised. **The refusal must be removed deliberately, not weakened incidentally** — a debug-key fallback appearing in the diff is the specific regression to watch for |
| KAR-RSK-033 no Apple Team ID | Delivered at Phase 4: identifiers agreeing per environment from one rule, that rule asserted character-for-character equal to the Gradle suffix table, one environment pair compared artifact to artifact, and the build failing closed when a team or profile is present and narrowing would be required (KAR-CTL-107, 109). Remaining: obtain a real Team ID, then run a **supported-device export and import between two enrolled devices** and confirm the excluded domains do not travel | Needs an Apple Team ID; first device-testing pass | Closed when a device-to-device transfer has been performed and the exclusions verified on the receiving device. **Nothing in this repository may invent a Team ID to unblock it** |
| KAR-RSK-034 no server-side revocation on abandonment | Local bounding delivered at Phase 4: a typed blocked recovery state reaching no protected content, an explicit retry, and an in-process latch for the remainder of the process (KAR-CTL-098). The closure is a **server-side revocation path invoked on abandonment** — the client reports the compound failure and the platform revokes the session and its refresh family rather than the device promising something it cannot deliver. That is an identity-module change with an endpoint, not a client change | 4 done (bounding); the phase that adds abandonment revocation | Closed when an abandoned session with an unconfirmable local wipe results in a **server-side** revocation, proven by test. Until then the register carries a bounded guarantee and says it is bounded |
| KAR-RSK-035 goldens unenforced | Delivered at Phase 4: `make test` matched to CI exactly so the two no longer disagree about which tests exist, `make test-golden` as the deliberate path, and goldens banned outright under `test/features` by source scan (KAR-CTL-096, 112). Remaining, in order: **run the baselines once on a Linux runner and record what happens** — the contradiction between the two in-repo comments is settled by execution and by nothing else — then either regenerate baselines per platform and enforce, or record a decision not to enforce with its reason | First CI job running Flutter tests on Linux | Closed either by CI enforcement of platform-appropriate baselines or by a recorded decision not to enforce. **An untested assumption about the runner is not a closure in either direction** |
| KAR-RSK-036 one artifact pair compared | Delivered at Phase 4: the transitive chain (per-environment iOS identity for all four against the shared rule, that rule asserted equal to the Gradle table) plus one direct artifact-to-artifact comparison (KAR-CTL-109). The remaining step is known and costed: **build all four Android variants on the macOS lane** so the comparison runs against two real artifacts per environment, at the price of a full Android toolchain on the expensive runner | Next mobile CI change | Closed when all four environment pairs are compared artifact to artifact, or when a recorded decision accepts the transitive argument for three of them with its cost reasoning stated |
| KAR-RSK-037 mobile lanes do not gate | Two steps, neither of them code: **add `mobile-android`, `mobile-ios`, and `mobile-supply-chain` to the required status checks** on `main`, and update `docs/operations/repository-security-settings.md` in the same change so the settings document and the settings agree. Both are outside the compliance workstream's write scope and are named here as owner actions. Until then the compensating position is that `mobile` is required and covers the code path, while the artifact evidence is not merge-blocking (KAR-CTL-113) | Next branch-protection review | Closed when the three lanes are required checks, verified by the `gh api` command the settings document already carries for the existing eight, and KAR-CTL-113 moves DESIGNED → IMPLEMENTED on that verification |
| KAR-RSK-038 runtime loopback rule narrower | Widen the Dart runtime configuration loader's loopback rule to match the build-time rule the guards now apply — the bracket unwrap, the trailing-dot strip, `0.0.0.0`, and the `.internal` and `.test` suffixes — **with a test case per host form**, since the lesson of this phase's IPv6 defect is that a rule present in source is not a rule that runs (KAR-CTL-106) | Next mobile change | Closed when the loader refuses every host form the build-time guard refuses, each proven by its own case. Deliberately not done under a release-blocking remediation round, and deliberately not left unrecorded |
| KAR-RSK-039 conformance coverage 82 of 128 | Approach delivered at Phase 4 with the covered set named explicitly and the validator throwing on unimplemented keywords (KAR-CTL-104). The treatment is a working rule rather than a backlog item: **each new response surface is bound to the suite as it is written, not after** — the Phase 5 entry criteria already carry it. The three in-process-unreachable statuses are documented in the suite header and are not counted as debt | 4 done (approach); 5 and each phase after | Never fully — coverage is a moving target against a growing contract. Held by the rule that a new surface lands inside the suite; a surface landing outside it is a gate finding |
| KAR-RSK-040 compromised device | Accepted at Phase 4 with both halves deliberate: no root or jailbreak detection, because it is an arms race that produces false confidence and breaks legitimate users while the platform keystore is the boundary being relied on; and no `FLAG_SECURE`, with the sensitive-screen wrapper covering its subtree, removing it from the semantics tree when the application is not resumed, and **stating its own limit in source** (KAR-CTL-096, 097) | 4 (accepted); re-score at first real data; revisit 20 | Re-scored when real customer data first exists on a device — which is the condition under which the acceptance may stop being reasonable, and the only honest trigger for revisiting it |
| KAR-RSK-041 platform-vendor dependence | Delivered at Phase 4: assert against what ships rather than against documentation — exact merged-manifest comparison, every backup mode declared, packaged-bundle posture and identifier read from the artifact, and per-dependency records with named owners and review triggers for the two packages that decide credential storage and lock behaviour (KAR-CTL-107, 108, 110, 111). Remaining and unclosable by assertion: **a channel through which a platform behaviour change under an installed application would be noticed**, which needs device testing and a signed distributable before it can exist at all | Ongoing per platform release; re-score at first device pass | Never fully — the vendors own the behaviour. Held by artifact-level assertion, re-run per build, and by the `compileSdk` pin carrying its own removal trigger rather than drifting |
| KAR-RSK-042 client never seen to run | Reproduce the startup suspension on a second runtime (Android emulator, then a physical device) so an iOS-specific plugin hang is separated from application logic; instrument the two secure-storage reads that precede the network so the suspended one names itself; fix it; then add a **runtime smoke test that launches the built artifact and asserts a terminal startup state is reached**, because every path involved is currently covered by fakes only. Until that test exists, no document may describe the client as working | Phase 6 entry — this blocks the Phase 6 entry criterion that the Phase 5 surface be reachable | A launched artifact reaches a terminal startup state on at least two runtimes, asserted by an automated check rather than by a screenshot |
| KAR-RSK-043 picker unreachable end to end | Accept and record rather than engineer around. The picker is gated on `TRANSACTIONS` being AVAILABLE, which requires a deployed environment, a declared jurisdiction and an approving policy pack — none of which exists or may be invented here. A harness build that forced the gate open would produce evidence about the harness | Phase 6+, at the first deployed environment | An end-to-end import performed by a person on a real device against a deployed environment. Nothing weaker closes it, and the row says so instead of substituting a proxy |
| KAR-RSK-044 controls that pass without verifying | Delivered at Phase 5 for the two known instances: a retired guard now reports `NOT_APPLICABLE` and is excluded from the pass count, a zero-scan inside a control's own window is a failure, and the documentation gate derives capability state, control tally, gate existence and phase status mechanically instead of exempting the corpus that carries them. **The general treatment is KAR-CTL-116** — the merge-blocking register sweep, still DESIGNED and still the outstanding leg | Phase 6 (KAR-CTL-116 graduation, CI-006) | Every register id, path and quoted status is checked by a merge-blocking automated check, and each verification control carries a self-test that fails when the control is made vacuous |
| KAR-RSK-045 unrun CI lane | Delivered at Phase 5 for the instance: the property the broken lane depended on is asserted by a test inside the workspace suite, so it fails where it is seen. The behavioural half — **open the phase pull request early enough that CI runs against the branch as it develops** rather than once at the end — is a process change owned by the Engineering Owner and is the actual treatment; 101 commits with no pull request is what let a required lane stay broken | Phase 6, from the first commit of the phase | A phase branch whose pull request is opened within its first working session, with required checks running on it thereafter |
| KAR-RSK-046 pre-limiter work cost | **Not treated by code at Phase 5, and the gate says so rather than a risk row saying it quietly.** The obvious fix — a limiter in front of identity — has no trustworthy subject to key on, so building one now would mean trusting a forwarding header before any proxy-trust boundary exists, which is a worse control wearing the same name. The treatment is therefore a **deployment gate**: no environment may serve this surface without an edge or pre-authentication limit, or an equivalent protection, defined against a stated proxy-trust boundary. Recorded in the Phase 5 gate §3b and in the roadmap's Phase 20 production gates | **Deployment gate, not a phase** — before the first non-local environment serves a request | An edge control exists in a deployment profile with its proxy-trust boundary written down, and the 22-query pre-limiter path is measured behind it |
| KAR-RSK-050 unrun screen assertion | Delivered at Phase 5 for the instance: the two masked-value assertions now count a contiguous digit run rather than every digit in a rendered string, so a formatted date stops reading as a card number, and the same assertion holds on both platforms' ICU data. **The process half is KAR-RSK-045's and is not double-counted here** — the reason a wrong, platform-divergent, security-relevant assertion survived 116 commits is that no pull request existed to run the lane that catches it. The draft pull request opened for this closeout is that treatment beginning, not that treatment finished | Phase 6, from the first commit of the phase | The `mobile` lane has run on every head of a phase branch from its first working session, so a platform-divergent assertion is found on the day it is written |

## Reading the plan

- **"Lands at"** references are to [roadmap](../roadmap.md) phases; a treatment naming Phase 17+ depends on a cloud account existing, which is itself not scheduled — that is the roadmap's honesty, not this plan's evasion.
- Treatments that say **"never fully"** are standing risks (supply chain, drift): the plan's job is to keep them scored and reviewed, not to pretend a closure.
- Every row is re-checked at the phase gate; a treatment that slipped its phase is a gate finding, and the risk's score is re-evaluated with that fact.
