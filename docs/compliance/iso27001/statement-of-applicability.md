# Statement of Applicability

**Status:** ACTIVE register · **Owner:** Compliance Owner · **Version:** 0.6 · **Date:** 2026-08-22 · **Review:** every phase gate (deltas per gate report §5)

**v0.6 (2026-08-22, Phase 5 gate): swept against matrix v0.7. No Annex A row moved, and one FORK was found and removed.** Three rows described **KAR-CTL-113 as DESIGNED** and one used that as the reason a finding "stops nothing today". The matrix has recorded it **IMPLEMENTED [P4]** since the Phase 4 PR close, when `mobile-android` and `mobile-ios` became required status checks. This file's own header rule — a framework view must not fork a status the matrix owns — is the rule that was broken, and the sweep that was supposed to catch it is the one that let it through, because a sweep re-reads what changed rather than what did not. Phase 5 added no control and moved no control status, so no control's applicability, justification or implementation state changes. Recorded explicitly rather than by omission, for the same reason the mapping view states it: a register with no entry for a phase reads exactly like one nobody re-read.

**Two applicability judgements were re-examined and HELD, because Phase 5's subject matter is what they are about.** **8.24 (use of cryptography)** stays IMPLEMENTED on the same narrow reading it has carried since Phase 3 — mechanisms exist in-repo, key custody does not, and Phase 5's field-level encryption of highly-sensitive financial data is more of the same mechanism under the same absent custody (ADR-0017, KAR-RSK-009). **8.12 (data leakage prevention)** stays where it is: Phase 5 added a bounded CSV ingestion path and a redaction discipline over untrusted statement narrative, and neither is the egress-monitoring control 8.12 describes. **5.34 (privacy and PII protection)** was re-read against account deletion and is unchanged — the erasure ports exist, the HTTP verb does not, and the row already says the mechanism is in-repo and operates nowhere.

**v0.5 (2026-08-18, Phase 4):** swept against **matrix v0.6**, which adds KAR-CTL-094–116 (20 IMPLEMENTED [P4], 2 DESIGNED, 1 DEFERRED). One row moved to IMPLEMENTED (5.21) on the strength of matrix-IMPLEMENTED KAR-CTL-110, per this file's standing rule that a move tracks a matrix-IMPLEMENTED control; 5.9, 5.15, 5.17, 5.19, 5.22, 8.1, 8.3, 8.5, 8.7, 8.8, 8.9, 8.10, 8.11, 8.12, 8.15, 8.21, 8.25, 8.26, 8.28, 8.29, 8.31, 8.32, 8.33 annotated without a status change; **8.24 deliberately not advanced for the fourth consecutive phase**, and the reason it does not advance changed, which is why it is restated rather than repeated. No row added or removed — the set is and stays the 93 Annex A controls. Deltas listed under the tally. **Phase 4 was IN PROGRESS and its gate had not been held when this version was written**, so nothing here is a gate outcome — the gate has since returned PASS_WITH_DOCUMENTED_DEFERRED_ITEMS and Phase 4 is merged, and no status below was advanced on either fact; every Phase 4 statement below describes a mechanism in this repository, and **no build has been installed on any device**.

**v0.4 (2026-08-16, Phase 3.5):** two rows moved to IMPLEMENTED on the strength of matrix-IMPLEMENTED [P3.5] controls (5.15, 8.32); 5.9, 5.12, 5.13, 5.19, 5.22, 5.23, 5.31, 5.34, 8.2, 8.3, 8.9, 8.10, 8.11, 8.12, 8.15 annotated without a status change; 8.24 deliberately not advanced for the third consecutive phase. No row added or removed — the set is and stays the 93 Annex A controls. Deltas listed under the tally.

**v0.3 (2026-08-16, Phase 3):** four rows moved to IMPLEMENTED on the strength of matrix-IMPLEMENTED [P3] controls (5.16, 5.17, 8.3, 8.5); 5.15, 5.34, 8.2, 8.10, 8.15 annotated without a status change. Deltas listed under the tally.

**v0.2 (2026-08-15, Phase 2):** seven rows moved to IMPLEMENTED on the strength of matrix-IMPLEMENTED controls (5.13, 5.33, 8.9, 8.11, 8.12, 8.15, 8.28); 8.3, 8.10, 8.16, 8.24, 8.32 annotated without a status change. Deltas listed under the tally.

All 93 controls of ISO/IEC 27002:2022 (Annex A of ISO/IEC 27001:2022), by identifier, with a short **Karar-language name** (paraphrased — not the standard's text), an applicability status, and a justification. Where a Karar control implements the Annex A control, the KAR-CTL ID links into the [control matrix](../control-matrix.md), whose status detail is authoritative.

**Status vocabulary (SoA-specific):**

| Status | Meaning here |
|---|---|
| `APPLICABLE` | In scope now; Karar's treatment is designed/underway in the current phase |
| `PLANNED` | In scope; treatment begins at the named phase — not pretended before then |
| `IMPLEMENTED` | A mechanism exists today (Phase-1 tooling); evidence still pending |
| `OPERATING` | Running with reviewed evidence — **no control holds this status yet** |
| `NOT_APPLICABLE` | Excluded, with the reason stated and a revisit trigger where one exists |

---

## Organizational (5.1–5.37)

| ID | Karar-language name | Status | Justification / linkage |
|---|---|---|---|
| 5.1 | Security policy set defined and approved | APPLICABLE | 14 policies **DRAFT and none approved** ([policy-index](../policy-index.md)); the original Phase 2 gate target was not met, and the standing deadline is Platform Owner review **before the first non-local deployment**. The "defined" half of this control is satisfied; the "approved" half is not, and the row stays APPLICABLE rather than IMPLEMENTED for exactly that reason. KAR-CTL-001 |
| 5.2 | Security responsibilities assigned to roles | APPLICABLE | [control-owners.md](../control-owners.md). KAR-CTL-002 |
| 5.3 | Conflicting duties separated | APPLICABLE | SoD triggers defined; current single-person reality carried as EXC-001/KAR-RSK-002 — applicable and honestly deficient |
| 5.4 | Management holds people to the policies | APPLICABLE | Platform Owner approval/acceptance duties; gate sign-off |
| 5.5 | Relationships with authorities | PLANNED (20) | Regulator engagement is a production-readiness gate; premature contact would imply claims Karar does not make |
| 5.6 | Contact with security community/advisories | PLANNED (2) | Today only scanner-fed advisories; a deliberate advisory intake lands with vulnerability management Phase 2 |
| 5.7 | Threat intelligence informs defenses | PLANNED (2) | Threat model exists; a recurring intel loop starts with vuln management. KAR-CTL-021 partial |
| 5.8 | Security built into project management | APPLICABLE | Phase gates + MODULE.md-first intake. KAR-CTL-004, 024 |
| 5.9 | Inventory of information and supporting assets | APPLICABLE | [asset-inventory.md](../asset-inventory.md). 2026-08-16: the first externally-held name asset recorded — `kararfinance.com`, registered with **nothing configured on it** (no hosting, no DNS record, no proxy, no mail); registration status USER_CONFIRMED and every hardening item TO_VERIFY ([domain runbook](../../operations/domain-and-dns-runbook.md), EV-427 PENDING). Database objects updated to the Phase 3.5 reality (48 tables). 2026-08-18 (Phase 4): the register gains its first client-side assets — the Flutter application and its build artifacts, the generated Dart client, the two device-side stores (the secure token store and the local security-state store), and the four application identifiers issued per environment on each platform (KAR-CTL-097, 098, 109). **No device holds any of them**: no build has been installed anywhere, and no store account, signing material, or Apple Team ID exists |
| 5.10 | Acceptable use and handling rules for assets | APPLICABLE | [acceptable-use-policy](../../policies/acceptable-use-policy.md); classification handling matrix |
| 5.11 | Assets returned on exit | PLANNED (first hire) | No employment relationships exist; rule activates with them |
| 5.12 | Information classified by sensitivity | APPLICABLE | Six-class scheme, canonical (`docs/security/data-classification.md`). KAR-CTL-033. 2026-08-16: the classification decision is now being made at design time for genuinely sensitive new data — `subject_policy_selections` is classified CONFIDENTIAL because an elected policy option can reveal a personal fact about the subject, and the classification drives its RLS, audit, and no-events rules rather than sitting beside them (KAR-CTL-090). The row stays APPLICABLE: classifying is a practice, and the enforcing mechanism is 5.13's |
| 5.13 | Information carries its classification label | IMPLEMENTED | 2026-08-15: every Phase 2 table carries a six-field lifecycle row including its class (`packages/platform/db/DATA_LIFECYCLE.md`, `modules/audit/MODULE.md`, enforced by architecture test 25); event catalogue fields carry classes. KAR-CTL-033, 061 (IMPLEMENTED [P2]); labelling extends to each new surface as it lands. 2026-08-16: all eleven Phase 3.5 tables carry their six-field lifecycle rows, test 25 unchanged and still merge-blocking |
| 5.14 | Information transfer rules | APPLICABLE | Today's only transfer surface is authenticated TLS to SCM/registries; classification matrix defines in-transit rules for the future system |
| 5.15 | Access control rules established | IMPLEMENTED | [access-control-policy](../../policies/access-control-policy.md); layered model in `docs/security/access-control.md`. KAR-CTL-007. 2026-08-16 (Phase 3): the application layer began enforcing the rules as code — deny-by-default RBAC against a closed migration-seeded catalogue, central PolicyService (KAR-CTL-072, IMPLEMENTED [P3]). 2026-08-16 (Phase 3.5): the rules themselves became typed, versioned, and resolvable rather than prose — what a deployment may offer whom resolves through eight deny-by-default gates whose ceiling arms run before any grant-like input, over a compile-time capability registry that configuration cannot widen, tenant entitlements that gate without pricing, and restrict-only jurisdiction settings (KAR-CTL-085, 086, 087, 088, IMPLEMENTED [P3.5]). IMPLEMENTED here still means a mechanism exists with passing tests and **no evidence of operation** — nothing is deployed, and no capability resolves available anywhere. 2026-08-18 (Phase 4): a client now consumes those rules, and the honest statement is that it enforces none of them — capability navigation is a compile-time allowlist that drops any id the server did not name, the client asserts no tenant and takes its binding from the server's own membership read, and the application lock grants no session. What the client holds is a presentation of a decision made elsewhere, which is exactly what keeps a device-resident allowlist from being an access control anybody could switch off (KAR-CTL-101, 102, 114, IMPLEMENTED [P4]) |
| 5.16 | Identity lifecycle managed | IMPLEMENTED | One SCM identity today, managed (KAR-CTL-007). 2026-08-16: application identity lifecycle exists as tested code — registration with verification, password lifecycle, session revocation (individual and bulk), token-version invalidation of outstanding tokens (KAR-CTL-010, 067, 068, IMPLEMENTED [P3]); account disable/enable use cases exist but their permissions are deliberately unseeded pending an invoking surface (migration 0050 header). Local/test execution only — nothing operates anywhere |
| 5.17 | Authentication material protected | IMPLEMENTED | `docs/security/secrets.md`; per-env stores still DESIGNED (KAR-CTL-036); scanning (KAR-CTL-026). 2026-08-16: application authentication material now protected by real mechanisms — argon2id with versioned parameters and rehash-on-login, refresh and invitation tokens sha256-only at rest, verification/reset codes as HMAC digests, TOTP secrets encrypted via the `EncryptionProvider` (KAR-CTL-066, 068, 069, 074, IMPLEMENTED [P3]). 2026-08-18 (Phase 4): authentication material acquired a second custody location — the device. One secure entry holds the access and refresh tokens, their expiries, and the session id: on iOS and macOS in the Keychain at `first_unlock_this_device`, deliberately out of iCloud Keychain and off any restored backup; on Android in Keystore-backed storage that resets on key-material error, so undecryptable ciphertext forces re-authentication rather than leaving a half-readable store. Every operation returns a typed outcome in which a genuine absence and an unreadable store are different values, and on failure only the key **name** is logged — never the value, and never the platform error message, which can echo the entry. The custody is the platform's rather than Karar's, which is why 8.24 does not advance on it (KAR-CTL-096, 097, 100, IMPLEMENTED [P4]) |
| 5.18 | Access rights provisioned and reviewed | IMPLEMENTED | KAR-CTL-014. **Correction, 2026-08-16: the "reviews at gates" this row previously asserted had not happened at any gate** — the control text claimed it, the gate template had nowhere to record it, and nothing caught the gap until the Phase 3.5 review (CI-004). The gate report gained a standing §9 Access review, and **the Phase 3.5 gate record contains the first genuine one**, which moved KAR-CTL-014 DESIGNED → IMPLEMENTED and moves this row APPLICABLE → IMPLEMENTED with it. The provisioning half is real and unchanged (write access maintainer-only, branch protection verified, EV-007). **IMPLEMENTED here means what it means everywhere in this file — the mechanism exists and has run, evidence over time does not.** One review of a one-person, no-environment estate is a baseline, not a history, and the reviewer is the reviewed (EXC-001) |
| 5.19 | Supplier security managed | APPLICABLE | [vendor-security-policy](../../policies/vendor-security-policy.md) + register. KAR-CTL-047. 2026-08-16: Cloudflare recorded as a current vendor scoped narrowly to **registrar and authoritative DNS**, explicitly not a financial-data processor or subprocessor, with the five conditions that would change that classification stated in its row — and its own security baseline recorded as TO_VERIFY rather than assumed. 2026-08-18 (Phase 4): the supplier surface widened by six directly-declared mobile packages and one federated platform interface, two of which carry their own record with a named owner because they are security-critical (`flutter_secure_storage`, the only place tokens are written; `local_auth`, which decides whether the lock opens and contributes permissions to the shipped manifest). **None of the seven has had a security review.** Adding suppliers while KAR-CTL-047's periodic-review leg is an open nonconformity (CI-005) widens that gap rather than leaving it where it was, and it is stated here rather than left to the vendor register alone |
| 5.20 | Security terms in supplier agreements | PLANNED (production) | Standard vendor ToS today; negotiated terms/DPAs when personal data or custom contracts exist. KAR-CTL-048 |
| 5.21 | ICT supply chain risk managed | IMPLEMENTED | SCA, SBOM, pinning. KAR-CTL-025, 027, 028. 2026-08-18 (Phase 4): pinning stopped being a convention and became an assertion. Every direct non-SDK mobile dependency carries an **exact version — no caret, no range** — with the committed lockfile beside it, and the check fails on a nested-block (`git:`/`path:`/`hosted:`) bypass an exact-version regex alone would miss, on a **missing** named security-critical package so that removing one is also a failure, on any `dependency_overrides` section, and on a lockfile that does not record hosted sources with checksums from a single host; every Flutter step in both workflows runs `--enforce-lockfile`. The regressions that motivated it were real: `local_auth` had drifted to a caret range with nothing failing, and the earlier lockfile check asserted that pub.dev **appeared** rather than that it was the only host, so a redirect to a private mirror left the suite green. One workspace override exists and is pinned exact — `deepmerge-ts` 8.0.1 against GHSA-ggr8-5vv4-36mx / CVE-2026-40345 — because a range under a security override is the one place a range actively hurts. The same discipline was then turned on the pins nobody had checked: all **fourteen GitHub Action pins across both workflows** were verified against the tags their comments name, and one disagreed — `actions/download-artifact` pointed at a README merge while its comment read `v6.0.0`. The SHA is the half a machine enforces; the comment is the only half a reviewer reads, so a comment that lies leaves the mechanical control intact and the review control hollow (CI-012). KAR-CTL-110 (IMPLEMENTED [P4]), with KAR-CTL-111 covering the artifact-side inspection. **IMPLEMENTED means the mechanism exists and the suite runs it. It does not mean any supplier was reviewed**, and none has been (5.19, 5.22, CI-005): pinning proves that what builds is what was read, never that what was read is safe |
| 5.22 | Suppliers monitored and changes reviewed | APPLICABLE | Register review dates; per-gate check. KAR-CTL-047. 2026-08-16: register rule 6 added — a vendor's classification is scoped to what it actually handles, and a broader product from the same vendor is a **new adoption with its own review**, not an extension of the existing row. **Correction, same date: the monitoring half had lapsed** — all five vendor rows carried a "Phase 2 gate" review date through two closed gates without the lapse being raised, contrary to the register's own rule 3 (CI-005). Dates re-set and the gate checklist now names the check; no vendor security review has been performed since the Phase 1 baseline, and KAR-CTL-047 stays DESIGNED accordingly. 2026-08-18 (Phase 4): the dates set at Phase 3.5 come due **at the Phase 4 gate, which has not been held** — so they have not lapsed a second time, and that is the only good thing to say about them. No vendor security review was performed in this phase either, the register now records the Phase 4 dependency additions so the gate reads a current list rather than a two-gate-old one, and **CI-005 stays OPEN and is now falling due** |
| 5.23 | Security in cloud service use | APPLICABLE | GitHub and (from 2026-08-16) Cloudflare are today's cloud services, both governed via the register; Cloudflare's use is registrar and DNS only, with proxy, CDN, WAF, email, and any compute or storage product each a separate reviewed adoption ([domain runbook](../../operations/domain-and-dns-runbook.md) §4). Platform cloud selection remains Phase 17 with shared-responsibility mapping |
| 5.24 | Incident management prepared | APPLICABLE | [incident-response-policy](../../policies/incident-response-policy.md) DRAFT. KAR-CTL-042 |
| 5.25 | Events assessed and classified | APPLICABLE | Severity model defined (SEV-1 at n=1 for sealed exposure) |
| 5.26 | Incidents responded to per procedure | APPLICABLE | Procedure drafted; untested pre-operations, stated in policy |
| 5.27 | Incidents feed learning | APPLICABLE | Post-incident review → [continual-improvement.md](continual-improvement.md) |
| 5.28 | Evidence collected and preserved | APPLICABLE | [evidence-handling.md](../evidence-handling.md) incl. incident evidence rules |
| 5.29 | Security maintained during disruption | PLANNED (20) | BC policy DRAFT; substance requires operations. KAR-CTL-045 |
| 5.30 | ICT continuity readiness | PLANNED (20) | DR runbook executed + RTO measured are Phase 20 gates |
| 5.31 | Legal/regulatory requirements identified | APPLICABLE | Roadmap non-engineering gates; jurisdiction docs; residency open (KAR-RSK-006). KAR-CTL-006. 2026-08-16: identification became structural — a PolicyPack enumerates the per-jurisdiction questions (consent requirements, processing bases, retention, identity requirements, disclosure obligations, currency, AI processing) as **typed slots that must each carry a decision or an explicit `PENDING_LEGAL_REVIEW` with its open question**, so an unidentified requirement is a validation failure rather than a silence (KAR-CTL-080). `qa/v1` identifies Qatar's questions and answers **none** of them: identification is not determination, and no legal review has occurred (KAR-RSK-023). Which jurisdiction's requirements apply to a given principal is itself a recorded, audited, verification-aware assignment rather than an inference (KAR-CTL-079) |
| 5.32 | Intellectual property respected | APPLICABLE | Greenfield rule (no legacy code, AC-012); dependency licenses visible via SBOM. KAR-CTL-022 |
| 5.33 | Records protected | IMPLEMENTED | Git history + evidence retention rules (KAR-CTL-046, still DESIGNED). 2026-08-15: a purpose-built records-protection mechanism now exists — the append-only audit store with grant- and trigger-enforced immutability (KAR-CTL-056, IMPLEMENTED [P2]); local/test execution only, no operating history |
| 5.34 | Privacy and PII obligations met | PLANNED (5/16/prod) | **No PII is held today** (KAR-CTL-038); ADR-0026 lifecycle + consent controls activate Phases 3–16. 2026-08-16: the consent mechanism itself landed — fail-closed gates, reviewed re-consent classification with no default, immutable entity/jurisdiction-pinned grants (KAR-CTL-049, 076, IMPLEMENTED [P3]); the row stays PLANNED because no personal data is processed and the deriving lifecycle/export/erasure mechanisms remain Phases 5/16. 2026-08-16 (Phase 3.5): three more privacy-shaped mechanisms landed without changing that — consent grants and data-protection role assignments now pin the PolicyPack version that produced them as a value/state pair (KAR-CTL-084), the subject's own policy election is CONFIDENTIAL, dual-GUC RLS'd, event-free and reader-free beyond the subject (KAR-CTL-090), and processing bases are typed per purpose per jurisdiction. **All of it fails closed, because `qa/v1` decides nothing:** the mechanism to hold a privacy obligation now exists; the obligations themselves remain unidentified in law and unmet in fact, and still no personal data is processed |
| 5.35 | Independent review of security | PLANNED (20) | Independent assessment by a party that did not build the system — hard Phase 20 gate; nothing in-house substitutes |
| 5.36 | Compliance with policies verified | APPLICABLE | Phase gates check policy adherence; three gate records exist and KAR-CTL-004 moved to IMPLEMENTED on them (KAR-CTL-004, 005). Stays APPLICABLE rather than IMPLEMENTED for a reason worth stating: the Phase 3.5 review found that the gates had been checking the controls without anything checking the gates — an unperformed access review, an empty nonconformity log, lapsed vendor dates, and drifted statuses all survived three closes (CI-001, CI-004…CI-006). Verification that misses its own gaps is not yet a verified state |
| 5.37 | Operating procedures documented | PLANNED (2) | Developer docs exist; operational runbooks arrive with things to operate |

## People (6.1–6.8)

| ID | Karar-language name | Status | Justification / linkage |
|---|---|---|---|
| 6.1 | Background checks before engagement | PLANNED (first hire) | No hires exist; rule activates with the first |
| 6.2 | Security duties in employment terms | PLANNED (first hire) | Same trigger |
| 6.3 | Awareness and training | APPLICABLE | Solo: the docs corpus + CONTRIBUTING are the training surface; formal onboarding per `docs/onboarding/developer.md` at hire |
| 6.4 | Consequences for violations | PLANNED (first hire) | Meaningless solo; defined with employment terms |
| 6.5 | Duties surviving exit | PLANNED (first hire) | Same trigger |
| 6.6 | Confidentiality agreements | PLANNED (first hire/contractor) | Includes external reviewers touching non-public material |
| 6.7 | Remote working secured | APPLICABLE | All work is remote by structure; acceptable-use-policy endpoint rules |
| 6.8 | Security events reported | APPLICABLE | SECURITY.md private channel. KAR-CTL-043 |

## Physical (7.1–7.14)

| ID | Karar-language name | Status | Justification / linkage |
|---|---|---|---|
| 7.1 | Security perimeters around facilities | NOT_APPLICABLE | No offices, data centers, or facilities are operated. Revisit: any premises, or Phase 17 (provider facilities inherited per shared-responsibility model) |
| 7.2 | Controlled physical entry | NOT_APPLICABLE | Same reason and trigger |
| 7.3 | Secured rooms and work areas | NOT_APPLICABLE | Same |
| 7.4 | Physical surveillance/monitoring | NOT_APPLICABLE | Same |
| 7.5 | Protection from physical/environmental threats | NOT_APPLICABLE | Same |
| 7.6 | Working rules in secure areas | NOT_APPLICABLE | Same |
| 7.7 | Clear desk and screen | APPLICABLE | Screen lock + no unattended unlocked session; acceptable-use-policy §R3 |
| 7.8 | Equipment placed and protected sensibly | APPLICABLE | One workstation; physical care rules in acceptable-use-policy |
| 7.9 | Assets protected off premises | APPLICABLE | The workstation *is* off-premises by structure: FDE, lock, no shared use (acceptable-use-policy) |
| 7.10 | Storage media controlled | APPLICABLE | Encrypted disk; no removable media for project data (acceptable-use-policy §R6) |
| 7.11 | Supporting utilities resilience | NOT_APPLICABLE | No facilities; provider-inherited at Phase 17 |
| 7.12 | Cabling protected | NOT_APPLICABLE | No cabling plant exists |
| 7.13 | Equipment maintained | APPLICABLE | Workstation kept on current OS/security updates (acceptable-use-policy §R4) |
| 7.14 | Equipment wiped before disposal/reuse | APPLICABLE | Disposal rule in acceptable-use-policy §R7; recorded in asset inventory on the event |

## Technological (8.1–8.34)

| ID | Karar-language name | Status | Justification / linkage |
|---|---|---|---|
| 8.1 | Endpoint devices secured | APPLICABLE | FDE, lock, updates, separation of concerns on the one workstation (acceptable-use-policy). 2026-08-18 (Phase 4): a second class of endpoint enters scope and Karar manages none of it — the customer's own phone. The client's posture is what it can set from inside the sandbox: platform-backed secure storage, an application lock, Android backup and device-to-device transfer closed by rule, and a per-environment application identity. **Root and jailbreak detection are deliberately not attempted** — both are defeated by the same access they would detect, and their presence invites treating a compromised device as trustworthy — and **screen capture is not prevented at all**. No mobile device management exists, none is planned at this phase, and no build is installed on any device |
| 8.2 | Privileged access tightly held | APPLICABLE | SCM admin = one person today, recorded; control-plane model for the future (KAR-CTL-013). 2026-08-16: application privileged access is now code-governed — PLATFORM_ADMIN-only role assignment with the peer-delegation rule, audited with actor and reason (KAR-CTL-073, IMPLEMENTED [P3]); no privileged HTTP surface exists until the control plane (ADR-0021). 2026-08-16 (Phase 3.5): privileged capability, entitlement, assignment, and pack-activation operations exist as permission-gated use cases whose **four permissions are deliberately unseeded**, so deny-by-default means every one of them currently refuses; the country, jurisdiction, and settings tables carry no `karar_app` write grant at all and change by reviewed migration only (KAR-CTL-093). Absence is the control, and it ends when Phase 8 seeds the permissions alongside the audited operator surface |
| 8.3 | Access to information restricted | IMPLEMENTED | Repository is public by decision (read); write access maintainer-only. No restricted-class information is held in it by rule. 2026-08-15: database-layer restriction real — DML-only `karar_app` vs schema-owning `karar_migrator`, denials proven (KAR-CTL-053). 2026-08-16: application-layer restriction delivered — RLS ENABLE+FORCE with fail-closed transaction-local principal context on every tenant-scoped table (37 tables: 17 FORCEd, 27 allow-listed with justification), deny-by-default RBAC, token-scoped invitation redemption (KAR-CTL-010, 011, 071, 072, IMPLEMENTED [P3]); adversarial suites prove denials on non-empty data. 2026-08-16 (Phase 3.5): the inventory is now **48 tables — 22 RLS ENABLE+FORCE, 33 allow-listed with per-table justification, 7 in both** — with test 22 refusing boilerplate allow-list reasons; subject policy selections are FORCEd on **both** principal GUCs, and the session tenant binding that supplies the tenant GUC is itself sourced server-side and re-verified (KAR-CTL-011, 090, 091, IMPLEMENTED [P3.5]). Local/test execution only. 2026-08-18 (Phase 4): the client added **no table, no permission, and no operator surface**, and its four new operations are self-scoped reads or a self-declaration that grants nothing — the memberships surface is mounted deliberately without a tenant and answers only from the caller's own principal, and the operating-entity summary is resolution-scoped so the register can be neither named into nor enumerated. The genuinely new restriction is enforced by a SELECT rather than by a mapper: four columns cross to the device, so licence evidence, contract references, registration internals, role assignments, and administrative metadata never enter the process (KAR-CTL-101, 102, IMPLEMENTED [P4]) |
| 8.4 | Source code access controlled | APPLICABLE | Write access maintainer-only; branch protection on `main` verified 2026-08-15 (EV-007): PR-only, 8 required checks, admins bound. KAR-CTL-007, 008 |
| 8.5 | Strong authentication | IMPLEMENTED | MFA on SCM (verification EV-007). 2026-08-16: application authentication exists as tested code — argon2id credential storage, TOTP MFA with one-time recovery codes, ledger-derived non-resetting lockout, fail-closed pre-auth rate limits, trusted-proxy discipline (KAR-CTL-010, 066, 069, 070, IMPLEMENTED [P3]); nothing operates in any environment. 2026-08-18 (Phase 4): the client-side half exists — MFA challenge, email verification, password recovery and change, and a single-flight refresh in which concurrent callers share one in-flight future, an already-replaced token short-circuits with no call, and an expired refresh chain terminates **without any network request**; the refresh is issued over a separate raw transport with no session manager, coordinator, or retry policy, so a refresh cannot trigger a refresh. **A device biometric unlock is not authentication**: it grants no session, substitutes for no sign-in, receives, stores and transmits no biometric representation, and its failure mapping is fail-closed **including the default arm**, because the plugin's error list is explicitly open (KAR-CTL-096, 099, 100, IMPLEMENTED [P4]). **None of it has been exercised on a device** |
| 8.6 | Capacity managed | PLANNED (17) | Nothing to capacity-manage; CI quotas trivial |
| 8.7 | Malware defenses | APPLICABLE | Endpoint hygiene + no-untrusted-downloads rule (acceptable-use-policy §R5); pinned dependencies reduce the realistic vector. 2026-08-18 (Phase 4): pinning became exact and asserted across a second package ecosystem (5.21), which is the whole of the realistic malware control for a build pulling code from two public registries |
| 8.8 | Technical vulnerabilities managed | **IMPLEMENTED** | Phase-1 CI runs dependency scanning per-PR (report-only pending its blocking threshold — KAR-CTL-025), CodeQL static analysis (KAR-CTL-029, [C1]), and automated update PRs (dependabot); runtime/platform vuln management PLANNED with runtimes. The mechanism exists; the blocking decision is the open piece. 2026-08-18 (Phase 4): the mechanism produced a finding somebody acted on rather than a report nobody moved on — `deepmerge-ts` below 8.0.0 carries a high advisory (GHSA-ggr8-5vv4-36mx / CVE-2026-40345, uncontrolled recursion) and reaches this repository only transitively through `@prisma/config`, so the workspace forces the patched line at an exact version with a stated removal trigger, and the reachability argument (what it merges is this repository's own Prisma configuration, not attacker-supplied input) is recorded **next to** the pin rather than used to skip it. The counterweight, stated in the same breath: **the mobile artifact and supply-chain CI lanes run without gating a merge** — KAR-CTL-113 was DESIGNED rather than IMPLEMENTED for exactly that reason. **It moved to IMPLEMENTED at the Phase 4 PR close** (matrix v0.7), when `mobile-android` and `mobile-ios` became required status checks, so a finding raised there does gate a merge (KAR-CTL-110, 111, IMPLEMENTED [P4]) |
| 8.9 | Configuration managed | IMPLEMENTED | All config in git via PR. 2026-08-15: typed, boot-validated, fail-fast runtime configuration delivered (KAR-CTL-051, IMPLEMENTED [P2]); errors name fields, never values. IaC skeleton remains DESIGNED (KAR-CTL-031). 2026-08-16: the boundary between configuration and code is now drawn deliberately — policy packs and capability descriptors are **reviewed code, not rows**, the reference registers and jurisdiction settings have no runtime write path, environment identity and the first-party tenant are typed boot-time values with no identifier literal in domain code, and runtime rows can only ever narrow what the code permits (KAR-CTL-080, 085, 086, 091, 093). 2026-08-18 (Phase 4): configuration became something a build refuses rather than something a process complains about. Four environments compile in as dart-defines; an unrecognised environment stops the build; a define whose **name** looks credential-shaped is a configuration violation reported by key name and never by value; and the endpoint guard fails the build on both platforms — on iOS as the Runner target's last phase, **before code signing** — for any non-LOCAL environment lacking an HTTPS endpoint that carries no credentials and names no loopback, private alias, bracketed or expanded IPv6 loopback, or `.local`/`.localhost`/`.internal`/`.test` host. **Absence of dart-defines is now the failure rather than the exemption**, which it was not until this phase: a direct Gradle invocation passed through untouched. Recorded against it: **the runtime loader's rule is narrower than the build-time rule and was not widened with it**, so the two layers disagree and the narrower one is the one that runs on a device (KAR-CTL-098, 105, 106, IMPLEMENTED [P4]) |
| 8.10 | Information deleted when no longer needed | PLANNED (5) | ADR-0026 lifecycle declarations + erasure strategies. KAR-CTL-037. 2026-08-15: every Phase 2 table already carries its declared retention and erasure strategy (`packages/platform/db/DATA_LIFECYCLE.md`); the deriving mechanism is still Phase 5. 2026-08-16: every Phase 3 table likewise declared; consent grants are deliberately retained immutable evidence — withdrawal preserves the row (KAR-CTL-076); retention values remain policy-configuration placeholders pending Phase 3.5 PolicyPacks. 2026-08-16 (Phase 3.5): the PolicyPack retention **slots** now exist and are typed per category (`audit-events`, `consent-evidence`, `user-profile`), and `qa/v1` fills every one with an explicit `PENDING_LEGAL_REVIEW` naming its open question — so the placeholders became *declared unknowns with an owner* rather than prose. **No retention period was decided and no deletion mechanism was built**; the row stays PLANNED (5). 2026-08-18 (Phase 4): the first deletion mechanism anywhere in the codebase is a device-side one, and it states its own ceiling. Terminal refresh failures and session end wipe the stored credential; the in-memory copy is dropped **before** storage is wiped, so a failed wipe still leaves the process credential-free; and a session abandoned without both the deletion and the durable invalidation confirmed enters a typed blocked state that reaches no protected content, rather than presenting a clean signed-out one. **Nothing local can guarantee a credential is dead** — server-side revocation on abandonment is the closure and is not in this phase. The row does not move: wiping a credential is not the retention and erasure machinery this control is about (KAR-CTL-097, 107, IMPLEMENTED [P4]) |
| 8.11 | Data masked where full values are not needed | IMPLEMENTED | 2026-08-15: log/event/audit-metadata redaction delivered (`[redacted:*]` markers, HSF stored redacted — KAR-CTL-040, 057, IMPLEMENTED [P2]). Admin projections instead of raw tables remain Phase 8. 2026-08-16: subject-policy audit records carry **references and pins only, never option content** (KAR-CTL-090) — the minimization is in what is written, not in what is redacted afterwards. 2026-08-18 (Phase 4): the client's transport logs URIs with every query **value** replaced, generated DTOs print their type name only so a PII-bearing DTO cannot leak through string interpolation, and server-authored `title` and `detail` are parsed and never logged or branched on (KAR-CTL-101, IMPLEMENTED [P4]) |
| 8.12 | Data leakage prevented | IMPLEMENTED | Secret scanning was the first live layer (KAR-CTL-026); 2026-08-15: classification-driven event/log rules landed (SEALED identifier-only without exemption, `SecretValue` redaction — KAR-CTL-052, 061, IMPLEMENTED [P2]). Egress-side DLP for a running system remains with runtimes. 2026-08-16: the first client-facing egress boundary is structural — a closed field set picked name by name, with hidden capabilities omitted in every state and non-actionable denial reasons omitted entirely, single-sourced so the filter cannot fork, and leak-regression suites driving hostile ports at the edge (KAR-CTL-089, 092, IMPLEMENTED [P3.5]). Timing equivalence is **not** asserted (KAR-RSK-028). 2026-08-18 (Phase 4): the first device-side egress boundary. `allowBackup="false"` is necessary and **not sufficient** at targetSdk 31 and above, where it disables cloud backup only and a backup mode whose section is missing from the rules resource is *fully enabled* rather than off — so all three attributes are set, the data-extraction rules declare every mode and exclude every documented domain from each, and the rules resource, its packaging into the APK, and all nine exclusions are asserted against a real build. `print` is banned by the analyzer and a redacting logger is the only sink; goldens are banned under the feature tree by a source scan, because a golden is a committed image of whatever was on screen and the MFA setup key, recovery codes, and session list must never be in one. Two residuals, stated rather than implied: tokens are Keystore-wrapped, so a transferred copy would be ciphertext — which is a weaker claim than *no copy left*, and only the second is the control — and **screen capture is not prevented** (KAR-CTL-096, 097, 101, 107, IMPLEMENTED [P4]) |
| 8.13 | Backups taken and tested | PLANNED (17) | Nothing to back up but git, covered by clone recoverability (KAR-CTL-046); real backups with restore verification at Phase 17+, KAR-CTL-044 |
| 8.14 | Redundant processing capability | PLANNED (17/20) | KAR-CTL-045 |
| 8.15 | Logs produced and protected | IMPLEMENTED | 2026-08-15: structured classification-redacting logs and the append-only audit store with immutability enforcement delivered (KAR-CTL-040, 056, 063, IMPLEMENTED [P2]); staff-read auditing remains Phase 8 (KAR-CTL-039, DEFERRED). 2026-08-16: security-event surfaces widened — append-only `authentication_security_events` ledger, kill-switch history via SECURITY DEFINER trigger, audited authorization denials (KAR-CTL-075, 077, IMPLEMENTED [P3]). 2026-08-16 (Phase 3.5): three further append-only ledgers that hold against the table owner as well as `karar_app` — the pack-activation ledger (insert-only grant plus a statement-level immutability trigger, storing activation metadata and never pack content) and the two `SECURITY DEFINER` trigger-written history tables with `UNIQUE (row_id, version)` forbidding skipped or forked history (KAR-CTL-082, 088, IMPLEMENTED [P3.5]), plus per-resolution provenance pins recording which pack version, availability row, and entitlement a decision actually used (KAR-CTL-083). Local/test execution only — nothing runs anywhere to produce logs continuously, and **nothing watches any of these records** (KAR-RSK-003). 2026-08-18 (Phase 4): the client has a redacting logger as its only sink, with a test asserting that no record the transport produces contains a credential, header, or body. The fact that matters more than the mechanism: **no client log reaches the platform** — nothing is shipped off the device, and no crash reporter or analytics package exists in the dependency set — so there is no client-side record for anyone to read, which is a different thing from a record nobody reads. The one Phase 4 control that widens a *server-side* signal is the rate-limit store's connection at startup with readiness reporting it (KAR-CTL-105, IMPLEMENTED [P4]), which closed a defect where every fail-closed identity policy answered 503 on the first request after boot |
| 8.16 | Systems monitored for anomalies | PLANNED (20) | KAR-CTL-041; a single alert recipient will not count as on-call. 2026-08-15, partial: the signal substrate exists (metrics, traces, truthful `/readyz`, outbox lag/DLQ metrics — KAR-CTL-058, 062, 063) but nothing watches, alerts, or pages, so this row does not advance (KAR-RSK-003) |
| 8.17 | Clocks synchronized | PLANNED (17) | Relevant when distributed runtime exists |
| 8.18 | Privileged utilities restricted | PLANNED (17) | Runtime concern; control-plane gateway design already excludes ad-hoc production tooling |
| 8.19 | Software on operational systems controlled | PLANNED (17) | No operational systems; endpoint software per acceptable-use-policy |
| 8.20 | Networks secured | PLANNED (17) | KAR-CTL-032 |
| 8.21 | Network services secured | PLANNED (17) | KAR-CTL-032; DB transport `verify-full` per `docs/security/secrets.md` §10. 2026-08-18 (Phase 4): the client's transport posture is set at build time rather than trusted at runtime — arbitrary loads are permitted in **no** iOS configuration, the localhost App Transport Security exception is **added** only to a Debug bundle whose compiled environment is LOCAL after validation against an exact expected value and is removed-then-re-read on every other configuration, and the Android network security configuration is inspected in the built artifact. **Certificate pinning is deliberately absent** (accepted risk C11), so transport trust is the platform trust store's. The row stays PLANNED because there is no network service: no endpoint exists for any environment (KAR-CTL-108, IMPLEMENTED [P4]) |
| 8.22 | Networks segregated | PLANNED (17/20) | Sealed vault into its own boundary before production sealed data (ADR-0017) |
| 8.23 | Web access filtered | NOT_APPLICABLE | No managed corporate network or endpoint fleet exists to filter. Revisit: ≥3 staff or managed endpoints |
| 8.24 | Cryptography used properly, keys managed | APPLICABLE | [cryptography-and-key-management-policy](../../policies/cryptography-and-key-management-policy.md); ADR-0017; implementation Phases 13/20 (KAR-CTL-034, 035). 2026-08-15: **design only** — custody, provenance, rotation, and canary contracts exist as code with tests (KAR-CTL-064, 065, DESIGNED); no key management operates, no cloud KMS, no production keys, and this row deliberately does not advance. 2026-08-16 (Phase 3.5): **still not advanced, and for the third consecutive phase.** Phase 3.5 delivered no cryptographic mechanism at all; the only key material in play remains the local-only dev provider's, and TLS on `kararfinance.com` is `NOT_CONFIGURED` because nothing is served. 2026-08-18 (Phase 4): **still not advanced, for the fourth consecutive phase — and the reason changed, which is why it is restated rather than repeated.** Phase 4 is the first phase in which key material is in custody anywhere: session tokens sit in the iOS Keychain at `first_unlock_this_device` and in Android Keystore-backed storage that resets on key-material error. That custody is the **platform's**, and Karar's whole contribution to it is an accessibility class and a reset policy — no key is generated, wrapped, rotated, escrowed, or destroyed by anything in this repository. No KMS exists, no production key exists, no rotation has ever run. Choosing a platform's storage class well is a good decision; it is not key management, and this row will not read as though it were. Release-signing custody is recorded as **KAR-CTL-115, DEFERRED** — no signing material exists, so there is nothing to custody yet |
| 8.25 | Development follows a secure lifecycle | APPLICABLE | [secure-development-policy](../../policies/secure-development-policy.md); CI gates land Phase 1 (KAR-CTL-020, 021). 2026-08-18 (Phase 4): the lifecycle extended to a second toolchain rather than being re-invented for it — Dart and Flutter code passes an analyzer with `print` banned, a domain-purity scan that refuses framework, transport, storage, biometric, generated-DTO, and analytics imports anywhere under `features/*/domain/`, a generated-client drift check that regenerates in memory as its own CI step, and artifact-level assertions against real Android and iOS builds. **The row stays APPLICABLE for the reason it always has:** the policy is DRAFT and nobody has approved it (KAR-CTL-094, 099, 104, 112, IMPLEMENTED [P4]) |
| 8.26 | Security requirements set for applications | APPLICABLE | Threat model drives requirements; MODULE.md intake (KAR-CTL-021, 024). 2026-08-18 (Phase 4): the client's threat rows were written against the surface as it landed rather than after it. Three of the phase's defects are also the argument for why stated requirements are not enough on their own — a hardening resource that only the release build path rejects, a navigation dead end whose every unit assertion was individually correct, and a lockfile check that proved a host **appeared** rather than that it was the only one. Each passed a suite that was looking at the right file (KAR-CTL-094, 095, IMPLEMENTED [P4]) |
| 8.27 | Secure architecture principles applied | APPLICABLE | 26 ADRs + architecture tests as enforcement (KAR-CTL-019, 020) |
| 8.28 | Secure coding practiced | IMPLEMENTED | Standards in secure-development-policy. 2026-08-15: the first application code exists and the enforcement bites it — lint/type checks, CodeQL (KAR-CTL-029), architecture tests at Phase 2 scope (KAR-CTL-020), typed error and Result discipline in the platform code itself. 2026-08-18 (Phase 4): the same discipline in Dart — failures are values through a sealed `Failure` paired with `Result`, so a `switch` over them is exhaustiveness-checked; exceptions exist only inside the data layer; and a payload the decoder cannot classify becomes a typed contract-violation failure that degrades the client rather than crashing it (KAR-CTL-094, IMPLEMENTED [P4]) |
| 8.29 | Security testing through development | APPLICABLE | Architecture tests + scans in merge-blocking CI from Phase 1 (KAR-CTL-016, 020). 2026-08-18 (Phase 4): testing moved from source to artifact — nine endpoint refusal cases driven through real Gradle, each asserting the **stated reason** rather than only a failure; a merged manifest compared **exactly** rather than as a subset, so a permission disappearing is also a finding; packaged iOS bundles verified across four environments; an unzipped APK scanned for credential material; and a release assembly asserted **unsigned rather than debug-signed**. Two admissions belong beside that: assertions had been living on a lane that builds nothing and were skipping silently, which is precisely how they were passing; and the artifact and supply-chain lanes still **do not gate a merge**, which is why KAR-CTL-113 is **IMPLEMENTED [P4]** since the Phase 4 PR close (KAR-CTL-104, 111, 112, IMPLEMENTED [P4]). The row does not advance |
| 8.30 | Outsourced development supervised | NOT_APPLICABLE | No third-party development organization is engaged; AI-assisted work happens inside the same SDLC controls under maintainer review. Revisit: any contracted development |
| 8.31 | Development, test, production separated | PLANNED (17–19) | Ladder designed (`docs/architecture/environments.md`); staging is a hard pre-production gate (KAR-CTL-018, 030). 2026-08-16: the first environment-differentiated behaviour exists — an unapproved policy pack activates **only** in `local`, and per-environment capability deployment is a separate registry dimension from implementation (KAR-CTL-081, 086, IMPLEMENTED [P3.5]). The row stays PLANNED because **the environments themselves do not exist**: code that behaves differently per environment is not separated environments, and the predicate trusts the process's own configuration (KAR-RSK-023). 2026-08-18 (Phase 4): per-environment application identifiers now exist on both platforms — `com.kararfinance.app.local`, `.dev`, `.staging`, and `com.kararfinance.app` — derived from one rule, refused when the build is told nothing about its environment, and compared artifact to artifact in CI for the one environment pair both lanes build. Every iOS artifact carried the production identifier until this phase, whatever it was compiled for. **The row stays PLANNED, and the reason is the point:** four identifiers and a build that refuses without an endpoint are separated *artifacts*, not separated environments — no endpoint exists for any environment, so the only packages this repository can build are LOCAL ones (KAR-CTL-106, 109, 114, IMPLEMENTED [P4]) |
| 8.32 | Changes controlled | IMPLEMENTED | PR-only flow + merge-blocking CI (KAR-CTL-015, 016); branch protection verified 2026-08-15 (EV-007); **EXC-001 qualifies the approval leg and that qualification is material** — author and approver are the same person. 2026-08-15: schema change controlled the same way — checksum-verified forward-only migrations with the destructive-op guard (KAR-CTL-054, 055, IMPLEMENTED [P2]). 2026-08-16 (Phase 3.5): the class of change that would otherwise have escaped review is now inside it — jurisdiction and capability *policy* changes by reviewed code and reviewed migration only, with no runtime write path, no operator route, and unseeded permissions, so there is no path by which policy drifts without a diff (KAR-CTL-093, IMPLEMENTED [P3.5]); domain and DNS changes are brought under the same discipline by a one-change-at-a-time rule ([domain runbook](../../operations/domain-and-dns-runbook.md) §4). 2026-08-18 (Phase 4): two further classes of change came inside the diff. The Dart API client is **generated and never hand-edited**, with a `--check` regeneration running in memory as its own CI step ahead of the suite and a companion test asserting the generated files still declare themselves generated and carry matching digests — so the CI step cannot quietly stop proving anything. And dependency versions became reviewable rather than resolvable: exact pins, `--enforce-lockfile`, host exclusivity, and per-package checksums (5.21). **EXC-001 still qualifies the approval leg, and it has not moved** (KAR-CTL-095, 104, 106, 109, 110, 111, 114, IMPLEMENTED [P4]) |
| 8.33 | Test data selected and protected | APPLICABLE | Synthetic-only rule, currently true by construction (KAR-CTL-038). 2026-08-18 (Phase 4): the rule got its first enforcement of substance. The one piece of synthetic content with any pull toward production — a legal-document fixture — lives in its own package that appears in **no production `dependencies` block anywhere**, so a deployed install has no copy to serve rather than a runtime branch declining to serve one; the consent module names the package in exactly one place, through a guarded runtime resolution rather than an import. A test walks the real production dependency closure and the real build output asserting every fixture marker is absent, and carries a **positive control** proving the same scanner finds them in the fixture package itself — an absence test that cannot demonstrate its own scanner works proves nothing (KAR-CTL-103, IMPLEMENTED [P4]) |
| 8.34 | Systems protected during audits/tests | PLANNED (20) | Pen-test scoping at Phase 20; no system exists to protect or test |

## Tally (v0.5)

Re-derived in Phase 4 by counting the rows above, not carried forward from the previous version.

| Status | Count |
|---|---|
| APPLICABLE | 39 |
| PLANNED | 28 |
| NOT_APPLICABLE | 10 |
| IMPLEMENTED | 16 |
| OPERATING | 0 |
| **Total** | **93** |

**Nothing is certified.** No ISO/IEC 27001 certification is held, claimed, applied for, or sought at this stage; no certification body has been engaged and no external audit has been performed. This document is an internal applicability record maintained to the standard's structure, which is a different thing from conformity and a very different thing from certification. `IMPLEMENTED` means a mechanism exists in this repository with passing tests, `OPERATING` is held by nothing, and the independent review that ISO 27002 5.35 describes is a Phase 20 gate that nothing in-house substitutes for.

Every NOT_APPLICABLE row states its reason and (where one exists) a revisit trigger; the 7.x exclusions and the ISMS scope exclusions are the same facts viewed twice, deliberately.

**Phase 2 deltas (2026-08-15):** PLANNED → IMPLEMENTED: 5.13, 8.11, 8.12, 8.15. APPLICABLE → IMPLEMENTED: 5.33, 8.9, 8.28. Annotated without status change: 8.3 (DB roles), 8.10 (declarations exist, deriving mechanism Phase 5), 8.16 (signals exist, watching does not), 8.24 (design only, deliberately not advanced), 8.32 (EV-007 verified; migration discipline). Every move tracks a matrix-IMPLEMENTED control per this file's rule; IMPLEMENTED here still means evidence pending, and no row holds OPERATING.

**Phase 3 deltas (2026-08-16):** APPLICABLE → IMPLEMENTED: 5.16 (application identity lifecycle), 5.17 (application authentication material), 8.3 (application-layer restriction: FORCE RLS, deny-by-default RBAC), 8.5 (application authentication). Annotated without status change: 5.15 (RBAC enforcement of established rules), 5.34 (consent mechanism landed; no personal data processed, row stays PLANNED), 8.2 (peer-gated delegation; no privileged HTTP surface), 8.10 (Phase 3 lifecycle rows; retention placeholders pending Phase 3.5), 8.15 (security-event ledger, kill-switch history). 8.24 again deliberately not advanced — MFA secrets encrypt through the provider seam under a local-only dev provider; no key management operates, no cloud KMS, no production keys. Every move tracks a matrix-IMPLEMENTED [P3] control per this file's rule; IMPLEMENTED here still means evidence pending, and no row holds OPERATING.

**Phase 3.5 deltas (2026-08-16):** APPLICABLE → IMPLEMENTED: **5.15** (access control rules — the rules themselves are now typed, versioned, restrict-only policy resolved through eight deny-by-default gates: KAR-CTL-085, 086, 087, 088) and **8.32** (changes controlled — policy and capability change only by reviewed code and reviewed migration, with no runtime write path, no operator route, and unseeded permissions: KAR-CTL-093). Annotated without status change: 5.9 (the domain asset, USER_CONFIRMED with every hardening item TO_VERIFY), 5.12 (design-time classification of the subject election), 5.13 (all eleven Phase 3.5 tables carry lifecycle rows), 5.19 / 5.22 / 5.23 (Cloudflare scoped to registrar and DNS; new register rule 6), 5.31 (per-jurisdiction legal questions now typed slots — identified, not determined), 5.34 (pack pinning, subject-election privacy, typed processing bases — all failing closed because `qa/v1` decides nothing), 8.2 (unseeded permissions and grant-free registers as the control), 8.3 (48-table RLS inventory, dual-GUC subject isolation, server-sourced tenant binding), 8.9 (the deliberate code-versus-configuration boundary), 8.10 (retention slots typed as declared unknowns; no period decided, no mechanism built), 8.11 (reference-only subject-policy audit), 8.12 (the closed-field-set client boundary), 8.15 (three further owner-proof append-only ledgers). **8.24 deliberately not advanced for the third consecutive phase** — Phase 3.5 delivered no cryptographic mechanism at all. Every move tracks a matrix-IMPLEMENTED [P3.5] control per this file's rule; IMPLEMENTED here still means evidence pending, and **no row holds OPERATING**. No SoA row was added or removed.

**Phase 3.5 gate delta (2026-08-16, at close):** one further move, and it is the only status change made *at* the gate rather than during the phase. **5.18 APPLICABLE → IMPLEMENTED**, tracking KAR-CTL-014's own move, because the Phase 3.5 gate record is the first to contain an actual access review (§9) — the thing 5.18's correction note said had never happened. Tally re-derived: **40 / 28 / 10 / 15 / 0 = 93**. **8.24 remains deliberately not advanced**, and no row holds OPERATING. Nothing else moved at the gate.

**Phase 4 deltas (2026-08-18, against matrix v0.6 / KAR-CTL-094–116):** APPLICABLE → IMPLEMENTED: **5.21**, tracking matrix-IMPLEMENTED **KAR-CTL-110** (ICT supply chain — exact pins asserted by test across a second package ecosystem, lockfile host exclusivity with per-package checksums, `--enforce-lockfile` in every Flutter CI step, and one exact-pinned workspace override against GHSA-ggr8-5vv4-36mx / CVE-2026-40345). Annotated without status change: 5.9 (the first client-side assets), 5.15 (a client that enforces none of the rules it renders), 5.17 (device-side token custody at `first_unlock_this_device` and Keystore-with-reset), 5.19 / 5.22 (seven new unreviewed suppliers while CI-005 is still open and now falling due), 8.1 (an endpoint class Karar does not manage; no root or jailbreak detection, no screen-capture prevention), 8.3 (no new table, permission, or operator surface; a four-column projection enforced by the SELECT), 8.5 (single-flight refresh; a biometric unlock that authenticates nobody), 8.7 (pinning as the realistic malware control), 8.8 (an advisory acted on; lanes that do not gate), 8.9 (a build that refuses its own misconfiguration, and a runtime rule narrower than the build one), 8.10 (credential wipe as the first deletion mechanism, and what it cannot promise), 8.11, 8.12 (backup and device-transfer closure; screen capture unprevented), 8.15 (no client log leaves the device), 8.21 (build-time transport posture; no certificate pinning), 8.25, 8.26, 8.28, 8.29 (testing moved from source to artifact, on lanes that do not gate), 8.31 (four application identifiers, still no environments), 8.32 (a generated client and reviewable dependency versions), 8.33 (the fixture outside the production closure, with a positive control). **8.24 deliberately not advanced for the fourth consecutive phase**, on a changed reason: key material is in custody for the first time, and the custody is the platform's — and the one Phase 4 control mapped to 8.24 that would change that, release-signing custody (KAR-CTL-115), is **DEFERRED** with no signing material to custody. Two further Phase 4 controls are **DESIGNED rather than IMPLEMENTED** and both are named in the rows above rather than left to the matrix: KAR-CTL-116 (register traceability is still a hand-run sweep — see CI-011). IMPLEMENTED here still means evidence pending, **no row holds OPERATING**, and no SoA row was added or removed. **Phase 4's gate has not been held**, so none of the above is a gate outcome.

**Corrections from the Phase 3.5 independent review (2026-08-16), recorded separately from the deltas because they are fixes, not progress:** 5.18 said access rights were reviewed at gates when no gate record contained a review (CI-004); 5.22 said suppliers were monitored while five vendor rows carried a review date two gates past due, unraised (CI-005); 5.36's annotation now states that three closed gates checked controls while nothing checked the gates. **No status moved as a result of any of these** — each correction made a row's justification match reality, and in every case reality was the weaker of the two. The tally is unchanged by them.
