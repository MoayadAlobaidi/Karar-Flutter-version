# Continual Improvement

**Status:** ACTIVE register · **Owner:** Compliance Owner · **Version:** 0.3 · **Date:** 2026-08-16 · **Review:** every phase gate

**v0.3 (2026-08-16, Phase 3.5 close):** three entries closed on verifications the Phase 3.5 gate record actually performed rather than promised. **CI-001 CLOSED** — the gate record contains a completed §10 referencing this log, which is the loop fix working rather than merely existing. **CI-004 CLOSED** — the gate record contains a real §9 access review, which is also the trigger that moved KAR-CTL-014 DESIGNED → IMPLEMENTED. **CI-006 CLOSED** — the register-integrity sweep ran at the gate with **zero forks** (76 EV, 93 KAR-CTL, 30 KAR-RSK ids all resolving; cited count equal to defined count in all three), and the corrective-action decision is recorded: the sweep should graduate into the architecture-test suite as a merge-blocking check, owner Engineering Owner, target Phase 4. **CI-005 stays OPEN**, due the Phase 4 gate — a register re-read is not the vendor security review the NC is about, and closing it on one would repeat the substitution that raised it.

**v0.2 (2026-08-16, Phase 3.5):** the log is no longer empty, and the reason it was empty is itself the first thing recorded. Three gates closed while this file still said the first entry could not arrive before the Phase 1 gate; two of those gates produced independent-review findings that step 2 of the loop below requires to be logged here, and none were. **An unlogged finding is itself an NC — by this file's own rule — so the omission is logged as CI-001, and the historical findings are backfilled as CI-002 and CI-003 with their real dispositions.** The Phase 3.5 review is logged as CI-004…CI-006 alongside the register-hygiene findings it raised. Nothing here was invented to fill the table: every entry traces to a dated record already in the repository.

The Clause 10 mechanism: a log of nonconformities and corrective actions, plus the loop that feeds it.

---

## What counts as a nonconformity

- A control practiced differently from its matrix statement without a recorded exception
- An exception whose exit trigger fired without action
- Evidence missing where the [evidence plan](../soc2/type-ii-evidence-plan.md) says it must exist
- A phase closed without its gate report, or a gate item silently skipped
- A published claim contradicting built reality (the P1 pattern — also an assurance-registry event)
- An incident post-review finding a control that failed as designed

Improvement opportunities (better control, cheaper evidence, clearer policy) are logged too, marked `OFI` rather than `NC` — they carry no corrective-action obligation, only an owner and a decision.

## Log schema

| Field | Meaning |
|---|---|
| `id` | `CI-###`, stable |
| `type` | `NC` (nonconformity) · `OFI` (improvement opportunity) |
| `source` | gate review · incident review · independent-reviewer-agent finding · external review · ad hoc |
| `description` | What was found, with the control/policy/register reference |
| `root cause` | For NC: why it happened — pattern-level, in the spirit of `docs/legacy/security-findings.md` §9 (root causes, not symptoms) |
| `correction` | The immediate fix |
| `corrective action` | The change that prevents recurrence (control edit, new test, process change) |
| `owner` | Role |
| `opened / due / closed` | Dates; due defaults to the next phase gate |
| `verification` | How closure was confirmed, by whom, when |
| `status` | `OPEN` · `IN_PROGRESS` · `CLOSED` · `ACCEPTED` (Platform Owner accepts without corrective action, with reason) |

## The improvement loop

1. **Find** — each phase gate reviews: gate report findings, incident post-reviews, reviewer-agent output, stale evidence, fired exception triggers.
2. **Log** — every finding lands here within the gate cycle that found it; an unlogged finding is itself an NC.
3. **Fix** — correction now, corrective action with an owner and a due gate; corrective actions that change controls flow into the [control matrix](../control-matrix.md) via PR.
4. **Verify** — closure requires named verification, recorded; the gate after the due date checks it.
5. **Learn** — recurring root causes (two NCs sharing one) escalate to the Platform Owner as a systemic item, per the legacy audit's own method.

## What a reviewer-agent finding is, and is not

Recorded once, because the log's honesty depends on it. The independent-reviewer step runs **before** a phase PR opens: it inspects the integrated diff and the registers, and its findings are remediated or formally deferred before merge. Most such findings are therefore **pre-merge defect detection, not nonconformities** — the control was never practised differently from its statement, because the divergence never reached the default branch.

A reviewer finding **is** an NC when it shows a control or register that *was already merged* behaving differently from its written statement, or a gate item that was skipped. Those are the entries below. Findings caught and fixed inside the phase are summarized against the phase they belong to, so the log carries the whole picture without inflating the NC count.

**And the meta-rule that produced CI-001:** whichever way a finding is classified, it must be *classified here*. Leaving the log empty is not a way of deciding that nothing qualified.

## Log

| id | type | source | description | root cause | correction | corrective action | owner | opened | due | closed | verification | status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CI-001 | NC | Phase 3.5 independent-reviewer finding (H5) | **This log stayed empty across three closed gates while the loop below required entries.** Phases 1, 2, and 3 each ran an independent review producing findings, and step 2 says every finding lands here within the gate cycle that found it, with an unlogged finding itself an NC. The file's single row asserted the first possible entry was the Phase 1 gate — an assertion that stopped being true when Phase 1 closed and was never revisited | The gate report's required sections (`../phase-compliance-gate.md` §§1–8) never named this log, so nothing in the closing procedure ever pointed at it. A step that lives only in the file describing it does not get performed by the person reading a different file | Log backfilled at Phase 3.5: CI-002, CI-003 for the historical reviews; CI-004…CI-006 for the findings this review raised | Gate report gains a standing **§10 Continual improvement** section requiring the reviewer's findings to be logged or explicitly classified as not-NC, so the gate cannot close silently on an empty log again | Compliance Owner | 2026-08-16 | Phase 3.5 gate | 2026-08-16 | **Verified:** the Phase 3.5 gate record contains a completed §10 that references this log entry by entry and dispositions every finding of the phase's independent review. The log was not empty at a gate that produced findings — the condition whose absence created this NC | CLOSED |
| CI-002 | OFI | Phase 1 and Phase 2 independent-reviewer findings (historical, backfilled) | Phase 1: 0 BLOCKING / 2 HIGH / 6 MEDIUM / 6 LOW / 4 INFORMATIONAL — all HIGH and MEDIUM remediated before the PR opened (repository-visibility corrections in five compliance locations, committable Terraform stubs, stale Phase 0 stamps, branch protection applied and recorded rather than claimed, exception-trigger alignment, CI-enforcement wording matched to the activation registry); five LOW fixed, the remainder accepted with recorded rationale. Phase 2: 0 BLOCKING / 0 HIGH / 2 MEDIUM / 4 LOW / 6 INFORMATIONAL — all fixed pre-PR. Details in `../../phases/phase-01.md` and `../../phases/phase-02.md` | Not an NC: every finding was caught and closed before merge, so no merged control diverged from its statement. Logged as OFI so the Clause 10 record reflects that reviews happened and produced results | Already applied in the respective phases | None beyond CI-001's loop fix | Compliance Owner | 2026-08-16 (backfilled) | — | 2026-08-16 | Dispositions recorded in the two phase reports; re-read at backfill | CLOSED |
| CI-003 | OFI | Phase 3 independent-reviewer findings (historical, backfilled) | 0 BLOCKING / 3 HIGH / 2 MEDIUM / 4 LOW — every HIGH and MEDIUM resolved or **formally deferred** before the PR; the deferrals carry reason, owner, target, residual, and closure condition in the Phase 3 gate record. The review also surfaced the undocumented dormant tenant-bound surface, which became KAR-RSK-021 and closed at Phase 3.5 | Not an NC on the same grounds as CI-002. Recorded separately because it produced a carried risk rather than only fixes | Already applied at Phase 3 | None beyond CI-001's loop fix | Compliance Owner | 2026-08-16 (backfilled) | — | 2026-08-16 | Phase 3 gate record; KAR-RSK-021's closure at Phase 3.5 | CLOSED |
| CI-004 | NC | Phase 3.5 independent-reviewer finding (M9) | **KAR-CTL-014 states access rights "are reviewed at every phase gate". No gate record contains such a review, and the gate report's required sections had nowhere to put one.** A control practised differently from its matrix statement, with no recorded exception — the first bullet of "what counts as a nonconformity" | The control was written before the gate template, and nothing reconciled the two. The set being trivial (one maintainer, one SCM account, no environments) removed the pressure that would have exposed the gap | KAR-CTL-014's note corrected to state plainly that no review has occurred; status held at DESIGNED rather than quietly reworded to match what was being done | Gate report gains a standing **§9 Access review** section, making the review a required field from Phase 3.5 onward. Closure needs a gate record that actually contains one | Security Owner | 2026-08-16 | Phase 3.5 gate | 2026-08-16 | **Verified:** the Phase 3.5 gate record contains a completed §9 recording repository and CI access, the registrar/DNS account, the empty set of cloud and vendor accounts, and the application catalogue (14 permissions, 8 roles) with the four Phase 3.5 permissions noted as deliberately unseeded. KAR-CTL-014 moved DESIGNED → IMPLEMENTED on that record. Closed on the review having happened — not on it having been comprehensive, which with one maintainer and no environments it cannot yet be | CLOSED |
| CI-005 | NC | Phase 3.5 independent-reviewer finding (H6) | **Vendor review dates lapsed across two gates without the lapse being raised.** All five vendor rows read "Next review: Phase 2 gate" through the Phase 2 and Phase 3 closes; register rule 3 says a lapsed review is a gate finding, and none was raised. KAR-CTL-047's evidence cell still read "none yet" | Review dates lived only in the vendor register, and no gate section required reading them. Same root cause as CI-001 and CI-004: a check written in one file, never wired into the procedure that closes a phase | Register-level re-read performed and dated 2026-08-16, next dates set; KAR-CTL-047's note corrected to record the lapse and to state that a register re-read is **not** the vendor security review the control describes | Gate checklist item added for vendor review dates. Closure needs either a performed vendor security review or a recorded decision that the Phase 1 baseline stands, with a next date | Compliance Owner | 2026-08-16 | Phase 4 gate | — | A gate record showing the vendor rows checked against their dates | OPEN |
| CI-006 | NC | Phase 3.5 independent-reviewer findings (H4, M5, M6, M7, M10, M11) | **Register drift across the compliance corpus.** EV-217/218/219 held `COLLECTED` with location cells still reading "URL pending" though the instances existed; EV-007 carried a status outside the register's own vocabulary; EV-008 was `PENDING` while an executed gate record asserted its deltas were signed; twenty-two evidence-status citations in the control matrix still read `(PENDING)` after the Phase 2 close moved them to `COLLECTED`; the policy index and the Clause 4–10 mapping carried targets and versions two gates out of date | Cross-file statuses were refreshed by hand at the file being edited, never swept. Nothing mechanically checks that a status quoted in one register matches its source in another | All corrected at Phase 3.5 and the corrections recorded in each file's changelog; the control matrix now states explicitly that its evidence parentheticals are a per-version snapshot and the register wins | A scripted cross-reference sweep (EV/CTL/RSK ids, quoted statuses, cited paths) run at every gate, with the intent that it graduate into the architecture-test suite as a merge-blocking check | Compliance Owner | 2026-08-16 | Phase 4 gate | 2026-08-16 | **Verified:** the sweep ran at the Phase 3.5 gate with **zero forks** — 76 EV, 93 KAR-CTL, and 30 KAR-RSK ids cited anywhere in `docs/` all resolve to a defining row, cited count equals defined count in all three, every path cited in a Phase 3.5 evidence row exists on disk, and the matrix, SoA, and trust-services tallies agree. **Decision recorded:** the sweep graduates into the architecture-test suite as a merge-blocking check — owner Engineering Owner, target Phase 4 — because a hand-run sweep has now been the fix for this defect class twice, and the second time is where it should stop being manual | CLOSED |

**Phase 3.5 independent review, for the record:** 0 BLOCKING / 7 HIGH / 11 MEDIUM / 13 LOW. Findings in the compliance corpus are CI-001 and CI-004…CI-006 above; the remainder fall outside this directory and are dispositioned in the Phase 3.5 report. **No BLOCKING finding has ever been raised in any phase** — which is worth stating precisely because it is the one claim in this file that flatters, and it is true only of a system that has never been deployed or attacked.
