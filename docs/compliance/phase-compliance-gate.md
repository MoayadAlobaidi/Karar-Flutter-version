# Phase Compliance Gate

**Status:** DRAFT · **Owner:** Compliance Owner · **Approver:** Platform Owner (pending) · **Version:** 0.7 · **Date:** 2026-08-18 · **Review:** Phase 5 gate

**v0.7 (2026-08-18):** **Phase 4 gate record written and executed at close — outcome PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.** Twelve deferred items, each carrying all five required fields. §9 access review and §10 continual improvement performed. **Phase 4 implementation and its internal gates are complete; the PR is not yet open when this record is written, and nothing is merged; Phase 5 is NOT STARTED.** Two of this record's own nonconformities concern its own reporting — CI-011, an NC closed at Phase 3.5 on its correction while its corrective action stayed outstanding, and CI-013, a canonical count asserted without re-derivation. CI-005 remains OPEN and widened by seven unreviewed Phase 4 suppliers. EV-427 reaches the deadline this gate's predecessor set for it with nothing to cite.

**v0.6 (2026-08-16):** **Phase 3.5 gate record written and executed at close — outcome PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.** The prepared checklist's expected counts predated final integration and are corrected in the record against the executed clean-clone figures at head `5411b0d`. §9 Access review and §10 Continual improvement were **performed for the first time**, which closes CI-004 and CI-001 respectively and moves **KAR-CTL-014 DESIGNED → IMPLEMENTED** on its own stated trigger — the matrix tally moves to 16 DESIGNED / 62 IMPLEMENTED accordingly. **Phase 3.5 is COMPLETE; Phase 4 is NOT STARTED**, and no Phase 4 work, evidence, or control status is claimed anywhere in this document.

**v0.5 (2026-08-16):** Phase 3.5 gate checklist appended — **checklist only, outcome PENDING**; the gate record itself is executed and written by the lead at Phase 3.5 closeout. A required-fields rule added, applying to this and every later gate record.

**Two standing sections added at v0.5, and the reason is a finding rather than a refinement.** The Phase 3.5 independent review found that KAR-CTL-014's access review had never been performed at any gate, and that the Clause 10 nonconformity log had stayed empty across three gates that produced review findings. Both controls existed; neither had a place in the closing procedure, so neither happened. **§9 Access review** and **§10 Continual improvement** are that place. The root cause is recorded once, in [continual-improvement](iso27001/continual-improvement.md) CI-001: a check written only in the file that describes it does not get performed by someone reading a different file. A personal name was also removed from the Phase 3 record per [control-owners.md](control-owners.md) rule 1.

**v0.4 (2026-08-16):** Phase 3 gate record written at closeout — outcome PASS_WITH_DOCUMENTED_DEFERRED_ITEMS, six deferred items each with reason/owner/target/residual/closure.

**v0.3 (2026-08-16):** Phase 3 gate checklist appended — checklist only, outcome PENDING; the gate record itself is written by the lead at Phase 3 closeout.

**v0.2 (2026-08-15):** Phase 2 gate checklist appended — checklist only; the gate record itself is written by the lead at phase close.

Every phase ends with a Security/Compliance review whose output is a recorded gate report. This defines what that report must contain. It is the compliance half of the phase-closure template the docs process defines at [`docs/phases/PHASE_TEMPLATE.md`](../phases/PHASE_TEMPLATE.md) — the template carries the overall phase report; the sections below are what its security/compliance section must satisfy. The gate report itself is evidence (EV-008 pattern) and is referenced from the evidence register.

---

## The gate report — required sections

**1. Control status deltas.**
Which KAR-CTL controls changed status this phase (DESIGNED → IMPLEMENTED → OPERATING …), each with the pointer that justifies the move — a CI run, a settings export, a review record. A status change with no pointer does not pass. Statuses only ever move in the [control matrix](control-matrix.md); the report cites, never forks.

**2. Evidence generated and missing.**
EV rows collected or reviewed this phase; expected evidence that did not materialize (e.g. a [C1]-contingent control whose check never ran), each with a reason and a follow-up owner.

**3. Risk register update.**
New risks, re-scored risks, closed risks (EV-008 record). Explicit statement that the register was reviewed even when nothing changed.

**4. Exceptions.**
New exceptions opened, existing ones re-affirmed or closed, and any exception whose exit trigger fired but was not acted on — the last is a gate failure, not a note.

**5. Mapping deltas.**
Changes flowing into the SOC 2 [trust-services mapping](soc2/trust-services-mapping.md) and the ISO [Statement of Applicability](iso27001/statement-of-applicability.md) (e.g. a PLANNED Annex A control becoming real this phase). Nonconformities and corrective actions recorded in [continual-improvement](iso27001/continual-improvement.md).

**6. Claims reconciliation.**
Assurance Claim Registry entries touched by this phase's scope re-reviewed (`docs/security/assurance-claims.md` §3); any published promise the phase's output contradicts is named — this is the anti-P1 check.

**7. Residual risk statement.**
One paragraph, plain language: what the platform is still exposed to entering the next phase, and why that is acceptable (or what was accepted by whom if it is High/Critical).

**8. Next-phase entry criteria.**
The compliance preconditions for the next phase to begin — deferred controls that activate, evidence that must start flowing, legal/external gates that fall due (e.g. Phase 14's Amanat clearance, Phase 20's custody gates). Pulled from the [treatment plan](treatment-plan.md) and the [roadmap](../roadmap.md) gate lists, not invented per gate.

**9. Access review.** *(Added 2026-08-16 at the Phase 3.5 gate — see below for why.)*
Who holds access to what, re-read and recorded: repository and CI accounts and their roles, the registrar and DNS account, any cloud or vendor account that exists, and the application role and permission catalogue once one is seeded. Every role change since the last gate is named. **The set is trivial today — one maintainer, one SCM account, no environments — and it is recorded anyway**, because a review that only starts when it is hard has no baseline. KAR-CTL-014 asserted this happened at every gate from Phase 1; no gate record contained it, and the omission is logged as CI-004 in [continual-improvement](iso27001/continual-improvement.md). Also re-read here: vendor review dates against the [vendor register](vendor-and-subprocessor-register.md), whose lapse across two gates is CI-005.

**10. Continual improvement.**
Every finding from this phase's independent review is either logged in [continual-improvement](iso27001/continual-improvement.md) or explicitly classified as not-a-nonconformity with the reason. Open NCs from earlier gates are checked against their due dates. **An empty log at a gate that produced findings is itself a nonconformity** — that is the log's own rule, and it went unenforced across three gates (CI-001) because no gate section pointed at it. This section is that pointer.

## Rules

- **No gate, no phase close.** A phase without a recorded gate report is not complete, whatever the code says.
- **Honesty over completeness:** "not done, because X, owned by Y" passes review; silence or an unsupported OPERATING claim fails it.
- The reviewer is the Compliance Owner; where a phase's work was authored under EXC-001 (single person), the independent-reviewer-agent findings for the phase are attached to the report.
- Hard roadmap gates (staging before production, custody before sealed data, independent assessment before launch) cannot be waived by a gate report — they are preconditions the report verifies, not items it may accept as risks.

## Phase 1 specifics

The first gate report (closing Phase 1) must additionally record: verification of EV-007 (branch protection, per `docs/operations/repository-security-settings.md`), the workflow-file review against KAR-CTL-009 and KAR-RSK-005, first-run references for EV-001–EV-006, disposition of the `Archive.zip` item (KAR-RSK-012), a decision on the SCA blocking threshold (KAR-CTL-025 — currently report-only), and confirmation that every [C1]-contingent control's check actually exists in the merged CI.

## Phase 1 gate record — PASSED, 2026-08-15

- EV-007 verified row-by-row via the GitHub API; observed values in `docs/operations/repository-security-settings.md`.
- EV-001–EV-006 COLLECTED with first-run URLs (all 12 checks green; one transient Actions-allowlist failure fixed and recorded).
- `Archive.zip` (KAR-RSK-012) disposition: untracked and git-ignored in the Phase 1 PR; history deliberately not rewritten; risk row updated.
- KAR-CTL-025 decision: dependency audit stays report-only for Phase 2; the blocking dependency gate remains `dependency-review` (KAR-CTL-027). Tightening criterion unchanged in the settings doc.
- All [C1]-contingent controls confirmed: every referenced check exists in the merged CI and ran green.
- Independent review: 0 BLOCKING / 2 HIGH / 6 MEDIUM — all HIGH and MEDIUM remediated pre-PR ([phase report](../phases/phase-01.md)).
- No control moved to OPERATING or EVIDENCED at this gate; statuses remain as the matrix records.

## Phase 2 specifics — gate checklist

**Status: PENDING. This is the checklist, not the record.** The gate outcome (PASS or findings) is recorded by the lead at phase close, below this block, following the Phase 1 pattern. Prepared 2026-08-15 alongside the Phase 2 compliance updates (matrix v0.2, registers v0.2).

The Phase 2 gate report must verify, beyond the standard eight sections:

1. **EV-201–EV-219 first instances.** The Phase 2 PR CI run URL(s) recorded against the new evidence rows and their statuses moved PENDING → COLLECTED; the phase report's verification section (`docs/phases/phase-02.md`) referenced as the lead-local-run record of 2026-08-15. Any row that cannot cite a green run stays PENDING, with a reason and owner.
2. **Control deltas with pointers.** KAR-CTL-051–065 added (13 IMPLEMENTED [P2], 2 DESIGNED); KAR-CTL-033 and 040 moved to IMPLEMENTED [P2]; KAR-CTL-007, 008, 015 moved to IMPLEMENTED on the EV-007 verification. Each move must cite its pointer; confirm no control anywhere claims OPERATING or EVIDENCED and nothing is represented as deployed.
3. **Design-only boundaries hold.** KAR-CTL-064/065 remain DESIGNED; no cloud KMS, no production keys, no operating canary is claimed anywhere (SoA 8.24 deliberately not advanced).
4. **Policy approval decision.** The 14 DRAFT policies name this gate as their approval target ([policy-index](policy-index.md)); the gate records approval by the Platform Owner or the slip with a new target. Silence fails the gate.
5. **Evidence-store decision.** The interim-store exit was due "no later than the Phase 2 gate" ([evidence register](evidence-register.md), KAR-RSK-011): record the chosen store or the explicit re-acceptance of the interim.
6. **Risk review record (EV-008 pattern).** Register v0.2 deltas signed: KAR-RSK-014/015 added, 010 re-scored 12 → 9, 003 note revised, no closures; every other row confirmed reviewed.
7. **Exceptions re-affirmed.** EXC-001, 002, 003 re-affirmed (register v0.2); verify no exit trigger fired — the team is still one person.
8. **Mapping deltas match the matrix.** SoA v0.2 (seven rows to IMPLEMENTED, tally 47/28/10/8/0) and trust-services mapping v0.2 quote matrix v0.2 exactly; any disagreement resolves toward the matrix.
9. **Claims reconciliation.** The threat model's "Phase 2 platform threats" evidence refs (EV-201–EV-213) resolve one-for-one against the evidence register; assurance-claim entries touched by Phase 2 re-reviewed; architecture test 26 green on the closing run.
10. **Architecture-test activation.** Tests 5, 6, 23 ACTIVE and 25 deepened; 19 registry-active numbered tests plus the supplementary check passing (20 passing); self-test 22 seeded-violation cases (EV-217).
11. **Fresh-clone verification.** Executed at phase close and recorded (EV-218, KAR-CTL-046).
12. **KAR-CTL-025 threshold decision.** The Phase 1 gate deferred the SCA blocking threshold to this gate: decide, or re-defer with the reason and the tightening criterion re-affirmed.
13. **Database-reality checks.** Spot-verify in the CI logs: from-zero creation ran twice, a 42501 denial and a P0001 trigger denial actually occurred, and the destructive-reset guard refused outside its double condition.

## Phase 2 gate record — PASS_WITH_DOCUMENTED_DEFERRED_ITEMS, 2026-08-16

Evaluated against the checklist above at commit `324fce1` plus the closeout commit.

**Evidence status.** EV-201–EV-219 COLLECTED (CI run 31907338148, Security run 31907338141, both green on the final head; clean-clone record and lead local runs in the phase report). Reviewed: the independent reviewer re-executed every underlying suite and check and reproduced every count. Pending: formal evidence-file review in a protected store — the store itself is a deferred item below. Owner: Security Owner.

**Control status.** KAR-CTL-051–063 confirmed IMPLEMENTED with the [P2] local-only contingency (mechanism plus tests in-repo; deployed nowhere); KAR-CTL-064 and 065 remain DESIGNED — key custody and the integrity canary are design-only, no cloud KMS and no production key exists. Nothing was upgraded to OPERATING or EVIDENCED.

**Policies.** All fourteen remain DRAFT. Policy approval deferred — Platform Owner review required. Deadline: before the first non-local deployment (Phase 18 at the latest; earlier if an external audit engagement begins first).

**Interim evidence storage.** Decision recorded: non-sensitive engineering evidence is referenced through GitHub Actions artifacts and controlled local verification records. Sensitive evidence must never enter the public repository. A protected long-term evidence store remains undecided — owner: Platform Owner; risk retained (KAR-RSK-011 vendor concentration and run-URL retention limits); mandatory reassessment before Phase 4 or before any external audit engagement, whichever occurs first.

**Separation of duties.** EXC-001 stays open. Agent-based independent review is genuine technical review but is not independent human separation of duties; the exception and its two-engineer remediation trigger remain visible in the exceptions register.

**SCA policy.** dependency-review blocks newly introduced high-severity dependencies at the PR boundary (KAR-CTL-027); the package-manager audit job remains report-only under the approved policy (KAR-CTL-025 DESIGNED), with the tightening trigger and owner in `docs/operations/repository-security-settings.md`. Historical transitive findings are not claimed to be blocking. Residual risk carried in the risk register.

**Deferred items (with owners and targets):** policy approval (Platform Owner, before first non-local deployment) · protected evidence store (Platform Owner, before Phase 4 or external audit) · KAR-CTL-025 threshold tightening (Engineering Owner, criterion in the settings doc) · custody/canary implementation (Phase 13/20) · EXC-001 (team growth trigger).

**Outcome: PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.** No hard roadmap gate was waived; every deferred item carries an owner and a target.

## Phase 3 specifics — gate checklist

**Status: PREPARED, outcome PENDING. This is the checklist, not the record.** The gate is executed at Phase 3 closeout; its outcome (PASS or findings) is recorded by the lead below this block, following the Phase 1 and Phase 2 patterns. Prepared 2026-08-16 alongside the Phase 3 compliance updates (matrix v0.3, evidence register v0.4, risk register v0.3, SoA v0.3). Nothing in this checklist asserts or presumes an outcome.

The Phase 3 gate report must verify, beyond the standard eight sections:

1. **EV-301–EV-317 instances.** The Phase 3 PR CI run URL(s) recorded and EV-317 moved PENDING → COLLECTED; EV-301–EV-316's executed-run basis re-confirmed on the closing head; the phase report's verification sections (`docs/phases/phase-03.md`) completed and referenced. Expected counts to reproduce: full workspace suite 797 passed / 5 skipped (802 total) across 80 passed / 1 skipped test files, the skipped file being the KARAR_INTEGRATION-gated readiness suite, which runs in CI. Any row that cannot cite a green run reverts to PENDING with a reason and owner.
2. **Control deltas with pointers.** KAR-CTL-066–078 added (13 IMPLEMENTED [P3]); KAR-CTL-010, 011, 049 moved DEFERRED → IMPLEMENTED [P3]; KAR-CTL-020's Phase 3 activation note. Each move cites its pointer; confirm no control anywhere claims OPERATING or EVIDENCED and nothing is represented as deployed.
3. **Kill-switch guard mounting.** The integration wiring mounts `KillSwitchGuard`/`RequireOperationAllowed` on the registration, login, refresh, and invitation routes; verify on the closing head and close KAR-RSK-019 on that verification — or hold it OPEN with reason and owner. Until verified, kill-switch route enforcement is not claimed anywhere.
4. **Phase 3.5 boundaries hold.** No jurisdiction PolicyPack, capability-availability, or SubjectPolicySelection behaviour smuggled in; architecture test 21's registry activation gate for the Phase 3.5 pinning columns present (the run fails when 3.5 arrives without them); `LocalDevEncryptionProvider`/`LocalMailSink` still refuse outside local; SoA 8.24 deliberately not advanced — no cloud KMS, no production keys, no operating canary claimed anywhere.
5. **Permission-absence honesty.** `identity.account.disable`/`enable` remain documented-but-unseeded (migration 0050 header); `amanat.content.read` absence still test-pinned; no permission returns credential material; DB seed == code catalogue on the closing run (14 permissions, 8 roles).
6. **Policy approval decision.** The 14 policies remain DRAFT under the Phase 2 gate's deadline (before the first non-local deployment); the gate records Platform Owner approval or re-affirms the deadline. Silence fails the gate.
7. **Risk review record (EV-008 pattern).** Register v0.3 deltas signed: KAR-RSK-016–020 added, 009 note dated, no re-scores; the only closure candidate is KAR-RSK-019 under item 3; every other row confirmed reviewed.
8. **Exceptions.** EXC-001, 002, 003 re-affirmed or updated; EXC-002's compensating-control refresh (short-lived tokens with server-side revocation delivered as Phase 3 code) verified; confirm no exit trigger fired — the team is still one person.
9. **Mapping deltas match the matrix.** SoA v0.3 (four rows to IMPLEMENTED, tally 43/28/10/12/0) and trust-services mapping v0.3 quote matrix v0.3 exactly; any disagreement resolves toward the matrix.
10. **Claims reconciliation.** The threat model's "Phase 3 identity, tenancy, and access-control threats" evidence refs (EV-301–EV-313) resolve one-for-one against the evidence register; assurance-claim entries touched by Phase 3 re-reviewed; architecture test 26 green on the closing run.
11. **Architecture-test activation.** Tests 9 (tenant scoping), 21 (pinning), 22 (RLS coverage) ACTIVE and test 4 narrowed; 23 passed / 0 failed / 5 skipped with zero registry activation errors; built-in self-test 35/35 seeded cases (EV-315).
12. **Database-reality checks.** Spot-verify in the CI/local logs: the RLS inventory matches EV-316 (37 tables = 17 ENABLE+FORCE + 27 allow-listed, 7 in both); the adversarial suites' non-empty two-tenant seeding visible before denial assertions; escalation probes (SET ROLE, DDL, trigger disabling, `session_replication_role`) rejected; append-only ledgers hold against the migrator role.
13. **Fresh-clone verification.** Executed at phase close and recorded (per-phase frequency on the EV-218 row; KAR-CTL-046).
14. **KAR-CTL-025 threshold.** Unchanged from the Phase 2 gate decision (package-manager audit report-only; dependency-review blocking at the PR boundary); re-affirm the tightening criterion and owner, or act on it.

---

## Phase 3 gate record — executed 2026-08-16

**Outcome: PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.** Gate executed by the phase lead (Compliance Owner role) at close; the independent-reviewer-agent findings for the phase (0 BLOCKING / 3 HIGH / 2 MEDIUM / 4 LOW, all HIGH and MEDIUM resolved or formally deferred before the PR) are attached per the EXC-001 rule. All review here is maintainer-directed agent review, not organizational separation of duties.

**Standard sections.**

1. *Control deltas:* KAR-CTL-066–078 added IMPLEMENTED [P3]; KAR-CTL-010, 011, 049 DEFERRED → IMPLEMENTED [P3]; KAR-CTL-020 activation note extended — every move carries its pointer in matrix v0.3. No control anywhere claims OPERATING or EVIDENCED; nothing is represented as deployed. Reviewed per the closeout instruction across identity lifecycle, registration/verification, password authentication and recovery, MFA foundation, session lifecycle, refresh rotation and reuse detection, server-side revocation, RBAC, privileged access, tenant isolation, RLS, invitations, consent, legal-document lifecycle, re-consent, kill switches, security events, and audit trails: all IMPLEMENTED [P3] or DESIGNED with honest notes. Local-only encryption/signing/mail providers are recorded as exactly that — they establish no production key-management readiness (KAR-RSK-009 custody gates unchanged).
2. *Evidence:* EV-301–EV-319 COLLECTED (register v0.5) with executed-run references — CI run 31921545097 and Security run 31921545041 green at implementation head `8627f16`, clean-clone verification EV-319, suppression review EV-318. No expected evidence failed to materialize. Nothing is REVIEWED by an independent organization; nothing is EVIDENCED.
3. *Risk register:* v0.3 deltas signed (KAR-RSK-016–021 added across the phase; 009 note dated; no re-scores). KAR-RSK-019 CLOSED at this gate on the mounting verification (item 3 below). Every other row reviewed; KAR-RSK-021 carried into Phase 3.5 (deferred items below).
4. *Exceptions:* EXC-001 re-affirmed OPEN (the team is one person; no exit trigger fired). EXC-002 compensating-control refresh verified (short-lived tokens with per-request server-side revalidation delivered as code). EXC-003 re-affirmed unchanged.
5. *Mapping deltas:* SoA v0.3 (5.16, 5.17, 8.3, 8.5 → IMPLEMENTED; tally 43/28/10/12/0) and trust-services mapping v0.3 quote matrix v0.3 exactly; verified at close, no disagreement. 8.24 deliberately not advanced.
6. *Claims reconciliation:* threat-model EV-301–EV-313 references resolve one-for-one against the register; architecture test 26 green on the closing run; no published promise contradicted. The tenant-binding dormancy is stated wherever the tenant-bound surface is described (phase report, MODULE.md files, OpenAPI description, threat model).
7. *Residual risk:* entering Phase 3.5, the platform's principal exposures are: the dormant tenant-bound surface until session binding lands (KAR-RSK-021 — fail-closed, so exposure is unavailability, not disclosure); single-maintainer concentration (KAR-RSK-001/002, EXC-001); local-only key/mail custody with no production providers (KAR-RSK-009/020 — fail-closed constructors); no production deployment exists, so operational exposure is nil in substance. Acceptable because every gap denies rather than degrades open.
8. *Next-phase entry criteria:* Phase 3.5 begins only after PR #5 merges, the branch is deleted, and the new branch starts from the merge commit; Phase 3.5 owns the tenant-binding/bootstrap mechanism (KAR-RSK-021 closure), the PolicyPack/capability foundation the test-21 activation gate expects, and the Phase 3.5 compliance gate.

**Phase 3 checklist items 1–14:** all verified on the closing head. Notes where the record differs from the prepared expectation:

- Item 1: the prepared expected counts (797/5/802 across 80/1) predate final integration; the closing clean-clone counts are **807 passed / 5 skipped (812) across 83 passed / 1 skipped files** — the delta is the lead-integration tests (kill-switch mounts, composed error boundary, tenancy restriction case) and the close-out suppression-review test, all named in the phase report. EV rows updated to the executed numbers; nothing reverted to PENDING.
- Item 3: kill-switch guard mounting VERIFIED — register/login/refresh and invitation issue/redeem carry the consumer-declared gates, proven by the mount tests and the composed error-boundary test in the clean-clone run; KAR-RSK-019 closed on this verification.
- Item 6: policy approval — Platform Owner approval has NOT occurred; the 14 policies remain DRAFT and the deadline is re-affirmed: **Platform Owner review required before the first non-local deployment.** Nothing was approved on the owner's behalf.
- Item 13: fresh-clone verification executed and recorded (EV-319; phase report Close-out record).
- Item 14: KAR-CTL-025 threshold re-affirmed unchanged (report-only package audit; blocking dependency-review at the PR boundary); tightening criterion and owner unchanged.
- Additional at close: the Phase 3 security suppressions were individually re-reviewed (EV-318) — two exact-fingerprint gitleaks entries and one per-alert CodeQL dismissal, no broad suppressions.

**Deferred items (each with reason / owner / target / residual risk / closure condition):**

1. *Session tenant binding (KAR-RSK-021).* Reason: no safe binding mechanism existed in Phase 3 scope; the fail-closed design keeps the surface dormant rather than guessable. Owner: Engineering Owner. Target: Phase 3.5 (which owns the mechanism). Residual: tenant-bound endpoints unavailable; no disclosure exposure. Closure: binding/bootstrap mechanism implemented with its own adversarial and concurrency tests, and the risk row closed at the Phase 3.5 gate.
2. *Production signing/encryption key custody.* Reason: no non-local environment exists; local providers fail closed by construction. Owner: Platform Owner. Target: provider selection at Phase 17+, custody gates at Phase 20 (roadmap). Residual: identity flows needing mail/KMS cannot run outside local. Closure: real provider selection with custody evidence at those gates (KAR-RSK-009, 020).
3. *Separation of duties (EXC-001).* Reason: single maintainer; agent workstreams are technical, not organizational. Owner: Platform Owner. Target: exit trigger = engineering team ≥ 2 (exception register). Residual: author/approver identity. Closure: exception exit on team growth; until then merge-blocking CI plus reviewer-agent findings attached to every gate.
4. *Policy approval.* Reason: Platform Owner review pending. Owner: Platform Owner. Target: before the first non-local deployment. Residual: policies guide but do not bind formally. Closure: the Platform Owner's own recorded approval; never approved on that role-holder's behalf. *(Personal name removed 2026-08-16 per [control-owners.md](control-owners.md) rule 1 — roles only, in a public repository.)*
5. *Protected long-term evidence storage.* Reason: interim model keeps only safe references/summaries in-repo; no protected store exists. Owner: Compliance Owner. Target: revisit at the Phase 20 operational gates alongside off-host audit shipping. Residual: evidence files live in maintainer-controlled locations without organizational access control. Closure: protected store decision recorded with access design.
6. *Session-touch write amplification.* Reason: sliding-idle semantics chosen deliberately; optimization is a performance concern with no correctness effect. Owner: identity workstream. Target: first performance-sensitive phase. Residual: one UPDATE per bearer-carrying request. Closure: conditional touch (skip far from expiry) or a recorded decision to keep the semantics.

---

## Required fields — applies to this and every later gate record

**No field in a gate record may be left blank, dashed, or filled with a bare "PENDING".** A gate record is read later by people deciding whether to trust the platform, and an empty cell reads as "fine" when it means "unknown". Two rules, both enforced by the Compliance Owner before a record is accepted:

1. **Every field is answered.** If the honest answer is that something did not happen, the field says *what* did not happen and *why*, not nothing. "PENDING" alone fails; "PENDING — the run has not been executed; owner Engineering Owner; executes at PR open" passes.
2. **Every deferred item carries five things: reason · owner · target phase or date · residual risk · closure condition.** An item missing any of the five is not a deferral, it is an omission, and the gate does not pass with it. This is the shape the Phase 2 and Phase 3 records already use; it is written down here so it is a rule rather than a habit.

The same discipline applies to the registers a gate quotes: an evidence row that cannot cite a green run reverts to `PENDING` **with a reason and an owner**, a risk with no owner is not a recorded risk, and an exception with no exit trigger is not an exception.

## Phase 3.5 specifics — gate checklist

**Status: PREPARED, outcome PENDING. This is the checklist, not the record.** The gate is executed at Phase 3.5 closeout; its outcome (PASS, PASS_WITH_DOCUMENTED_DEFERRED_ITEMS, or findings) is recorded by the lead below this block, following the Phase 1, 2, and 3 patterns. Prepared 2026-08-16 alongside the Phase 3.5 compliance updates (matrix v0.4, evidence register v0.6, risk register v0.4, treatment plan v0.4, SoA v0.4, trust-services mapping v0.4, exceptions register v0.4, asset inventory v0.3, vendor register v0.2, and the domain runbook). **Nothing in this checklist asserts or presumes an outcome**, and preparing it is not passing it.

The Phase 3.5 gate report must verify, beyond the standard eight sections:

1. **EV-401–EV-429 instances.** The Phase 3.5 PR CI and Security run URLs recorded and **EV-426 moved PENDING → COLLECTED**; EV-401–EV-425's executed-run basis re-confirmed on the closing head and the implementation head recorded; the phase report's verification sections (`docs/phases/phase-03-5.md`) completed and referenced. Expected counts to reproduce (**as prepared, before final integration — the executed figures are in the record below and are higher; the prepared numbers are left here unedited so the delta stays visible**): full workspace suite 1100 passed / 5 skipped (1105 total) across 105 passed / 1 skipped test files (the skipped file is the `KARAR_INTEGRATION`-gated readiness suite, which runs in CI); architecture runner **24 passed / 0 failed / 4 skipped**, 0 registry activation errors, self-test **56/56**; docs-check 7/7; Prisma mapping 43 mapped tables. **Any row that cannot cite a green run reverts to PENDING with a reason and an owner.** EV-427 (domain hardening), EV-428 (independent review) and EV-429 (clean-clone) are expected to be resolved or explicitly re-stated as PENDING with reason and owner — silence on them fails the gate.
2. **Control deltas with pointers.** KAR-CTL-079–093 added (15, all IMPLEMENTED [P3.5]); **one existing control changed status** — KAR-CTL-004 DESIGNED → IMPLEMENTED on the three existing gate records; KAR-CTL-011, 020, 049, 076 note extensions verified against reality, and KAR-CTL-014 and 047 notes corrected to record what has *not* happened (CI-004, CI-005). Each addition cites its pointer. Confirm no control anywhere claims OPERATING or EVIDENCED, no evidence row anywhere claims EVIDENCED, and nothing is represented as deployed.
3. **Policy approval — records the deadline, never an approval.** The 14 policies remain **DRAFT** under the standing deadline set at the Phase 2 gate and re-affirmed at Phase 3: **Platform Owner review is required before the first non-local deployment.** The gate records either the Platform Owner's own recorded approval or the re-affirmed deadline with its owner. **Nothing is approved on the Platform Owner's behalf, by any workstream, at any gate.** Silence fails the gate.
4. **Separation of duties — EXC-001 stays open.** Verify no exit trigger fired (the team is one person) and that the record describes every review performed this phase — reviewer agent, security workstream, compliance workstream, suppression review — as **maintainer-directed technical review**, not organizational separation of duties or independent human review. A record implying otherwise is a gate failure, not a wording preference.
5. **Evidence completeness.** Every EV id cited by a Phase 3.5 control, risk, or assurance claim exists in the register; every EV-4xx the threat model cites is defined (EV-401–EV-414, one per threat row); every file path cited in a Phase 3.5 evidence row resolves on disk; and no evidence status anywhere is anything but PENDING, COLLECTED, or REVIEWED.
6. **`qa/v1` non-production posture.** Confirm on the closing head: `qa/v1` is `DRAFT` / `PENDING_LEGAL_REVIEW` with `approvalReference: null`, `clearedCapabilities` EMPTY, and **every** decision slot an explicit `PENDING_LEGAL_REVIEW` carrying its stated open question; `canActivate` refuses it outside `local`, and refuses any `APPROVED` claim without an approval reference everywhere. **No jurisdiction is approved and no pack is approved** — record that as a fact of the phase, not as a gap to be closed by anyone in this repository.
7. **Capability non-availability.** Confirm every capability in the registry is `NOT_IMPLEMENTED` and deployed in no environment, that the availability and entitlement tables ship with **no rows**, that Amanat declares no jurisdictions and is `HIDDEN`, that Zakat's Sharia gate is recorded as a non-engineering launch gate (KAR-RSK-007), that `FUNDRAISING` has no id and no descriptor, and that the four declared permissions are unseeded so every mutating use case refuses. **No capability resolves available anywhere** — verify the record says so plainly.
8. **Tenant binding and the closure of KAR-RSK-021.** Verify the binding mechanism on the closing head — server-side membership sourcing, bind-time re-verification, switch-time session **and refresh-family** rotation, verify → act → re-verify → compensate, uniform denial of arbitrary/revoked/expired/disabled targets, first-party tenant from typed configuration with no identifier literal in domain code — with its adversarial and concurrency tests (EV-411, 412, 420). **Close KAR-RSK-021 on that verification, or hold it OPEN with reason and owner.** Confirm the surviving residual is carried as KAR-RSK-030 rather than folded into the closure.
9. **Domain and vendor records.** Verify the `kararfinance.com` asset row records only confirmed facts (registrar and DNS Cloudflare, registration USER_CONFIRMED, hosting/traffic/API/email/proxy/CDN/WAF all NOT_CONFIGURED, **no DNS record configured**), that all seven hardening items remain **TO_VERIFY** unless someone actually checked and recorded an observed value with a date, that Cloudflare is registered as registrar and DNS only and **not** as a financial-data processor or subprocessor with the conditions that would change that stated, and that **no account identifier, credential, billing datum, invoice id, or screenshot** entered the repository. EV-427 stays PENDING until a real verification exists.
10. **Architecture-test activation.** Tests 12, 17 (second tier), 19, 21 (deferral machinery removed, real enforcement), 22 (anti-boilerplate allow-list rules), and 26 (repo paths resolve, EV ids well formed) verified on the closing run: **24 passed / 0 failed / 4 skipped**, 0 registry activation errors, self-test **56/56** (EV-422). Confirm test 21 no longer contains Phase 3.5 deferral machinery of any kind.
11. **Database-reality checks.** Spot-verify: the RLS inventory matches EV-423 (**48 tables = 22 ENABLE+FORCE + 33 allow-listed, 7 in both**); the Phase 3.5 adversarial suite reads its own non-empty data before every denial assertion (16 tests); the three new append-only ledgers hold against the table owner; migration `0086`'s CHECKs behave as written; and a fresh scratch database migrates clean through `0086` with the unused numbers (`0078`, `0079`, `0082`, `0084`, `0085`, `0087`–`0089`) left as gaps.
12. **Mapping deltas match the matrix.** SoA v0.4 (5.15 and 8.32 → IMPLEMENTED; tally **41 / 28 / 10 / 14 / 0** = 93) and trust-services mapping v0.4 quote matrix v0.4 (**93 controls; 17 DESIGNED / 61 IMPLEMENTED / 14 DEFERRED / 1 EXCEPTION / 0 OPERATING / 0 EVIDENCED** — 61 rather than 60 because KAR-CTL-004 moved DESIGNED → IMPLEMENTED on the three existing gate records) exactly; any disagreement resolves toward the matrix. Confirm 8.24 is again **not** advanced.
13. **Claims reconciliation.** The threat model's Phase 3.5 evidence refs (EV-401–EV-414) resolve one-for-one against the evidence register; AC-016 to AC-021 re-read and confirmed still `UNVERIFIED` — **no assurance claim may be marked VERIFIED at this gate**, because no human assurance review has been recorded; architecture test 26 green on the closing run.
14. **Risk review record (EV-008 pattern).** Register v0.4 deltas signed: KAR-RSK-021 closed on item 8, 022–030 added, notes dated on 010, 016, 017, 020, no re-scores; every other row confirmed reviewed. Treatment plan v0.4 rows exist for every open risk.
15. **Exceptions.** EXC-001, 002, 003 re-affirmed per the register's Phase 3.5 entry; confirm no exit trigger fired and **no exception was opened, closed, or re-approved**.
16. **KAR-CTL-025 threshold.** Unchanged from the Phase 2 and Phase 3 decisions (package-manager audit report-only; dependency-review blocking at the PR boundary); re-affirm the tightening criterion and owner, or act on it.
17. **The two new standing sections, performed for the first time.** §9 **Access review**: record who holds repository, CI, and registrar/DNS access, the role each holds, and any change since the Phase 3 gate — trivial today and recorded anyway; note that the application permission catalogue is unchanged and that the four Phase 3.5 permissions remain unseeded. §10 **Continual improvement**: confirm CI-001…CI-006 are logged with owners and due gates, and that this phase's review findings are each logged or explicitly classified as not-a-nonconformity. **An empty or unreferenced log fails this item.**
18. **Register-integrity sweep (CI-006).** Re-run the cross-reference checks and record the result: every EV / KAR-CTL / KAR-RSK id cited anywhere resolves; every quoted evidence status matches the register; every quoted control status matches the matrix; every cited repository path exists; the matrix, SoA, and trust-services tallies agree. **Zero forks is the pass condition**, and the gate records the number checked, not just the outcome. Also record a decision on CI-006's corrective action: whether this sweep graduates into the architecture-test suite as a merge-blocking check, with an owner and a target phase.
19. **Vendor review dates (CI-005).** Confirm every vendor row's next-review date is in the future and that the Phase 3.5 re-read is recorded as a **register re-read, not a vendor security review** — the distinction KAR-CTL-047's status depends on.

**Deferred items must each state reason, owner, target phase or date, residual risk, and closure condition** — the required-fields rule above, restated here because it is where gates most often go quiet. At minimum the following carry into the record unless something closed them: policy approval (Platform Owner, before first non-local deployment) · protected long-term evidence storage (Compliance Owner) · separation of duties, EXC-001 (Platform Owner, team ≥ 2) · production key custody (Platform Owner, Phases 17+/20) · the unseeded Phase 3.5 permissions and their operator surfaces (Phase 8) · the caller-settable pin cutoff, KAR-RSK-029 · legal review of every `PENDING_LEGAL_REVIEW` slot in `qa/v1` (Platform Owner with counsel; not an engineering task) · Sharia review for Zakat and legal clearance for Amanat (KAR-RSK-007, 008; non-engineering gates).

---

## Phase 3.5 gate record — executed 2026-08-16

**Outcome: PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.**

**Phase 3.5 is COMPLETE. Phase 4 is NOT STARTED** — no Phase 4 work exists, no Phase 4 evidence is claimed, and no control anywhere is advanced on the expectation of it.

Gate executed by the phase lead in the Compliance Owner role at close, against final head `5411b0d` (branch `claude/karar-v2-phase-3-5-jurisdiction-capabilities`, PR #6). The independent-reviewer findings for the phase — **0 BLOCKING / 7 HIGH / 11 MEDIUM / 13 LOW**, every HIGH and MEDIUM resolved before the PR — are attached per the EXC-001 rule (EV-428). **All review recorded in this document is maintainer-directed technical review. It is not organizational separation of duties, and it is not independent human review**; the reviewing role and the reviewed party resolve to one person.

**Why PASS_WITH_DOCUMENTED_DEFERRED_ITEMS and not PASS.** Every checklist item was verified and every mechanism behaved as specified, so this is not a FAIL. But a plain PASS would assert there is nothing outstanding, and there are eleven deferred items below — including one evidence row that can cite nothing (EV-427), an entire class of legal decisions nobody in this repository may take, and a consent endpoint that answers 503 to every caller. The deferrals are recorded, owned, and dated; they are not resolved. **No hard roadmap gate was waived**, because none of them was reachable to waive: staging-before-production, custody-before-sealed-data, and independent-assessment-before-launch all sit in later phases and none was touched.

### Verified figures this gate rests on

All from a clean clone of `5411b0d`, executed 2026-08-16 in the window 16:10–16:12 UTC, reusing nothing:

| Check | Executed result |
|---|---|
| Workspace suite | **1109 passed / 5 skipped (1114 total)** across **105 passed / 1 skipped (106) test files** |
| Readiness suite (the one skipped file, `KARAR_INTEGRATION`-gated) | run separately — **5 passed** |
| Architecture runner | **24 passed / 0 failed / 4 skipped** (deferred by activation phase); registry errors **0**; built-in self-test **56/56** |
| Documentation checks | **7/7** |
| Prisma mapping / drift | **43 mapped tables match the live database** |
| Migrations | **38 files, all 38 applied from zero**; `db:verify` clean, nothing pending |
| Database posture | **48 tables — 22 RLS ENABLE+FORCE, 33 allow-listed, 7 deliberately both** |
| OpenAPI | **30 merged paths** (`packages/api-contracts/openapi/openapi.yaml`) |
| Repository shape | **24 module directories (12 with code), 7 packages** |
| CI / Security | **12 checks, all green** on `5411b0d`; **8 branch-protection-required** |

CI run https://github.com/MoayadAlobaidi/Karar-Flutter-version/actions/runs/31953306622 · Security run https://github.com/MoayadAlobaidi/Karar-Flutter-version/actions/runs/31953306623.

**The prepared checklist's expected counts (1100 / 5 / 1105) predate final integration and are superseded by the figures above.** The delta is integration and close-out test work, not a change in what passes. Nothing reverted to `PENDING` as a result; the direction of the delta is more tests passing, not fewer.

### Standard sections

**1. Control status deltas.** KAR-CTL-079–093 added (15, all `IMPLEMENTED [P3.5]`), each citing its mechanism and test paths in matrix v0.4. Two existing controls moved, both on their own written triggers: **KAR-CTL-004** DESIGNED → IMPLEMENTED on the three existing gate records (recorded at v0.4, before this gate), and **KAR-CTL-014** DESIGNED → IMPLEMENTED at this gate, because §9 below is the first gate record to actually contain an access review — which is precisely the trigger the row stated. Note extensions verified against reality on KAR-CTL-011, 020, 049, 076; corrected notes on KAR-CTL-014 and 047 record what has *not* happened (CI-004, CI-005). Matrix tally re-derived rather than copied: **93 controls — 16 DESIGNED / 62 IMPLEMENTED / 14 DEFERRED / 1 EXCEPTION**. **No control anywhere claims OPERATING or EVIDENCED, no evidence row claims EVIDENCED, and nothing is represented as deployed**, because nothing is.

**2. Evidence generated and missing.** EV-401–EV-426 and EV-428–EV-430 are **COLLECTED** with executed references (register v0.7). EV-426, EV-428, EV-429 moved PENDING → COLLECTED at this gate because their events occurred; EV-430 was added for the suppression review. **Missing, and named rather than absorbed: EV-427** — the domain and DNS hardening verification. Reason: no repository-verifiable evidence of any registrar or DNS setting exists, and every one of the seven §3 checklist rows is `TO_VERIFY`. Owner: Security Owner. Target: the Phase 4 gate at the latest, immediately if any DNS record is created or the Cloudflare proxy is enabled. It stays `PENDING` and is **not** recorded against a self-written document. Nothing is `REVIEWED`: formal evidence-file review needs a protected store that does not exist.

**3. Risk register update.** Register v0.4 deltas signed: **KAR-RSK-021 CLOSED** at this gate on the verification in §8 below — its own stated closure condition, met by the delivered mechanism plus its adversarial and concurrency tests (EV-411, 412, 420). KAR-RSK-022–030 added with the Phase 3.5 mechanisms; notes dated on 010, 016, 017, 020; **no re-score**. Every other row was re-read and confirmed unchanged. The surviving residual of KAR-RSK-021 is carried as **KAR-RSK-030**, not folded into the closure — a bound session can outlive the membership that justified it, because RLS cannot re-check membership.

**4. Exceptions.** EXC-001, 002, 003 re-affirmed OPEN per exceptions register v0.4. **No exception was opened, closed, or re-approved, and no exit trigger fired** — the team is still one person. EXC-001 carries the explicit statement that this phase's several parallel agent workstreams, reviewer included, are **not** separation of duties. EXC-002's compensating-control facts were refreshed (a tenant switch now rotates the refresh family too). Phase 3.5's residuals — the caller-settable pin cutoff, the mislabelled-environment path, the bound-session window — are **risks with named owners** (KAR-RSK-029, 023, 030), not deviations from a stated control, so none became an exception.

**5. Mapping deltas.** SoA v0.4 (5.15 and 8.32 → IMPLEMENTED) and trust-services mapping v0.4 quote matrix v0.4; both re-swept at this gate and updated for the KAR-CTL-014 move. **8.24 deliberately not advanced for the third consecutive phase** — Phase 3.5 delivered no cryptographic mechanism at all. Nonconformities and corrective actions are in [continual-improvement](iso27001/continual-improvement.md); see §10.

**6. Claims reconciliation.** The threat model's Phase 3.5 evidence refs **EV-401–EV-414 resolve one-for-one** against the register. **AC-016 to AC-021 re-read and confirmed still `UNVERIFIED`** — no assurance claim was marked VERIFIED at this gate, because no human assurance review has been recorded against any of them. Architecture test 26 green on the closing run, now additionally requiring that a claim's cited repository paths resolve on disk. **No published promise is contradicted by this phase's output**; where the output is weaker than a reader might assume, §"Unresolved states" below says so in the gate record itself rather than only in the phase report.

**7. Residual risk statement.** Entering Phase 4, the platform's exposure is dominated by what does not exist rather than by what is weak. Nothing is deployed, no capability resolves available anywhere, no jurisdiction or pack is approved, and no personal data is processed — so the operational attack surface is nil in substance. The real residuals are structural: one maintainer holds every role and every credential (KAR-RSK-001/002, EXC-001); key custody and mail remain local-only fail-closed stubs (KAR-RSK-009/020); a bound session can outlive its membership for a bounded window (KAR-RSK-030); and the pin creation-cutoff keys on a caller-settable timestamp (KAR-RSK-029). This is acceptable entering Phase 4 because **every gap denies rather than degrading open**, and because Phase 4 builds a client against this surface without adding a deployment. It would not be acceptable entering a phase that deploys anything.

**8. Next-phase entry criteria.** Phase 4 begins only after PR #6 merges and a new branch starts from the merge commit. Phase 4 owns, as entry work: **restoring consent acceptance** (below); **making bootstrap capability-resolution failure distinguishable from a legitimately empty capability set** before any client relies on it; and a **minimal safe `OperatingEntity` projection** in the bootstrap response. Phase 4 does **not** unlock policy approval, capability implementation, or any operator surface — those are Phases 8+ and legal engagements. No Phase 4 control is pre-activated by this record.

**9. Access review — performed for the first time.** *(Standing section added at v0.5; this is its first execution, which is what closes CI-004 and moves KAR-CTL-014.)*

| Access holder | Scope and role | Change since the Phase 3 gate |
|---|---|---|
| Sole maintainer | GitHub repository owner (admin); GitHub Actions; all CI configuration | None |
| Sole maintainer | Cloudflare registrar and authoritative DNS account for `kararfinance.com` | None. **The account's own controls are unverified** — all seven hardening rows are TO_VERIFY (EV-427) |
| Cloud / vendor accounts | None exist | None — no cloud account, no environment, nothing provisioned |
| Application role and permission catalogue | **14 permissions, 8 roles**, seeded by migration and asserted equal to the code catalogue on the closing run | Unchanged this phase |
| Phase 3.5 declared permissions | `jurisdiction.assignment.manage`, `jurisdiction.pack.activate`, `capability.availability.manage`, `capability.entitlement.manage` — **all four deliberately unseeded and absent from the code catalogue** | Added as declarations only; deny-by-default means every mutating use case in these modules refuses today |

**The reviewed set is trivial — one person, one SCM account, one registrar account, no environments — and it is recorded anyway**, because a review that only begins when it is hard has no baseline to compare against. Two honest observations rather than a clean bill: the maintainer's access cannot be separated from itself while the team is one person (EXC-001), and the registrar account's security posture is asserted, not observed (EV-427). Vendor review dates were also re-read here per CI-005: every row now carries a dated 2026-08-16 register-level re-read and a future next date. **That re-read is a register re-read, not a vendor security review** — which is exactly why KAR-CTL-047 stays DESIGNED.

**10. Continual improvement — performed for the first time.** CI-001…CI-006 are logged with owners and due gates. Disposition at this gate: **CI-001 CLOSED** (verification is this record's §10 existing and referencing the log — the loop defect it recorded is fixed by the standing section being performed, not merely written); **CI-004 CLOSED** (verification is §9 above, a gate record that actually contains an access review); **CI-006 CLOSED** (the register-integrity sweep was run at this gate with **zero forks** — see item 18 — and the corrective-action decision is recorded there); **CI-002 and CI-003 remain CLOSED** as backfilled; **CI-005 remains OPEN**, due the Phase 4 gate, because no vendor *security review* has been performed and recording a register re-read as one would be the very substitution the NC was raised about. Every finding from this phase's independent review is either logged above or dispositioned in the Phase 3.5 report; **the log is not empty at a gate that produced findings**, which was the rule that went unenforced across three gates.

### Gate scope — the matters this gate must cover explicitly

Recorded item by item so none can be satisfied by silence.

1. **PolicyPack governance.** PolicyPacks are **pure reviewed code, never database rows** — a typed pack per (jurisdiction, version) with a DRAFT / PENDING_LEGAL_REVIEW / APPROVED / RETIRED lifecycle and a three-arm `PolicyDecision` union, so an undecided legal question is a typed state rather than a fabricated answer. `validatePack`/`validatePackSet` run **before any write** and refuse on any finding. The activation ledger is append-only twice over and stores **metadata only, never pack content** (KAR-CTL-080, 081, 082; EV-402, 403, 415).
2. **`qa/v1` DRAFT / PENDING_LEGAL_REVIEW.** Confirmed on the closing head and proven directly in the clean-clone run: `DRAFT`, `reviewStatus: PENDING_LEGAL_REVIEW`, **`approvalReference: null`**, **`clearedCapabilities` EMPTY**, and **every** decision slot an explicit `PENDING_LEGAL_REVIEW` carrying its stated open question. `canActivate` **DENIED for production, staging, and dev, and ALLOWED only for local**. **No jurisdiction is approved and no pack is approved.** That is a fact of this phase, not a gap for anyone in this repository to close — approval is a legal act (EV-416; KAR-RSK-023).
3. **No public capability clearance.** No pack clears any capability. `qa/v1`'s cleared set is empty by construction, and Amanat — the one disclosure-bearing capability — declares **zero jurisdictions**, so no pack can reach it and no client can learn it exists. Nothing was cleared for any public audience at this gate (KAR-CTL-086; EV-416, 418).
4. **Capability deny-by-default.** Eight gates whose **order is itself the control**: gates 1–4 (descriptor, environment, jurisdiction and pack clearance, availability row) consume **no grant-like input** and run before gates 5–8 (entitlement, consent, licence, provider), so nothing a grant can say widens a ceiling denial. Absence denies at every gate; a store outage denies rather than defaulting; the resolver never returns an untyped allow. Monotonicity is proven as a **property** over roughly 2,400 exhaustive plus 2,000 seeded randomized configurations. Verified directly in the clean clone: **all seven capabilities `NOT_IMPLEMENTED` and deployed nowhere**, so gate 1 denies all seven before any later gate is reached (KAR-CTL-087; EV-405, 406, 417, 418).
5. **Tenant entitlements.** RLS ENABLE+FORCE on the tenant GUC; a `SECURITY DEFINER` trigger-written append-only history with `UNIQUE (row_id, version)`; revocation as a **status, never a deleted row**; `version` incrementing by exactly one per UPDATE; windows validated **temporally at read time**, so a lapsed window denies while the row still reads ACTIVE. The table carries **no plan, price, or subscription concept**, and both the availability and entitlement tables ship with **no rows** (KAR-CTL-088; EV-407).
6. **SubjectPolicySelection privacy.** Classified CONFIDENTIAL; RLS ENABLE+FORCE on **both** principal GUCs, so a same-tenant different-user read returns zero rows and a forged-identity INSERT is refused by `WITH CHECK`; immutable with trigger-bounded supersession; restrict-only against the pack's permitted option set; **audited by reference and pin only, never option content**; publishes no events **by rule** and exposes no HTTP. No staff, support, tooling, or analytics read path exists (KAR-CTL-090; EV-409, 410).
7. **Tenant binding — and the closure of KAR-RSK-021.** Verified on the closing head: binding moves null → value **only from a server-side membership read, never from the request**; membership re-verified at bind time; a switch atomically revokes the old session **and its refresh family** before issuing the new bound session; verify → act → re-verify → compensate; arbitrary, revoked, expired-window, and disabled-tenant targets denied uniformly without reaching the rebind seam; the first-party tenant resolved from typed configuration with **no identifier literal in domain code** — with the adversarial and concurrency tests the closure condition named. **KAR-RSK-021 is CLOSED on this verification.** The residual is carried separately as **KAR-RSK-030** and is not folded into the closure (KAR-CTL-091; EV-411, 412, 420).
8. **Bootstrap exposure rules.** A **closed field set picked name by name**, not a filtered internal object: hidden capabilities, Amanat's existence, unimplemented and pending-legal capabilities, licence internals, full pack content, raw consent evidence, and internal audit or configuration data are all outside the shape. A HIDDEN capability is omitted in **every** state including a fabricated ALLOWED; a denial whose reason is not client-surfaceable is omitted **entirely** rather than rendered as `available: false`. The surface consumes the single client-safe projection **unenriched**, so the filter cannot drift into two implementations. Residual recorded, not claimed away: **omission hides the reason, not that a request took a different path — response-timing equivalence is deliberately not asserted this phase** (KAR-CTL-089, 092; EV-408; KAR-RSK-028).
9. **RLS.** Inventory re-derived at this gate rather than quoted: **48 migration-created tables — 22 RLS ENABLE+FORCE, 33 allow-listed with per-table justification, 7 deliberately in both** (the bootstrap-armed identity tables, which carry token-scoped or pre-auth policies *and* allow-list documentation). Union 48, **zero tables uncovered**. Test 22 gained anti-boilerplate rules: each allow-list reason must state why no tenant or subject predicate fits and describe the actual compensating controls, so a copied justification fails the run (KAR-CTL-011, 071; EV-423).
10. **Policy-version provenance.** Two distinct mechanisms, both verified. Legal-consequence **evidence** is schema-bound to the policy that produced it: migration `0086` pairs every version column with a NOT NULL pin-state column over a closed vocabulary, CHECKs tie `PINNED` to a non-null value, a creation-cutoff CHECK refuses the historical `PRE_*` states for rows created from Phase 3.5 on, and guard triggers cover the pin columns. Separately, every capability **resolution** records the pack version, availability row id and version, and entitlement id and version it used, so two resolutions racing a change disagree explainably (KAR-CTL-083, 084; EV-414, 419).
11. **Cloudflare and domain governance.** **Only three things are confirmed facts: the domain is registered, Cloudflare is the registrar, and Cloudflare is the authoritative DNS provider.** Registration status is `USER_CONFIRMED` on the Platform Owner's statement, not independently verified from this repository. **Nothing is configured** — no hosting, no DNS record, no mail, no proxy, no CDN, no WAF. Cloudflare is registered as registrar and DNS **only**, explicitly **not** as a financial-data processor or subprocessor, with the conditions that would change that classification written down; the first of them (enabling proxy/CDN/WAF) requires a DPA *before* the switch is flipped. All seven hardening rows remain **TO_VERIFY** and stay that way, because no repository-verifiable evidence exists. **No account identifier, credential, billing datum, invoice id, or screenshot entered the repository**, and none may (EV-427; KAR-CTL-047).
12. **Change control.** Every change reached the branch through PR #6 with 12 green checks, 8 of them branch-protection-required; admins are bound. Phase 3.5 extends change control **to policy itself**: packs and capability descriptors are reviewed code rather than rows, the country/jurisdiction/settings registers carry **no `karar_app` write grant at all**, and all four declared permissions are unseeded — so a policy change is necessarily a reviewed diff, and no operator can change any of it at runtime. Domain and DNS changes come under the same discipline **by runbook rule rather than by mechanism**, which is a weaker guarantee and is recorded as one (KAR-CTL-015, 016, 093; EV-426).
13. **Evidence status.** 30 Phase 3.5 rows: **29 COLLECTED, 1 PENDING (EV-427), 0 REVIEWED**. No status anywhere in the register is outside `PENDING` / `COLLECTED` / `REVIEWED`, and **nothing anywhere is EVIDENCED**. Every file path cited in a Phase 3.5 evidence row resolves on disk (item 18).
14. **Unresolved legal decisions.** Recorded as unresolved, with nobody in this repository able to resolve them: every `PENDING_LEGAL_REVIEW` slot in `qa/v1` — consent requirements, processing bases, retention periods, identity-verification requirements, disclosure obligations, currency policy, and AI-processing permission for Qatar; **jurisdiction approval itself**; **Sharia review for Zakat** (no scholar, board, or certificate exists — KAR-RSK-007); and **per-jurisdiction legal clearance for Amanat** (KAR-RSK-008). A decision lands as a **new pack version with its approval reference**, because published versions are immutable. Owner: Platform Owner with counsel. These are not engineering tasks and no engineering work can discharge them.
15. **Unresolved long-term evidence storage.** Still unresolved, and now overdue against its own stated trigger: the Phase 2 gate set "before Phase 4 or before any external audit engagement, whichever occurs first" as the mandatory reassessment point, and **Phase 4 is the next phase**. The interim model stands — non-sensitive references and summaries in-repo, GitHub Actions run URLs and artifacts, nothing sensitive ever in a public repository. Retained risk: run-URL retention limits and vendor concentration (KAR-RSK-011). Carried as a deferred item below with its target now falling due.
16. **The single-maintainer exception.** **EXC-001 stays OPEN.** No exit trigger fired — the team is one person. Phase 3.5 was delivered by several parallel agent workstreams with a reviewer among them, and **that is not separation of duties**: the workstreams are technical divisions of labour directed by one maintainer, and every one of them, plus the review of all of them, resolves to the same person. The exit trigger remains a second **engineer**, not a second agent. Every review named in this record — reviewer agent, security workstream, compliance workstream, suppression review, this gate — is maintainer-directed technical review.
17. **Policy approval status.** **All 14 policies remain DRAFT.** The Platform Owner has **not** approved them, and **nothing was approved on the Platform Owner's behalf at this gate, by any workstream**. The standing deadline set at the Phase 2 gate and re-affirmed at Phase 3 is re-affirmed again here: **Platform Owner review is required before the first non-local deployment.** The approval, when it happens, must be the Platform Owner's own recorded act.

### Checklist items 1–19 — verification notes

All nineteen verified on the closing head. Where the record differs from the prepared expectation:

- **Item 1:** the prepared counts (1100 / 5 / 1105) predate final integration; the executed clean-clone counts are **1109 passed / 5 skipped (1114) across 105 passed / 1 skipped (106) files**, with the gated readiness suite **run separately and passing 5**. EV rows updated to the executed numbers. **Nothing reverted to PENDING.** EV-426, 428, 429 resolved to COLLECTED; EV-427 explicitly re-stated as PENDING with reason, owner, target, residual, and closure condition — not left silent.
- **Item 2:** as §1. One addition to the prepared expectation: **KAR-CTL-014 also moved**, DESIGNED → IMPLEMENTED, because §9 fired its stated trigger. The prepared tally of 61 IMPLEMENTED is therefore **62**, and 17 DESIGNED is **16**. Recorded as a delta rather than silently absorbed.
- **Items 3, 4, 15, 16:** policy approval re-affirmed as deadline only; EXC-001 open with no exit trigger; no exception opened, closed, or re-approved; KAR-CTL-025 threshold unchanged (package-manager audit report-only, `dependency-review` blocking at the PR boundary), tightening criterion and owner unchanged.
- **Items 6, 7:** `qa/v1` posture and capability non-availability **proven directly in the clean-clone run rather than inferred from tests** — the denial matrix for production, staging, dev, and local was executed, and the seven descriptors were read at runtime.
- **Items 10, 11:** architecture runner 24 / 0 / 4 with 0 registry activation errors and self-test 56/56; test 21 confirmed to contain **no Phase 3.5 deferral machinery of any kind**. RLS inventory re-derived independently at this gate (48 = 22 + 33 − 7 both, 0 uncovered). Migration count re-derived as **38 files applied from zero**; the deliberate gaps (`0078`, `0079`, `0082`, `0084`, `0085`, `0087`–`0089`) remain gaps and were not backfilled.
- **Item 12:** SoA and trust-services mapping re-swept against the matrix and updated for the KAR-CTL-014 move, which carries one further consequence the prepared checklist did not anticipate: **SoA 5.18 moves APPLICABLE → IMPLEMENTED**, because 5.18 is the Annex A row KAR-CTL-014 implements and its correction note said the review had never happened. Re-derived SoA tally: **40 / 28 / 10 / 15 / 0 = 93** (prepared: 41 / 28 / 10 / 14 / 0). Trust-services mapping re-quotes KAR-CTL-014 as `(I)` in CC4.1–CC4.2, CC6.2, and CC6.3, and corrects CC1.2 from three gate records to four. **8.24 again not advanced.** All three tallies were re-derived by counting rows, not carried forward.
- **Item 18 (register-integrity sweep, CI-006):** run at this gate. **Zero forks.** Checked: **76 EV ids**, **93 KAR-CTL ids**, and **30 KAR-RSK ids** cited anywhere in `docs/` all resolve to a defining row in the evidence register, control matrix, and risk register respectively — cited count equals defined count in all three, so there are no orphans in either direction; every repository path cited in a Phase 3.5 evidence row exists on disk; every quoted evidence status matches the register and every quoted control status matches the matrix; matrix, SoA, and trust-services tallies agree. **Corrective-action decision recorded:** the sweep **should** graduate into the architecture-test suite as a merge-blocking check — owner Engineering Owner, target Phase 4 — because a hand-run sweep has now been the fix for the same class of defect twice, and the second time is the point at which it should stop being manual.
- **Item 19 (CI-005):** every vendor row's next-review date is in the future, and the Phase 3.5 re-read is recorded as a **register re-read, not a vendor security review**. KAR-CTL-047 stays DESIGNED on exactly that distinction, and CI-005 stays OPEN.

### Unresolved states carried into Phase 4 — stated plainly

These are correct behaviours with real consequences. None is papered over, and none is a defect this gate is waiving.

- **Consent acceptance is unreachable.** `POST /consent/acceptances` answers **503 for every caller**, including in `local`. A valid grant must pin jurisdiction, active pack version, operating entity, and legal-document version, and **no runtime write path exists** for the jurisdiction assignment or the pack activation — both are operator use cases with no HTTP surface and deliberately unseeded permissions. Refusing is correct: a grant whose provenance nobody resolved must not be written, and the schema refuses it independently. But an endpoint published in Phase 3 stopped working, which is a **regression in reachability**, and it is carried to Phase 4 as entry work.
- **Bootstrap capability-resolution failure is currently indistinguishable from a legitimate empty capability set** — both yield an empty list. This **must be corrected before the Flutter client relies on it**; a client cannot tell "you have nothing" from "we could not find out". Phase 4 owns it.
- **The bootstrap response has no safe `OperatingEntity` summary.** It **reports absence**. Phase 4 adds a minimal safe projection. **No such projection exists today and this record does not describe one as existing.**
- **The capability-clearance bridge in the composition fails closed when a pack clears a capability**, because the full clearance facts cannot yet be derived. It is unreachable today — `qa/v1` clears nothing and every capability is unimplemented — and it **must be completed before any real capability is marked IMPLEMENTED or DEPLOYED**.
- **Cloudflare: only registration, registrar, and DNS provider are confirmed facts. Nothing is configured.**

### Deferred items — each with reason, owner, target, residual risk, and closure condition

1. *Policy approval (14 policies DRAFT).* **Reason:** approval is the Platform Owner's own act and has not occurred; no workstream may perform it. **Owner:** Platform Owner. **Target:** before the first non-local deployment (Phase 18 at the latest; earlier if an external audit engagement begins). **Residual:** policies guide but do not formally bind; a control set resting on unapproved policy is weaker than it reads. **Closure:** the Platform Owner's own recorded approval, never on that role-holder's behalf.
2. *Protected long-term evidence storage.* **Reason:** no protected store exists; the interim model keeps only safe references and summaries in a public repository. **Owner:** Compliance Owner. **Target:** **now falling due** — the Phase 2 gate set "before Phase 4 or before any external audit", and Phase 4 is next. **Residual:** evidence lives in maintainer-controlled locations with no organizational access control, and run-URL retention is finite (KAR-RSK-011). **Closure:** a store decision recorded with its access design.
3. *Separation of duties (EXC-001).* **Reason:** one maintainer; agent workstreams are technical, not organizational. **Owner:** Platform Owner. **Target:** exit trigger = engineering team ≥ 2. **Residual:** author and approver are the same identity on every change. **Closure:** the exception exits on team growth; until then merge-blocking CI plus reviewer findings attached to every gate.
4. *EV-427 — domain and DNS hardening verification.* **Reason:** no repository-verifiable evidence of any registrar or DNS setting exists; all seven rows are TO_VERIFY. **Owner:** Security Owner. **Target:** Phase 4 gate, or immediately on any DNS record creation or proxy enablement. **Residual:** a domain takeover, lapse, or transfer would not be detected by anything in this repository. **Closure:** all seven §3 rows carrying an observed value and observation date, checked against the registrar account itself.
5. *Consent acceptance reachability.* **Reason:** the pin requires records with no runtime write path. **Owner:** Engineering Owner. **Target:** Phase 4 entry work. **Residual:** a published endpoint answers 503 to every caller, including local. **Closure:** the operator surface or a seeded local path exists and an acceptance succeeds with a genuine pin.
6. *Bootstrap resolution-failure ambiguity.* **Reason:** an empty capability list currently means either "nothing available" or "resolution failed". **Owner:** Engineering Owner. **Target:** Phase 4, **before the Flutter client consumes the surface**. **Residual:** a client could render a resolution outage as a legitimate empty state. **Closure:** the two cases are distinguishable in the response contract and proven by test.
7. *Safe `OperatingEntity` projection in bootstrap.* **Reason:** no safe read surface exists; the response reports absence. **Owner:** Engineering Owner. **Target:** Phase 4. **Residual:** clients cannot show which legal person serves them. **Closure:** a minimal safe id/name projection ships with its own leak-regression coverage.
8. *Capability-clearance bridge completion.* **Reason:** full clearance facts cannot yet be derived, so the composition fails closed. **Owner:** Engineering Owner. **Target:** **before any capability is marked IMPLEMENTED or DEPLOYED** — a hard precondition, not a phase number. **Residual:** none today (unreachable); it becomes a live availability defect the moment a capability is built. **Closure:** the clearance-facts mapping is complete and a cleared capability resolves correctly under test.
9. *Unseeded Phase 3.5 permissions and their operator surfaces.* **Reason:** deliberate — absence is the control while no audited surface exists. **Owner:** Platform Owner. **Target:** Phase 8 control plane, with the reviewed seeding migration. **Residual:** no jurisdiction assignment, pack activation, settings write, availability, or entitlement change can be made by anyone; the platform is unconfigurable at runtime by design. **Closure:** the seeding migration lands together with the audited operator surfaces.
10. *Production key custody and mail (KAR-RSK-009, 020).* **Reason:** no non-local environment exists; local providers fail closed by construction. **Owner:** Platform Owner. **Target:** provider selection Phase 17+, custody gates Phase 20. **Residual:** identity flows needing mail or KMS cannot run outside local. **Closure:** real provider selection with custody evidence at those gates.
11. *Caller-settable pin cutoff (KAR-RSK-029).* **Reason:** migration `0086`'s cutoff arm keys on a `created_at` the writing transaction supplies. **Owner:** Privacy Owner. **Target:** a later phase — it needs a database-supplied creation witness on the `recorded_at` pattern, which is a schema change, not a Phase 3.5 patch. **Residual:** a caller passing a pre-cutoff timestamp could record a legal-consequence row claiming to predate the machinery. **Closure:** creation time for legal-consequence rows is witnessed by the database rather than supplied by the writer.

Also carried, unchanged and non-engineering: **legal review of every `PENDING_LEGAL_REVIEW` slot in `qa/v1` and jurisdiction approval itself** (Platform Owner with counsel; target before any non-local jurisdiction goes live; residual: every legal question fails closed and no jurisdiction can be served; closure: a new approved pack version with its approval reference), **Sharia review for Zakat** (KAR-RSK-007; Platform Owner with an external reviewer; before Zakat launch at the Phase 9 gate; residual: Zakat cannot launch; closure: completed review with its scope recorded) and **per-jurisdiction legal clearance for Amanat** (KAR-RSK-008; Platform Owner with counsel; Phase 14 gate; residual: Amanat stays HIDDEN and unreachable; closure: clearance recorded per launch jurisdiction).

**No hard roadmap gate was waived at this gate.** Staging-before-production, custody-before-sealed-data, and independent-assessment-before-launch remain preconditions of later phases, untouched and unclaimed. **No SOC 2 report, ISO certification, regulatory approval, external assessment, or production-readiness claim is made or implied anywhere in this record.**

## Phase 4 gate record — executed 2026-08-18

**Outcome: PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.**

**Phase 4 implementation and its internal gates are complete. The PR is NOT YET OPEN at the moment this record is written, and nothing is merged. Phase 5 is NOT STARTED** — no Phase 5 work exists, no Phase 5 evidence is claimed, and no control anywhere is advanced on the expectation of it.

Gate executed by the phase lead in the Compliance Owner role at close, against the Phase 4 branch `claude/karar-v2-phase-4-flutter-foundation`, base `e23bbc8` (the Phase 3.5 merge on `main`). **All review recorded in this document is maintainer-directed technical review. It is not organizational separation of duties, and it is not independent human review**; the reviewing role and the reviewed party resolve to one person. Agent workstreams, reviewers included, do not change that (EXC-001).

**Why PASS_WITH_DOCUMENTED_DEFERRED_ITEMS and not PASS.** Every checklist item below was verified and every mechanism behaved as specified, so this is not a FAIL. A plain PASS would assert nothing is outstanding, and the deferred items at the end of this record are numerous and substantial: no build has run on a device, no build has been signed, no Apple Team ID exists, the compound credential-abandonment guarantee is bounded by the absence of server-side revocation, three of five mobile CI lanes cannot block a merge, and one evidence row (EV-427) reaches the deadline this gate set for it with nothing to cite. **No hard roadmap gate was waived** — staging-before-production, custody-before-sealed-data and independent-assessment-before-launch all sit in later phases and none was touched, because nothing was deployed.

**Two nonconformities in this record concern this gate's own reporting**, and they are recorded rather than absorbed: CI-011 (an NC closed at Phase 3.5 on its correction while its corrective action stayed outstanding) and CI-013 (a canonical test count asserted without being re-derived, wrong in two directions at once). A gate that cannot find fault with itself is not a gate.

### Verified figures this gate rests on

From a clean clone of `8d89328` executed 2026-08-18 in the window 03:04:05–03:10:14 UTC, reusing no `node_modules`, no `.dart_tool`, no generated client, no build output and no database or container state; and re-derived on the close-out tree after the documentation and compliance commits. Tooling: node v25.9.0, pnpm 11.18.0, Flutter 3.47.0, OpenJDK 25.0.2.

| Check | Executed result |
|---|---|
| Workspace suite | **1272 passed / 12 skipped (1284)** across **119 passed / 1 skipped (120)** files |
| Readiness and rate-limit-store suite (the one skipped file, `KARAR_INTEGRATION`-gated) | run separately — **12 passed** |
| Runtime OpenAPI conformance | **61 tests**; **82 of 128** declared operation/status pairs validated against real serialized responses; **0** media-type deviations; **0** prose-only operations |
| Flutter suite as CI runs it (goldens excluded) | **1190 passed / 19 skipped (1209)** — CI builds no artifact, so the artifact-gated assertions skip; a tree holding artifacts reports **1208 / 1** |
| Flutter goldens (local only) | **4 passed** |
| Localization gate | **36 passed** |
| Mobile security suite under its artifact gates | **113 passed / 1 skipped** |
| Generated Dart client | in sync — **35 operations / 98 schemas** |
| Architecture runner | **24 passed / 0 failed / 4 skipped** (deferred by activation phase); registry errors **0**; self-test **56/56** |
| Documentation checks | **13/13**; self-test **14/14** |
| Migrations | **38 files, all 38 applied in full sequence `0001`–`0086`** against an empty database; `db:verify` clean |
| Dependency audit | `pnpm audit --audit-level high` — **no known vulnerabilities** |
| Android | LOCAL debug and unsigned release assembled; `lintVitalRelease` passes; packaged id `com.kararfinance.app.local`; **release unsigned, not debug-signed** |
| iOS | four environments built without signing — `com.kararfinance.app.local` / `.dev` / `.staging` / `com.kararfinance.app`; **no provisioning profile in the packaged bundle** |
| Repository shape | 24 module directories (12 with code), 8 packages, **34 merged OpenAPI paths** |
| CI / Security | **PENDING — no workflow run exists against a Phase 4 head yet; owner Engineering Owner; executes at PR open** (EV-463, EV-464) |

**Two precisions, so the clean-clone row is not read as more than it is.** `db:create` reported "database karar already existed" — that is the Postgres image's own `initdb` on a brand-new volume, not a pre-existing database, and the from-zero claim rests on the 38 migrations running the full sequence. And toolchain caches (`~/.gradle`, `~/.pub-cache`, CocoaPods, the pnpm store) live outside the clone and were warm, so this is **maintainer reproducibility from a clean repository and clean infrastructure — not a hermetic build, and not independent organizational assurance.**

### Standard sections

**1. Control status deltas.** KAR-CTL-094–116 added (23) in matrix v0.6 — 20 `IMPLEMENTED [P4]`, 2 DESIGNED, 1 DEFERRED. Tally re-derived by counting rows rather than carried forward: **116 controls — 18 DESIGNED / 82 IMPLEMENTED / 15 DEFERRED / 1 EXCEPTION**. **No existing control moved**, and **nothing anywhere claims OPERATING or EVIDENCED**, because a control that passed once has not been shown to operate and an evidence row is not evidence. Two Phase 4 controls are deliberately not IMPLEMENTED, on the precedent KAR-CTL-025 and 047 set that a status is honest to its own statement rather than to the effort behind it: **KAR-CTL-113 (mobile CI) DESIGNED**, because only the `mobile` lane is a required check and the Android, iOS and supply-chain lanes cannot block a merge; **KAR-CTL-116 (register traceability) DESIGNED**, because the sweep the Phase 3.5 gate scheduled into the architecture suite for Phase 4 was not added and the runner still stands at 24 tests.

**2. Evidence generated and missing.** EV-431–EV-466 added (36); **32 COLLECTED, 4 PENDING**, each PENDING row stating what did not happen and why rather than carrying a bare status. PENDING: **EV-461** (independent closeout review — executes at close, owner phase lead), **EV-463** (PR CI run — no PR exists yet, owner Engineering Owner, executes at PR open), **EV-464** (PR Security run — same), **EV-465** (suppression review — Phase 4 adds a Dart analyzer and two mobile lanes, so Phase 3.5's "nothing new" finding may not be assumed). **EV-427 reaches at this gate the deadline the Phase 3.5 record set for it** — domain and DNS hardening — and all seven runbook rows remain `TO_VERIFY` with no repository-verifiable evidence of any registrar or DNS setting. It stays PENDING and is re-stated rather than allowed to pass in silence. Nothing is `REVIEWED`: formal evidence-file review needs a protected store that does not exist.

**3. Risk register update.** Register updated to 41 rows with KAR-RSK-031–041 added and treatments for all eleven in the treatment plan. Scores re-verified row by row against the methodology. The Phase 4 additions record what this phase built and what it did not close: unverified biometric behaviour on real hardware, no signed build on either platform, no Apple Team ID so cross-platform transfer is unverified on a device, the bounded local guarantee on compound credential abandonment, golden baselines unenforced, artifact-lane checks that do not gate a merge, and the absence of a protected long-term evidence store.

**4. Exceptions.** EXC-001, 002, 003 re-affirmed OPEN. **No exception was opened, closed, or re-approved, and no exit trigger fired** — the team is still one person. EXC-001 continues to state explicitly that this phase's parallel agent workstreams, reviewers included, are **not** separation of duties.

**5. Mapping deltas.** SoA v0.5 swept against matrix v0.6 with one move — **5.21 APPLICABLE → IMPLEMENTED** on dependency pinning, host exclusivity and the exact-pinned override — and 23 rows annotated without a move. Tally re-derived: **39/28/10/16 = 93**. **8.24 deliberately not advanced for the fourth consecutive phase, on a changed reason**: key material is in custody for the first time, and that custody is the platform's — Karar generates, wraps, rotates and destroys nothing. Trust-services mapping v0.6 re-swept with **zero forks**, naming the three of twenty-three Phase 4 controls that are not IMPLEMENTED rather than letting a reader assume a clean sweep. **No ISO certification is held, claimed, applied for or sought; no SOC 2 attestation is claimed and no examination has been performed.**

**6. Claims reconciliation.** Assurance claims AC-022–AC-031 added; architecture test 26 green, now parsing **31 AC rows**. One correction of substance: **AC-012's stated basis was false** — its Phase 3.5 evidence was "zero `Qarar` occurrences in any source file", and exactly one occurs, in a test that forbids the legacy brand name. The claim holds; its basis did not, and the basis is corrected rather than the claim quietly re-asserted. No claim was marked VERIFIED at this gate: no human assurance review has been recorded against any of them.

**7. Residual risk statement.** Entering the Phase 4 PR, exposure is still dominated by what does not exist. Nothing is deployed, no capability resolves available anywhere, no jurisdiction or pack is approved, no personal data is processed, and no artifact has been signed or installed on a device. Phase 4 changed the shape of the residual rather than its size: there is now a client, and its local controls are the first controls in this platform that would run on hardware nobody here has tested. The honest statement is that **every Phase 4 local control is verified against source, tests and built artifacts, and none is verified against a running device.**

**8. Next-phase entry criteria.** Phase 5 begins only after this PR merges and a new branch starts from the merge commit. Phase 5 does not unlock policy approval, capability implementation, signing, deployment or any operator surface. No Phase 5 control is pre-activated by this record.

**9. Access review.** Performed at this gate. GitHub repository access: one account, `MoayadAlobaidi`, holding admin — unchanged since the Phase 3.5 review, and the substance of EXC-001. Branch protection on `main` verified through the API at this gate: **8 required checks** (`workspace`, `architecture`, `mobile`, `codeql`, `secrets`, `dependency-review`, `iac-and-containers`, `sbom`), strict up-to-date enforcement on, admin enforcement on, force-push and deletion both disabled, required approving reviews 0 — which is a direct consequence of the single-maintainer exception rather than an oversight. Actions permissions: all fourteen action pins across both workflows were verified against the tags they name, resolving annotated tags; **one disagreed** and is recorded as CI-012. Cloudflare registrar and DNS access: **not reviewed, because no repository-verifiable evidence of any registrar or DNS setting exists** — this is EV-427, and asserting a review of an account this repository cannot observe would be the substitution EV-427 exists to prevent. Beyond GitHub and the two package registries, the only account recorded anywhere is the Cloudflare Registrar account in the domain runbook, whose status is `USER_CONFIRMED` and which is exactly the account this review could not observe — it is named here rather than omitted, because a sentence saying no other account exists would erase the one the previous sentence just failed to review. No role changed since the Phase 3.5 gate.

**10. Continual improvement.** CI-007–CI-013 logged; every Phase 4 review finding is either an NC with a corrective action or explicitly classified as not an NC **with a reason**. Nonconformities: **CI-007** (AI-attribution trailers on commit messages, against the style guide's own rule — the fourth entry sharing the root cause that `docs:check` never reads a commit message, escalated to the Platform Owner), **CI-008** (the phase report asserted corrections it had only partly made), **CI-011** (CI-006 was closed at Phase 3.5 on its correction while its corrective action stayed outstanding, and the log's closure rule permitted it), **CI-013** (a canonical count asserted without re-derivation, recorded as a recurrence of CI-008 rather than a fresh finding). Not nonconformities, with reasons recorded: **CI-009** (the security-state adapter's missing coverage — nothing merged diverged), **CI-010** (the five LOW findings — the phase has not closed), **CI-012** (the action pin — introduced and fixed within the phase). **CI-005 remains OPEN and widened**: no vendor security review has been performed, and Phase 4 added seven suppliers that entered the build before any review. **CI-011 carries a second limb for the Compliance Owner**: the closure rule needs amending so an NC cannot reach CLOSED with its corrective action outstanding. That is a gate decision, so CI-006 is left as recorded rather than unilaterally rewritten.

### Unresolved states carried into the Phase 4 PR — stated plainly

- **No build has run on a device.** Every Phase 4 mobile control is verified against source, tests and built artifacts. The biometric prompt has never been shown to appear.
- **No build has been signed**, and no signing material exists in the repository. The Android release configuration has no debug-key fallback, so an unsigned release fails rather than shipping under a debug key.
- **No Apple Team ID exists.** Android and iOS identifiers now agree per environment and are compared artifact to artifact, but naming the *owner* of those identifiers still requires a Team ID that does not exist and was not invented. Every deployed assembly refuses without one.
- **The compound credential-abandonment guarantee is bounded.** When neither the credential deletion nor the durable invalidation can be confirmed, the application blocks rather than presenting a clean state, and an in-process latch keeps the credential unusable for the process. Nothing local can promise the credential is dead; server-side revocation is the closure and is not in this phase.
- **Three of five mobile CI lanes do not gate a merge**, so several assertions this phase built stop nothing today. KAR-CTL-113 is DESIGNED for exactly that reason.
- **Runtime conformance covers 82 of 128 declared operation/status pairs**, and the suite skips entirely without infrastructure.
- **EV-427 can still cite nothing**, at the gate its own deadline named.

### Deferred items — each with reason, owner, target, residual risk, and closure condition

Twelve items. Every one carries all five fields; an item missing any of the five is an omission, not a deferral, and the gate does not pass with it.

| # | Item | Reason | Owner | Target | Residual risk | Closure condition |
|---|---|---|---|---|---|---|
| 1 | Biometric runtime verification, Android API 24–27 | No device or emulator of that range was available, and the AppCompat requirement bites only below API 28 | Security Owner | First device-testing pass, before public beta | A prompt that never appears, or a theme crash, on the exact range the control was written for | The prompt observed on a real or emulated API 27 device across success, cancellation, no-enrolment, device-credential fallback, lockout and background/resume |
| 2 | Biometric runtime verification, modern Android and iOS Face ID | Same absence of hardware; simulator Face ID was not exercised | Security Owner | First device-testing pass, before public beta | Failure mapping proven only against scripted outcomes, not against the platform | Same six outcomes observed on a modern Android device and a Face ID-capable iOS device, with an expired or revoked server session proven to remain authoritative |
| 3 | Signed Android device build | No signing material exists and none was created | Engineering Owner | Pre-release phase | Store validation and install-time behaviour untested end to end | A signed build installed on a device, with signing ownership, protected key storage, recovery, rotation and separation of duties recorded |
| 4 | Signed iOS device build | No Apple developer account, certificate or provisioning profile exists | Engineering Owner | Pre-release phase | As above, plus entitlement behaviour unverified | A signed build installed on a device, with the same custody record |
| 5 | Real Apple Team ID and cross-platform transfer device verification | No Team ID exists; inventing one would put an identity nothing can match into a shipping artifact | Security Owner | Before any deployed assembly is produced | The cross-platform transfer section names a counterpart whose owner is unproven | A real evidence-backed Team ID supplied out of band, plus a supported-device export/import test proving no Karar credential or state crosses platforms |
| 6 | Server-side revocation on compound credential abandonment | The closure is a backend mechanism, and Phase 4 is a client phase | Backend workstream | Phase 5, alongside the first substantial session surface | A credential that could not be erased or durably invalidated is unusable for the process but not provably dead | A server-side revocation path invoked on abandonment, with a restart test proving the old credential is refused by the server |
| 7 | CI enforcement of golden baselines | The four baselines are rasterised on macOS arm64 under a zero-tolerance comparator and have never run on Linux; two in-repo comments disagree about whether a Linux runner would fail | Engineering Owner | The first CI job that runs Flutter tests on Linux | Visual regressions land unnoticed; the baselines are a local guard only | Goldens running in CI on a supported platform, or a recorded decision that they remain local with the reason |
| 8 | Mobile artifact and supply-chain lanes as required checks | The lanes were introduced in this phase and their names are only verified stable once they have run on a PR head | Engineering Owner | At Phase 4 PR close, this gate's own action | The artifact assertions this phase built cannot stop a merge; KAR-CTL-113 is DESIGNED because of it | `mobile-android` and `mobile-ios` added to branch protection, verified through the API, with `repository-security-settings.md` updated in the same change |
| 9 | Direct Android/iOS artifact identity comparison beyond one environment pair | The two lanes run on different runners and do not build the same environment set | Engineering Owner | Next mobile CI change | Three of four environment pairs are verified against the shared rule rather than artifact against artifact | All four environments built on one runner, or the Android manifests for all four published into the iOS lane |
| 10 | Protected long-term evidence store | No store exists; nothing is `REVIEWED` anywhere in the register for this reason | Compliance Owner | Before any external assessment | Evidence integrity rests on Git history alone; no formal evidence-file review can be recorded | A protected store with retention and access control, and the first evidence rows moved to `REVIEWED` |
| 11 | Policy approval | All fourteen policies are DRAFT at 0.1; no workstream may approve on the Platform Owner's behalf, and Phase 3.5 re-affirmed them without a content review | Platform Owner | Phase 5 gate | The control corpus rests on documents nobody has approved or content-reviewed | Platform Owner approval recorded per policy, following an actual content review rather than a re-affirmation |
| 12 | Single-maintainer separation of duties (EXC-001) | Every role resolves to one person; agent workstreams are not organizational separation | Maintainer | Structural — when a second person joins | Author, reviewer and approver are the same party in every record this gate produced | A second person holding a distinct role, and the exception's stated exit trigger firing |

### Post-record reconciliation — PR, workflow evidence, and the required-check change

Written after the record above was frozen, because these four facts did not exist when it was written. **This section is the corrective action from CI-015 being applied to its own gate**: every claim here was re-derived from the GitHub API in the session that wrote it.

**Pull request.** [PR #7](https://github.com/MoayadAlobaidi/Karar-Flutter-version/pull/7), `claude/karar-v2-phase-4-flutter-foundation` into `main`, head `ebdcf99940415c2b2f264c707a7f10c92833b4dc`. **OPEN and NOT MERGED.** No merge reference exists and none is recorded.

**Workflow evidence at that head.** CI [run 32098073101](https://github.com/MoayadAlobaidi/Karar-Flutter-version/actions/runs/32098073101) — success, five jobs: `workspace`, `architecture`, `mobile`, `mobile-android`, `mobile-ios`. Security [run 32098073108](https://github.com/MoayadAlobaidi/Karar-Flutter-version/actions/runs/32098073108) — success, eight jobs: `codeql`, `secrets`, `dependency-review`, `dependency-audit`, `licenses`, `sbom`, `iac-and-containers`, `mobile-supply-chain`. **13 checks green.** EV-463 and EV-464 move PENDING → COLLECTED; EV-461 and EV-465 likewise, the latter on a review of the one code-scanning dismissal rather than on its comment.

**This is the first execution of the cross-platform identity comparison.** It is worth recording that the lane failed nothing only because it was fixed first: the independent closeout review reproduced a defect in which `mobile-ios` set the Android artifact gate variable while receiving only the merged manifest, so six assertions had nothing to read (CI-014). The step now passes with 38 tests, including the one that compares the iOS artifact's identifier against the counterpart the Android build names.

**Required-check change.** `mobile-android` and `mobile-ios` were added to branch protection on `main` after both had run to success on the PR head and their job names were observed stable. Prior required set (8): `workspace`, `architecture`, `mobile`, `codeql`, `secrets`, `dependency-review`, `iac-and-containers`, `sbom`. Final required set (10): the same eight plus `mobile-android` and `mobile-ios`. Strict up-to-date enforcement, admin enforcement, blocked force-pushes, blocked deletions, conversation-resolution and review settings were all re-read through the API afterwards and are **unchanged**. Actor: `MoayadAlobaidi` (the only account with admin). Date: 2026-08-18. Evidence: EV-466 family; settings recorded in [`../operations/repository-security-settings.md`](../operations/repository-security-settings.md).

**KAR-CTL-113 moves DESIGNED → IMPLEMENTED** on its own written statement, which names those artifact builds as required status checks. Matrix tally re-derived by counting rows: **17 DESIGNED / 83 IMPLEMENTED / 15 DEFERRED / 1 EXCEPTION = 116**. Deferred item 8 is discharged.

**Two honest limits on that discharge.** The green run predates the change, so it is evidence the lanes **pass**, not evidence they **gate** — the next PR head is the first at which they gate anything. And **`mobile-supply-chain` is still not a required check**, which matters because it carries the blocking mobile secret scan; that is recorded as a gap in the settings document rather than presented as a decision, and it is left for the next branch-protection review because a check should be observed stable on more than one head before it blocks a merge.

**KAR-CTL-116 does not move.** Nothing about the register-integrity sweep changed, and CI-011 stays OPEN.

**The gate outcome is unchanged: PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.** Eleven of the twelve deferred items stand exactly as recorded.
