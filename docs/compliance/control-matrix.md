# Control Matrix

**Status:** ACTIVE register · **Owner:** Compliance Owner · **Version:** 0.6 · **Date:** 2026-08-18 · **Review:** every phase gate

**v0.7 (2026-08-18, Phase 4 PR close):** **KAR-CTL-113 DESIGNED → IMPLEMENTED**, on its own written statement rather than on effort: the row requires the mobile analysis, drift, localization and suite lane plus real Android and iOS artifact builds to be **required status checks**, and `mobile-android` and `mobile-ios` were added to branch protection once both had run to success on the PR head. The required set moves 8 → 10 with every prior check preserved; strict up-to-date, admin enforcement, force-push and deletion settings were re-read through the API afterwards and are unchanged. Tally re-derived by counting rows: **17 DESIGNED / 83 IMPLEMENTED / 15 DEFERRED / 1 EXCEPTION = 116**. `mobile-supply-chain` remains not-required and is recorded as a gap in `../operations/repository-security-settings.md`; it is outside this row's statement, so it does not hold the status back. **KAR-CTL-116 stays DESIGNED** — nothing about the register sweep changed.

**v0.6 (2026-08-18, Phase 4):** client, mobile-artifact, and build controls **KAR-CTL-094–116 added** (23 rows: 20 IMPLEMENTED [P4], 2 DESIGNED, 1 DEFERRED). **No existing control status moved**, and none moved to `OPERATING` or `EVIDENCED` — a phase cannot produce an operating history, and nothing is deployed. Notes were extended, dated, on the rows Phase 4 changed the facts of: KAR-CTL-016 (three new mobile lanes that do **not** gate a merge), 020, 023, 025, 026, 027, 028, 036, 038, 049, 062, 070, 079, 089, 091, 092. **Phase 4 is IN PROGRESS, not complete** — this version registers the control set the phase built; the gate record, the CI run, the clean-clone verification, and the independent review do not exist yet, and five evidence rows are `PENDING` because of it. Re-derived tally: **18 DESIGNED / 82 IMPLEMENTED / 15 DEFERRED / 1 EXCEPTION = 116**.

**Two Phase 4 rows are deliberately not IMPLEMENTED, on the precedent KAR-CTL-025 and KAR-CTL-047 set** — a status is honest to its own control statement, not to the effort behind it. **KAR-CTL-113** stayed DESIGNED at v0.6 because its statement requires the mobile lanes to be merge-blocking and only `mobile` was a required check *(superseded at v0.7: all three lanes are now required and the row is IMPLEMENTED)*. **KAR-CTL-116** stays DESIGNED because the register-integrity sweep it describes is still run by hand: the Phase 3.5 gate recorded the corrective action to graduate it into the architecture suite as a merge-blocking check with a **Phase 4 target** (CI-006), and that has not happened — architecture test 26 still covers assurance-claim traceability only, and the runner stands at the same 24 active tests as Phase 3.5.

**v0.5 (2026-08-16, Phase 3.5 close):** the gate executed, and **Phase 3.5 is COMPLETE; Phase 4 is NOT STARTED** — no control below is advanced on any Phase 4 expectation. One status moved at the gate: **KAR-CTL-014 DESIGNED → IMPLEMENTED**, on the trigger the row itself wrote — the Phase 3.5 gate record is the first to contain an actual access review (§9), which also closes CI-004. The tally is re-derived accordingly: **16 DESIGNED / 62 IMPLEMENTED / 14 DEFERRED / 1 EXCEPTION**. The [P3.5] contingency note is resolved against the executed clean-clone figures at head `5411b0d` (the pre-integration counts it carried are superseded). No other status changed, nothing is OPERATING or EVIDENCED, and nothing is deployed.

**v0.4 (2026-08-16, Phase 3.5):** jurisdiction/capability/binding controls KAR-CTL-079–093 added (15, all IMPLEMENTED [P3.5]); KAR-CTL-011, 020, 049, 076 had their notes extended with the Phase 3.5 facts (RLS inventory 37 → 48 tables; architecture tests 12, 17, 19, 21, 22, 26 activated or strengthened; the PolicyPack mechanism landing without deciding anything; legal-consequence pack pinning delivered by migration 0086).

**v0.4 corrections from the Phase 3.5 independent review**, made in the same version: **KAR-CTL-004** moved DESIGNED → IMPLEMENTED (three gate records exist; the row still said the first was pending). **KAR-CTL-014**'s note corrected — its present-tense claim that access rights "are reviewed at every phase gate" was not true of any gate record, and the gate document now carries a standing access-review section so it becomes true from Phase 3.5. **KAR-CTL-047**'s note corrected — the vendor periodic-review leg lapsed across two gates without being raised. **Twenty-two stale evidence-status parentheticals refreshed** against the register (mostly Phase-2 rows left reading `(PENDING)` after the Phase 2 close moved them to `COLLECTED`; plus `EV-007 (VERIFIED …)` → `(REVIEWED …)` for the register's vocabulary and `EV-008 (PENDING)` → `(COLLECTED)`). Both corrected control rows stay honest about what has *not* happened.

**On the evidence parentheticals:** the `(STATUS)` shown beside an EV id in the Evidence column is a **convenience snapshot as of this matrix version**, refreshed at each gate. The [evidence register](evidence-register.md) is authoritative; where the two disagree the register wins and the matrix is the file that gets fixed. Statuses in the *Status* column are the opposite — those live here and nowhere else.

**v0.3 (2026-08-16, Phase 3):** identity/tenancy/access controls KAR-CTL-066–078 added; KAR-CTL-010, 011, 049 moved DEFERRED → IMPLEMENTED [P3] with pointers; KAR-CTL-020's note extended with the Phase 3 architecture-test activations (tests 9, 21, 22; test 4 narrowed).

**v0.2 (2026-08-15, Phase 2):** platform-foundation controls KAR-CTL-051–065 added; KAR-CTL-033 and 040 moved to IMPLEMENTED [P2] with pointers; Phase-1 evidence pointers refreshed to the evidence register's COLLECTED/VERIFIED state.

The single source of truth for Karar's control set and each control's status. Other documents (SOC 2 mapping, Statement of Applicability, policies) reference these IDs and **must not fork the statuses recorded here**.

---

## Status model

| Status | Meaning |
|---|---|
| `DESIGNED` | The control is defined in a canonical document; its enforcing mechanism does not yet run |
| `IMPLEMENTED` | The mechanism exists (tooling, CI check, in-repo artefact) but has not yet produced reviewed evidence over time |
| `OPERATING` | The mechanism runs repeatedly as intended |
| `EVIDENCED` | Operation is demonstrated by collected, reviewed evidence in the evidence register |
| `NOT_APPLICABLE` | Does not apply, with a stated reason |
| `DEFERRED` | Applies at a named later phase; not pretended before then |
| `EXCEPTION` | Deviates from its own statement; recorded in the exceptions register with compensating controls |

**No control below is claimed OPERATING or EVIDENCED.** Phase 1 produced designs and tooling; Phase 2 produced platform code with tests executed locally against live PostgreSQL; Phase 3 produced identity, tenancy, authorization, consent, and control-plane code under the same discipline; Phase 3.5 produced jurisdiction, PolicyPack, capability, subject-policy, and session-binding code under the same discipline again; Phase 4 produced a Flutter client, a generated API client, and two platform build pipelines under the same discipline once more. None of that is an operating history, and nothing is deployed anywhere. **Phase 4 adds a class of proof the earlier phases did not have and a limit that comes with it:** several of its controls are asserted against a **real build artifact** — a merged Android manifest, an unzipped APK, a packaged iOS bundle — which is stronger than reading the source and still weaker than a device. **No control in this matrix has been exercised on a physical device, and no signed build exists on either platform.**

**[C1] contingency note:** controls marked `IMPLEMENTED [C1]` rest on the Phase-1 CI workflows authored in parallel with this register (`.github/workflows/ci.yml`, `security.yml`, `.github/dependabot.yml` — present on this branch). If the named check is absent when Phase 1 merges, the status reverts to `DESIGNED`. Resolved 2026-08-15: the checks exist in the merged CI and the first run URLs are COLLECTED in the [evidence register](evidence-register.md) (EV-001–EV-006); the marker is retained for traceability.

**[P2] contingency note:** controls marked `IMPLEMENTED [P2]` rest on the Phase-2 platform code and its test suite — run locally by the lead against live PostgreSQL on 2026-08-15 and expected to run on the Phase 2 PR CI. IMPLEMENTED here means the mechanism exists in-repo with passing tests; it does not mean the mechanism operates in any environment, because none exists. Evidence for all [P2] controls is `PENDING` until the Phase 2 PR CI run URL is recorded against EV-201–EV-219. Resolved 2026-08-16: the Phase 2 CI run URLs are recorded and EV-201–EV-219 are COLLECTED in the [evidence register](evidence-register.md) (v0.3, Phase 2 close); the register is authoritative for evidence status, and the marker is retained for traceability.

**[P3] contingency note:** controls marked `IMPLEMENTED [P3]` rest on the Phase-3 identity, tenancy, authorization, consent, operating-entity, and control-plane code and its test suites — run locally by the lead against live PostgreSQL on 2026-08-16 (full workspace suite 797 passed / 5 skipped, 802 total, across 80 passed / 1 skipped test files; the skipped file is the KARAR_INTEGRATION-gated readiness suite, which runs in CI) and expected to run on the Phase 3 PR CI. IMPLEMENTED means the mechanism exists in-repo with passing tests; nothing operates in any environment, because none exists. The EV-301–EV-317 family indexes the proofs; the Phase 3 PR CI run URL is recorded against EV-317 at the Phase 3 gate. Resolved 2026-08-16 at the Phase 3 gate: EV-301–EV-319 are COLLECTED against CI run 31921545097 and Security run 31921545041 on implementation head `8627f16`; the closing counts were 807 passed / 5 skipped (812) across 83 passed / 1 skipped files. The marker is retained for traceability.

**[P3.5] contingency note:** controls marked `IMPLEMENTED [P3.5]` rest on the Phase-3.5 jurisdiction, PolicyPack, capability, subject-policy, and session-binding code and its test suites — executed by the lead against live PostgreSQL on 2026-08-16. **Resolved at the Phase 3.5 gate against the clean-clone run at final head `5411b0d`** (the pre-integration counts this note previously carried are superseded): full workspace suite **1109 passed / 5 skipped (1114 total) across 105 passed / 1 skipped (106) test files**, with the one skipped file — the `KARAR_INTEGRATION`-gated readiness suite — **run separately and passing 5**; architecture runner **24 passed / 0 failed / 4 skipped**, 0 registry activation errors, built-in self-test 56/56 seeded-violation cases; lint, typecheck, build, and format clean; documentation checks 7/7; Prisma mapping and drift check **43 mapped tables matching the live database**; **38 migration files, all 38 applied from zero** with checksum verify clean. IMPLEMENTED means the mechanism exists in-repo with passing tests; **nothing operates in any environment, because none exists**, and no capability in the registry is built or deployed. The EV-401–EV-430 family indexes the proofs; the CI and Security run URLs are recorded against EV-426 (12 checks green on `5411b0d`, 8 branch-protection-required). **One row could not cite a green run and reverted as the rule requires: EV-427 stays `PENDING`** with its reason, owner, target, residual, and closure condition — no repository-verifiable evidence of any registrar or DNS setting exists. The marker is retained for traceability.

**[P4] contingency note:** controls marked `IMPLEMENTED [P4]` rest on the Phase-4 Flutter client, generated API client, backend contract hardening, and Android/iOS build code and their suites — executed by the lead at head `8d89328` on 2026-08-18 against live PostgreSQL and Redis, and re-executed end to end in a clean-clone run at the same head in which **all 33 steps exited 0** (EV-462): workspace suite **1272 passed / 12 skipped** across **119 passed / 1 skipped** test files, with the `KARAR_INTEGRATION`-gated readiness and rate-limit-store suite **run separately and passing 12**; **4** golden baselines locally and **36** localization-gate tests; **113 passed / 1 skipped** in the mobile security suite; generated-client drift in sync at **35 operations / 98 schemas**; runtime OpenAPI conformance **61 tests** validating **82 of 128** declared operation/status pairs with **zero** media-type deviations and **zero** prose-only operations; architecture runner **24 passed / 0 failed / 4 phase-deferred**, self-test **56/56**; documentation checks **13/13** with a **14/14** self-test; **38 migrations applied in full sequence `0001` to `0086`** with verification clean; an Android release assembled **unsigned** under `lintVitalRelease` with application id `com.kararfinance.app.local`; iOS packaged bundles across four environments (`.local`, `.dev`, `.staging`, unsuffixed) **with no provisioning profile**.

**The Flutter figure is stated as three numbers because one number cannot carry it honestly.** The suite holds **1209 tests**. **CI's `mobile` lane executes 1190 and skips 19**, and that is what the phrase *as CI runs it* means: the lane builds no Android or iOS artifact, so the artifact-gated assertions in `apps/mobile/test/security/platform_hardening_test.dart` and `apps/mobile/test/security/ios_packaged_bundle_test.dart` find no merged manifest and no packaged bundle and mark themselves skipped. Those 19 execute on the `mobile-android` and `mobile-ios` lanes under `KARAR_VERIFY_ANDROID_ARTIFACT` and `KARAR_VERIFY_IOS_ARTIFACT`, **where a missing artifact is a failure rather than a skip** (KAR-CTL-111) — the same gate that, seen from the security suite's own side, leaves it at **113 passed / 1 skipped** in a tree without artifacts. The same command in a working tree that already holds artifacts yields **1208 passed / 1 skipped**. 1190 + 19 and 1208 + 1 are the same 1209 tests.

**IMPLEMENTED means the mechanism exists in-repo with passing tests and, where stated, holds against a real build artifact. It does not mean the mechanism operates anywhere, and it never means a device ran it.** The EV-431–EV-466 family indexes the proofs. **This marker is UNRESOLVED**: unlike [C1], [P2], [P3], and [P3.5], **no Phase 4 gate has executed, no pull request exists, and no CI or Security run has been recorded against a Phase 4 head** — which is why EV-461, EV-463, EV-464, and EV-465 are `PENDING`. The standing rule applies at the gate: any row that cannot cite a green run reverts to `PENDING` with a reason and an owner, and any [P4] control whose named check did not run reverts to `DESIGNED`.

**Two Phase 4 caveats that apply to every row in its section.** First, **the device gap**: the client's own security behaviour — the biometric prompt, the platform keystore, the transfer path between two installed applications — is proven by unit tests, source scans, and artifact assertions, and **by nothing that ran on a phone** (KAR-RSK-031, 032, 033). Second, **the empty-surface gap**: every capability is still `NOT_IMPLEMENTED` and deployed nowhere, no endpoint exists for any environment, no jurisdiction or PolicyPack is approved, and no legal-document content exists in any deployed environment — so the client's capability-navigation allowlist is empty, its jurisdiction reference list is empty, and the positive paths behind several rows below are exercised only against synthetic fixtures and a LOCAL-only seed. That is proof by construction and by test, not proof by use, and no row below claims otherwise.

**A Phase 3.5 caveat that applies to every row in its section:** several of these controls are proven on *synthetic* fixtures because the production reality gives them nothing to act on — every capability in the registry is `NOT_IMPLEMENTED` and deployed nowhere, so the availability resolver's first gate denies all seven before any later gate is reached, and `qa/v1` clears no capability at all. The positive paths are exercised against test-local registries and synthetic packs whose identifiers never enter the production union, the production registry, client output, or a database row. That is proof by construction and by test, not proof by production traffic, and no row below claims otherwise.

Owners are roles, defined in [`control-owners.md`](control-owners.md). Canonical design detail lives in `docs/security/*`, `docs/architecture/*`, and the ADRs — rows link rather than restate.

## Controls

### Governance

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-001 | A defined set of security policies (14, [policy-index](policy-index.md)) exists, is approved by the Platform Owner, and is reviewed at phase gates | DESIGNED | Security Owner | CC1.1, CC5.3 | 5.1 | none yet | 1 | All 14 currently DRAFT, approval pending |
| KAR-CTL-002 | Security responsibilities are assigned to named roles with documented separation-of-duties triggers at headcount thresholds | DESIGNED | Platform Owner | CC1.3, CC1.4 | 5.2, 5.3 | none yet | 1 | Current single-person reality stated in control-owners.md |
| KAR-CTL-003 | Risks are assessed on the 5x5 methodology, recorded in the risk register, and reviewed at every phase gate | DESIGNED | Security Owner | CC3.1, CC3.2, CC3.4, CC9.1 | Clause 6.1 (27001) | EV-008 (COLLECTED) | 1 | Methodology and register exist; four per-phase reviews recorded (Phases 1, 2, 3, 3.5 — risk-register §Notes). Status stays DESIGNED because the control's "reviewed at every phase gate" leg is performed and signed by the same person who wrote the register (EXC-001); the review happens, its independence does not |
| KAR-CTL-004 | Each phase ends with a recorded security/compliance gate review per [phase-compliance-gate.md](phase-compliance-gate.md) | IMPLEMENTED | Compliance Owner | CC4.1, CC4.2 | 5.35, 5.36 | EV-008 (COLLECTED) | 1 | Moved DESIGNED → IMPLEMENTED 2026-08-16: the mechanism has run three times and produced records — the Phase 1 gate (PASSED), Phase 2 (PASS_WITH_DOCUMENTED_DEFERRED_ITEMS), and Phase 3 (PASS_WITH_DOCUMENTED_DEFERRED_ITEMS), all in `phase-compliance-gate.md`; the Phase 3.5 checklist is PREPARED with its outcome PENDING. IMPLEMENTED, not beyond it: three records are not an operating history, and every one was produced and reviewed by the same person (EXC-001) |
| KAR-CTL-005 | Deviations from any policy or control are recorded in the exceptions register with compensating controls and an expiry or exit trigger | DESIGNED | Compliance Owner | CC1.5, CC5.3 | 5.36 | none yet | 1 | Three exceptions currently open |
| KAR-CTL-006 | Every public technical or legal claim maps to an Assurance Claim Registry entry with evidence pointer and owner | DESIGNED | Compliance Owner | CC2.3 | 5.31, 5.36 | none yet | 1 | Registry exists (`docs/security/assurance-claims.md`); mechanical link check is architecture test 26, Phase 2+ |

### Access control

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-007 | Repository and CI access is restricted to authorized accounts with MFA enforced | IMPLEMENTED | Security Owner | CC6.1, CC6.2, CC6.3 | 5.15, 5.16, 8.4, 8.5 | EV-007 (REVIEWED 2026-08-15) | 1 | Moved DESIGNED → IMPLEMENTED on the EV-007 verification, per this row's own trigger |
| KAR-CTL-008 | The default branch is protected: PRs required, required status checks, force-push and deletion denied | IMPLEMENTED | Engineering Owner | CC6.1, CC8.1 | 8.4, 8.32 | EV-007 (REVIEWED 2026-08-15) | 1 | Settings export exists; required states and verification commands: `docs/operations/repository-security-settings.md` |
| KAR-CTL-009 | CI credentials follow least privilege: default token read-only, scopes elevated per job, no long-lived cloud credentials in CI | DESIGNED | Engineering Owner | CC6.1, CC6.3 | 8.2, 5.17 | none yet | 1 | Verified against workflow files at the Phase 1 gate |
| KAR-CTL-010 | Application authentication hardening: lockout without counter reset, trusted-proxy IP resolution, normalised-path rate policy, short-lived tokens with rotation and server-side revocation | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.2, CC6.6 | 8.5, 5.16, 5.17 | EV-301, EV-303, EV-304 | 3 | Delivered 2026-08-16 as designed (threat model T4): ledger-derived non-resetting lockout (KAR-CTL-070), trusted-proxy resolution — X-Forwarded-For honored only under explicit trusted-proxy config (`packages/platform/src/http/trusted-proxy.ts`), per-key rate policies with declared failure modes, ES256 access tokens with rotating refresh families and server-side revocation (KAR-CTL-067, 068). 17 HTTP endpoints under `modules/identity` |
| KAR-CTL-011 | Tenant isolation: RLS enabled and FORCEd on every table or explicitly allow-listed; adversarial cross-tenant tests assert on non-empty data | IMPLEMENTED [P3] | Engineering Owner | CC6.1, CC6.3 | 8.3 | EV-307, EV-308, EV-316, EV-421, EV-423 | 3 | ADR-0022 delivered 2026-08-16; architecture test 22 ACTIVE (every migration-created table has RLS ENABLE+FORCE or a complete allow-list entry; stale/duplicate/incomplete entries fail the run). 2026-08-16 (Phase 3.5): the inventory moved 37 → **48 tables — 22 RLS ENABLE+FORCE, 33 allow-listed with per-table justification (`packages/platform/db/rls-allow-list.json`), 7 in both**; test 22 gained anti-boilerplate rules requiring each allow-list reason to state why no tenant or subject predicate fits and to describe the actual compensating controls, so a copied justification fails the run. Adversarial suites prove non-empty two-tenant seeding before every denial assertion, now including the Phase 3.5 surface (`tests/security/__tests__/phase-3-5-policy-surface.integration.test.ts`) |
| KAR-CTL-012 | Sealed payload access requires a compiler-enforced `SealAccessGrant`; no support/admin/analytics/AI grant type exists | DEFERRED | Security Owner | CC6.1, C1.1, C1.2 | 8.3, 8.24 | none yet | 13 | ADR-0017; architecture tests 13/14 |
| KAR-CTL-013 | Environment access is mediated by the control plane: short-lived, single-environment, purpose-scoped tokens; production gateway adds reason capture and reauthentication | DEFERRED | Platform Owner | CC6.1, CC6.2, CC6.3 | 8.2, 5.15 | none yet | 8, 20 | ADR-0021; separate deployment is a Phase 20 gate |
| KAR-CTL-014 | Access rights are reviewed at every phase gate (quarterly once environments operate) and on any role change | IMPLEMENTED | Security Owner | CC6.2, CC6.3 | 5.18 | Phase 3.5 gate record §9 ([phase-compliance-gate.md](phase-compliance-gate.md)) | 1 | **The control statement's present tense was aspirational through Phases 1–3 and was corrected rather than quietly reworded: no gate record before Phase 3.5 contained an access review, and the gate report's required sections had no place to put one.** Raised as CI-004. Fixed forward: `phase-compliance-gate.md` gained a standing **§9 Access review** section, and **the Phase 3.5 gate record is the first to actually contain one** — repository and CI access, the registrar/DNS account, the (empty) set of cloud and vendor accounts, and the application catalogue at 14 permissions and 8 roles with the four Phase 3.5 permissions recorded as deliberately unseeded. Moved DESIGNED → IMPLEMENTED 2026-08-16 on that record, which is exactly the trigger this row previously stated. **IMPLEMENTED, and not a step beyond it:** one review is not a review history, the reviewed set is trivial (one maintainer, one SCM account, no environments), and the reviewer and the reviewed are the same person (EXC-001). The registrar account's own posture remains asserted rather than observed (EV-427, PENDING) |

### Change management

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-015 | All changes reach the default branch through pull requests; no direct pushes | IMPLEMENTED | Engineering Owner | CC8.1 | 8.32 | EV-001 (COLLECTED), EV-007 (REVIEWED 2026-08-15) | 1 | Enforcement verified by the EV-007 settings export (PR-only, force-push and deletion denied) |
| KAR-CTL-016 | CI checks are merge-blocking: a failing required check prevents merge, not merely a red run | IMPLEMENTED | Engineering Owner | CC8.1, CC7.1 | 8.32, 8.29 | EV-007 (branch protection verified 2026-08-15); EV-001 (COLLECTED 2026-08-15) | 1 | Branch protection on `main` requires the 8 CI checks; admins bound. The legacy's gates blocked runs, not merges (INFRA-07) — this control exists because of that. 2026-08-18 (Phase 4): **the required set did not grow with the surface.** Three lanes landed this phase — `mobile-android`, `mobile-ios`, `mobile-supply-chain` — and **none of them is a required check**, so the Android release assembly, the merged-manifest comparison, the unzipped-APK credential scan, the packaged-bundle iOS assertions, and the mobile secret scan all run without being able to stop a merge. `mobile` (analysis, client drift, localization gate, suite) is required and was already in the eight. Recorded rather than left to inference; the gap is KAR-CTL-113 and KAR-RSK-037, and the settings change is the Engineering Owner's at the next branch-protection review |
| KAR-CTL-017 | Every change is reviewed by someone other than its author before merge | EXCEPTION | Engineering Owner | CC8.1, CC4.2 | 8.32, 5.3 | none yet | 1 | EXC-001: single-approver reality; compensating controls recorded there |
| KAR-CTL-018 | Sensitive change classes (migrations, financial rules, AI changes, key operations, capability availability, jurisdiction policy) pass staging before production | DEFERRED | Operations Owner | CC8.1, A1.1 | 8.31 | none yet | 19 | List: `docs/architecture/environments.md` §4 |
| KAR-CTL-019 | Architectural decisions are recorded as ADRs; accepted ADRs are superseded, never edited | IMPLEMENTED | Platform Owner | CC2.2, CC8.1 | 8.27 | in-repo: `docs/adr/` (26 ADRs) | 0 | Practised through Phases 0–0.5; not yet independently evidenced |

### Secure development

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-020 | Architecture tests run in CI as merge-blocking structural controls (money-path, RLS shape, sealed rules, guard call sites, resource-limit declarations) | IMPLEMENTED [C1] | Engineering Owner | CC8.1, CC7.1, PI1.1 | 8.25, 8.27, 8.29 | EV-002 (COLLECTED); EV-217 (COLLECTED 2026-08-16); EV-315; EV-422 | 1 | Harness landed Phase 1; the suite accretes toward the 26 in `docs/testing/architecture-tests.md`. 2026-08-15 (Phase 2): tests 5, 6, 23 activated and 25 deepened to the full six-field rule. 2026-08-16 (Phase 3): tests 9 (tenant scoping), 21 (entity/jurisdiction pinning), 22 (RLS coverage) activated and test 4 narrowed (Prisma/pg imports confined to `packages/platform/src/db/**` and `packages/platform/prisma/client/**`) — 23 passed / 0 failed / 5 skipped (13/14/19/24/canary deferred to later phases by registry design), registry activation errors 0, built-in self-test 35/35 seeded-violation cases (EV-315). 2026-08-16 (Phase 3.5): **24 passed / 0 failed / 4 skipped** (13/14/24/canary remain deferred by registry design), 0 registry activation errors, self-test **56/56** — test 12 (no jurisdiction or country business branching) now bites real jurisdiction code; test 17 gained a second tier covering `packages/capability-registry` by manifest **and** source-import scanning; test 19 (approval-policy completeness) activated with three arms — static pack-source inspection, validator rule presence, and every non-test `validate` call passing `disclosureBearingCapabilityIds`; test 21's Phase 3.5 deferral machinery was **removed and replaced with real enforcement** of the migration-0086 pin columns; test 22 gained the anti-boilerplate allow-list rules (KAR-CTL-011); test 26 now verifies that cited repository paths resolve on disk and that evidence ids are well formed (EV-422). 2026-08-18 (Phase 4): **the runner did not move** — still 24 passed / 0 failed / 4 phase-deferred (13, 14, 24, canary), 0 registry activation errors, self-test 56/56, with **no test newly activated or strengthened** (EV-459). Two consequences are recorded rather than absorbed: the Phase 4 client is TypeScript-free Dart and **no architecture test reaches it** — its structural rules are enforced by a separate Dart scan (KAR-CTL-094) that this runner neither owns nor executes; and the register-integrity sweep the Phase 3.5 gate scheduled into this suite for Phase 4 (CI-006) **was not added**, which is why KAR-CTL-116 is DESIGNED |
| KAR-CTL-021 | Security requirements derive from the threat model, and every control ships a test that fails when the control is removed | DESIGNED | Security Owner | CC3.2, CC8.1, PI1.1 | 8.26, 8.25 | none yet | 1 | Canonical: `docs/security/threat-model.md`; test rule in CONTRIBUTING |
| KAR-CTL-022 | Greenfield rule: no legacy application code is ported into V2 | IMPLEMENTED | Platform Owner | CC8.1 | 5.32, 8.25 | AC-012 (assurance registry, verified at docs level 2026-08-15) | 0.5 | Re-verified at each phase gate while legacy references remain |
| KAR-CTL-023 | Documentation integrity checks (links, docs conventions) run in CI | IMPLEMENTED [C1] | Engineering Owner | CC2.2 | 5.37 | EV-006 (COLLECTED), EV-460 | 1 | Supports drift risk KAR-RSK-010. 2026-08-18 (Phase 4): the check set grew **7 → 13** with a **14/14** self-test — added were `phase-status-consistency` (a phase's claimed status must agree across the report, roadmap, and README), `phase-template-sections`, `mermaid-sanity`, and the three style rules this repository runs on prose (`no-emoji`, `no-ai-attribution`, `no-generated-by-line`, `no-assistant-boilerplate`) with a `style-allowances-live` rule that fails the run when a narrow per-occurrence allowance stops matching, so a suppression cannot outlive its reason (EV-460) |
| KAR-CTL-024 | New capabilities begin with a complete `MODULE.md` (17-point checklist, incl. legal documents and data lifecycle) before code | DESIGNED | Platform Owner | CC3.2, CC8.1 | 5.8, 8.26 | none yet | 1 | Template exists; first exercised when the first module lands |

### Supply chain

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-025 | Dependency vulnerability scanning (SCA) runs on every PR and blocks merge on findings above threshold | DESIGNED | Security Owner | CC7.1, CC8.1 | 8.8, 5.21 | EV-004 (COLLECTED), EV-455 | 1 | Scan runs per-PR from Phase 1 but is **report-only**; the blocking threshold is pending (tightening criterion in `docs/operations/repository-security-settings.md`; Phase 1 gate deferred the decision to the Phase 2 gate). Status honest to the control statement, which requires blocking. 2026-08-18 (Phase 4): a **second package ecosystem** entered scope with the Flutter client, and its supply-chain reporting (`mobile-supply-chain`, security.yml) is **report-only for the same reason and additionally does not gate a merge** — there is no advisory baseline for pub yet; the secret-scan step inside that job is the one part that fails the job. `pnpm audit --audit-level high` reported no known vulnerabilities at this head. One advisory was acted on rather than reported: `deepmerge-ts` reaches the tree only transitively through `@prisma/config`, and versions below 8.0.0 carry `GHSA-ggr8-5vv4-36mx` / `CVE-2026-40345` (uncontrolled recursion); `pnpm-workspace.yaml` forces the patched line at an **exact `8.0.1`**, with the reachability argument and the removal trigger recorded beside the pin instead of used to skip it (KAR-CTL-110) |
| KAR-CTL-026 | Secret scanning runs on every PR (and against history), blocking merge on findings | IMPLEMENTED [C1] | Security Owner | CC6.1, CC7.1 | 5.17, 8.12 | EV-005 (COLLECTED), EV-456 | 1 | Local pre-commit coverage is partial — EXC-003. 2026-08-18 (Phase 4): coverage extended to the mobile surface in two places, one of which is a build output rather than a source tree — the `mobile-supply-chain` job runs its secret scan **before Flutter is installed**, deliberately, so a scan cannot be affected by what a package resolution pulled in, and the Android lane **unzips the built APK and scans it for credential material**. Neither lane is a required check (KAR-CTL-113), so both are detective without being merge-blocking |
| KAR-CTL-027 | An SBOM is generated per CI build and retained as a build artifact | IMPLEMENTED [C1] | Engineering Owner | CC7.1 | 5.21 | EV-003 (COLLECTED), EV-457 | 1 | Format/tool per Phase-1 CI implementation. 2026-08-18 (Phase 4): the `sbom` job generates SPDX JSON over the **repository root** (`path: .`) and uploads it under the stable `sbom` artifact name, so the Flutter client's committed `pubspec.lock` is inside the scanned path — but **no assertion checks that the emitted document actually enumerates the pub packages**, and none is claimed here. Dependency-snapshot submission stays off so the workflow token remains read-only |
| KAR-CTL-028 | Dependencies and toolchain are pinned (committed lockfiles, declared tool versions, version-pinned base images) and updated only through reviewed PRs | IMPLEMENTED [C1] | Engineering Owner | CC7.1, CC8.1 | 5.21, 8.32 | EV-001 (COLLECTED), EV-455, EV-466 | 1 | Digest-pinning of container images is the target state; verify at Phase 1 gate. **2026-08-18 (Phase 4): a pin was found pointing at the wrong commit, and only a sweep would have found it.** All fourteen third-party action pins across both workflows were resolved against the tags their comments name, resolving annotated tags rather than trusting the comment; **one disagreed** — `actions/download-artifact` was SHA-pinned to a commit whose comment claimed `v6.0.0`, and that commit is a README merge in that repository rather than the release, so the pin was to an arbitrary point in history under the authority of a version number. Corrected in `be0d35a`; the other thirteen matched. **A pinned SHA is only as strong as the claim attached to it, and nothing checked the claim until this sweep** (EV-466). 2026-08-18 (Phase 4): the rule reached the pub ecosystem and became an executable assertion rather than a convention — every direct non-SDK mobile dependency carries an **exact version with no caret and no range**, and the pinning suite fails on an inexact constraint, on a `git:`/`path:`/`hosted:` nested-block bypass that is not one of the three SDK-sourced entries, on a missing `flutter_secure_storage`/`local_auth`/`dio`/`go_router`/`flutter_riverpod` (so *removing* one also fails), on any `dependency_overrides` section, and on a lockfile that does not record hosted sources with checksums. It opens with a guard that the parser found dependencies at all, so a parsing regression cannot make the rest vacuous. **The regression that motivated it was real:** `local_auth` had drifted to a caret range with nothing failing. Both workflows now run `flutter pub get --enforce-lockfile`, and — because `--enforce-lockfile` faithfully enforces whatever the lockfile says — **host exclusivity is asserted separately**: every hosted package must come from pub.dev and only pub.dev, with a per-package checksum (KAR-CTL-110; EV-455) |
| KAR-CTL-029 | Static analysis security testing runs in CI (lint/type checks plus CodeQL from Phase 1; scope grows with application code) | IMPLEMENTED [C1] | Security Owner | CC7.1 | 8.28, 8.29 | none yet | 1 | CodeQL job in `security.yml` runs from Phase 1 against tooling/scripts; meaningful application coverage begins with Phase-2 code |

### Infrastructure (future-gated)

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-030 | Environment ladder with full isolation: per-environment databases, keys, secrets, capped AI spend; boot-time environment identity assertion | DEFERRED | Operations Owner | CC6.1, A1.1 | 8.31 | none yet | 17–19 | Design: `docs/architecture/environments.md`; Compose profiles from Phase 1 keep the discipline pre-cloud |
| KAR-CTL-031 | Infrastructure is defined as code (Terraform compositions per deployment/environment) and changed only via reviewed PRs | DESIGNED | Operations Owner | CC8.1 | 8.9, 8.32 | none yet | 1 | Skeleton in `infra/terraform` from Phase 1; nothing provisioned |
| KAR-CTL-032 | Transport protections: TLS on every connection, database transport authenticated (`verify-full`), edge rate limiting | DEFERRED | Operations Owner | CC6.6, CC6.7 | 8.20, 8.21 | none yet | 17+ | `docs/security/secrets.md` §10; legacy ENC-1 lesson |

### Data protection

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-033 | Every column, event field, log statement, and projection column carries one of the six data classes, with handling per the classification matrix | IMPLEMENTED [P2] | Security Owner | CC6.1, C1.1 | 5.12, 5.13 | EV-204, EV-208 (COLLECTED) | 2 | Canonical: `docs/security/data-classification.md`. 2026-08-15: enforcement landed at Phase 2 — classification vocabulary in code, event payload rules, logger redaction, audit metadata guard, and six-field lifecycle rows for every Phase 2 table (`packages/platform/db/DATA_LIFECYCLE.md`); KAR-CTL-061 is the enforcement mechanism, and coverage extends to each new surface as it lands |
| KAR-CTL-034 | Encryption at rest by class: field-level AES-256-GCM for `HIGHLY_SENSITIVE_FINANCIAL`, per-record DEKs for `SEALED`, KMS-held KEKs — with coverage measured, not assumed | DEFERRED | Security Owner | C1.1, CC6.7 | 8.24 | none yet | 2 (design), 13, 20 | ADR-0017; coverage tool required by `docs/security/secrets.md` §8. 2026-08-15: the Phase-2 design portion exists as code contracts (KAR-CTL-064); encryption itself remains deferred |
| KAR-CTL-035 | Key custody, rotation, and the sealed-integrity canary follow ADR-0017; an approved `KeyCustodyStrategy` precedes any production `SEALED` data | DEFERRED | Security Owner | CC6.7, A1.2 | 8.24 | none yet | 13, 20 | Hard Phase 20 gate; legacy ENC-2 is the origin. 2026-08-15: custody, rotation, and canary contracts pinned in code at Phase 2 (KAR-CTL-064, 065 — DESIGNED); the gates stand unchanged |
| KAR-CTL-036 | Secrets live in per-environment stores, never in the repository, logs, or error messages; `.env.example` carries placeholders only | DESIGNED | Security Owner | CC6.1 | 5.17 | EV-456 | 1 | Detective layer is KAR-CTL-026; canonical: `docs/security/secrets.md`. 2026-08-18 (Phase 4): the rule acquired its first **client-side** expression, which matters because a mobile artifact is public by nature — the client holds no secret at all, a compile-time define whose **name** looks credential-shaped is a configuration violation reported by key name and never by value, signing material comes only from environment variables or an ignored file, and the built APK is unzipped and scanned in CI. Status stays DESIGNED: per-environment secret stores still do not exist, because no environment does |
| KAR-CTL-037 | Every persistent dataset declares subject relationship, purpose, classification, retention, export treatment, and erasure strategy, CI-enforced | DEFERRED | Privacy Owner | P4.1, P4.2, C1.2 | 8.10 | none yet | 5 | ADR-0026; architecture test 25 |
| KAR-CTL-038 | No real customer or personal data exists in any environment; all test and fixture data is synthetic | IMPLEMENTED | Privacy Owner | C1.1, P1.1 | 8.33 | EV-446 | 1 | True today by construction (no system, no customers); becomes an enforced rule at Phases 2 (seed data) and 19 (staging). 2026-08-18 (Phase 4): the rule was tested against its first hard case. The synthetic legal-document text that lets the read-and-accept path run end to end in LOCAL and TEST **is not a legal document**, and it is now **physically absent from a deployed artifact rather than merely refused at runtime** — the bytes live in `packages/consent-local-fixtures`, which appears in no production `dependencies` block anywhere, and the consent module names the package in exactly one place through a guarded runtime resolution rather than an import. A closure test walks the real production dependency closure and the real build output asserting every fixture marker is absent, and **carries a positive control proving the same scanner finds them in the fixture package itself** — an absence test that cannot demonstrate its own scanner works proves nothing (EV-446) |

### Logging and monitoring (future-gated)

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-039 | Append-only audit trail; every staff read of a customer record is audited, including reads returning nothing | DEFERRED | Security Owner | CC7.2, CC7.3 | 8.15 | none yet | 2, 8 | Threat model T5; legacy AZ5. 2026-08-15: the append-only store and immutability mechanism landed at Phase 2 (KAR-CTL-056); this row stays DEFERRED because its staff-read half activates with admin surfaces at Phase 8 |
| KAR-CTL-040 | Logs redact `CONFIDENTIAL` and above; `SECRET` and `SEALED` never appear in logs, events, projections, analytics, or AI context | IMPLEMENTED [P2] | Engineering Owner | CC7.2, C1.1 | 8.15, 8.11 | EV-208 (COLLECTED) | 2 | Architecture test 13 (deepens at Phase 13). 2026-08-15: the redaction mechanism landed at Phase 2 for logs, events, and audit metadata; projections, analytics, and AI context do not yet exist and inherit the mechanism when they do |
| KAR-CTL-041 | Monitoring and alerting with severity-differentiated routing, on-call rotation, and escalation | DEFERRED | Operations Owner | CC7.2, CC7.3, A1.1 | 8.16 | none yet | 20 | A single alert recipient is not on-call (`environments.md` §10) |

### Incident response

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-042 | Incidents are handled per the incident-response policy: severity model (sealed exposure is SEV-1 at n=1), roles, communication, and post-incident review feeding the improvement log | DESIGNED | Security Owner | CC7.3, CC7.4, CC7.5 | 5.24, 5.25, 5.26, 5.27 | none yet | 1 | Policy DRAFT; no operations to respond for yet |
| KAR-CTL-043 | A private vulnerability-reporting channel is published and acknowledged | IMPLEMENTED | Security Owner | CC2.3, CC7.3 | 6.8 | in-repo: `SECURITY.md` | 0.5 | No SLA pre-on-call, stated honestly in SECURITY.md |

### Availability, backup, continuity (future-gated)

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-044 | Backups run per policy with restore verification that includes application recovery, not only data restore | DEFERRED | Operations Owner | A1.2, A1.3 | 8.13 | none yet | 17, 20 | Legacy: RPO evidenced, RTO never measured |
| KAR-CTL-045 | Business continuity and DR: runbook executed and RTO measured before production launch | DEFERRED | Operations Owner | A1.2, A1.3, CC9.1 | 5.29, 5.30, 8.14 | none yet | 20 | Phase 20 gate list |
| KAR-CTL-046 | Canonical source and documentation are fully recoverable from any complete clone; no canonical artefact exists only in SCM-hosted state | DESIGNED | Operations Owner | A1.2 | 8.13 | EV-218 (COLLECTED) | 1 | Treatment for KAR-RSK-011; evidence-register export rule supports it; first fresh-clone verification is a Phase 2 close item |

### Vendor management

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-047 | Vendors and subprocessors are recorded in the register with data shared, DPA status, and review dates; new vendors get a security review before adoption | DESIGNED | Compliance Owner | CC9.2 | 5.19, 5.20, 5.21, 5.22, 5.23 | EV-427 (PENDING) | 1 | Register exists and the adoption leg works — Cloudflare was recorded with its scope and classification at adoption, 2026-08-16. **The periodic-review leg did not:** the five pre-existing vendor rows carried "Next review: Phase 2 gate" through the Phase 2 and Phase 3 gates without the review happening or the lapse being raised, contradicting register rule 3. Raised as a Phase 3.5 gate finding (CI-005); the rows now carry a dated register-level re-read of 2026-08-16 and a next date, and the gate checklist makes the check explicit. Status stays DESIGNED: a register re-read is not the vendor security review this control describes, and none has been performed since the Phase 1 baseline. **2026-08-18 (Phase 4): the adoption leg was exercised seven times and the review leg still has not run.** Phase 4 added **seven suppliers** — six directly-declared mobile packages plus a federated platform interface — and **all seven entered the build before any security review**, which is what register rule 1 exists to prevent. They are recorded so the gate reads a current list, and **recording is not reviewing**. The pinning and provenance controls that now sit under them (KAR-CTL-110: exact versions, host exclusivity, per-package checksums, named owners and review triggers for the two packages that decide credential storage and lock behaviour) reduce the *supply-chain* exposure and do nothing about the *vendor-review* obligation, which is a different control. CI-005 remains open and now falls due at the Phase 4 gate |
| KAR-CTL-048 | A DPA is executed with every processor before any personal data reaches it | DEFERRED | Privacy Owner | CC9.2, P6.1 | 5.20, 5.34 | none yet | 20 | Roadmap non-engineering gate; no personal data is processed today |

### Privacy

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-049 | Consent gates fail closed; republication of a legal document triggers re-consent evaluation with materiality decided, never defaulted | IMPLEMENTED [P3] | Privacy Owner | P2.1, P3.1 | 5.34 | EV-311, EV-414 | 3 | ADR-0024 delivered 2026-08-16 (legacy P1/P12 origin): `AssertConsentFor` fails closed on any non-ACTIVE status; re-consent classification (MATERIAL_REACCEPTANCE_REQUIRED / NOTICE_REQUIRED / NO_USER_ACTION_REQUIRED) records reviewer and reason with no default; publication without a classification is blocked (CHECK + typed error). 2026-08-16 (Phase 3.5): the PolicyPack **mechanism** for "which purposes legally require consent per jurisdiction" landed (KAR-CTL-080) and consent grants now pin the pack version that produced them (KAR-CTL-084) — but no pack **decides** anything: `qa/v1` carries `consentRequirements` and every processing basis as an explicit `PENDING_LEGAL_REVIEW`, so the basis-reference table still fails closed. The mechanism moved from absent to typed and pinned; the legal answers did not move at all. 2026-08-18 (Phase 4): acceptance became **reachable again, in LOCAL and TEST only** — the Phase 3.5 gate carried it as a regression in reachability (`POST /consent/acceptances` answered 503 to every caller, including local, because a valid grant must pin jurisdiction, active pack version, operating entity, and document version and no runtime write path existed). A local-and-test-only prerequisite seed plus self-declared jurisdiction (KAR-CTL-102) restores the path there, and **the pin requirement was not relaxed to do it** — the refusal to record an unpinned grant is now asserted on real serialized bytes rather than held by a comment. In every deployed environment the gap is unchanged and a second one is stated: **no legal-document content exists anywhere deployed** (KAR-CTL-103), so the content endpoint answers 409 with reason `NOT_RETRIEVABLE` and the client renders a localized "not available" notice rather than composing substitute wording. **No pack is approved, no capability is cleared, and no counsel or regulator has reviewed anything in this repository** |
| KAR-CTL-050 | Data export and erasure are derived from lifecycle declarations, and export coverage reconciles with declared treatments | DEFERRED | Privacy Owner | P4.3, P5.1, P5.2 | 5.34, 8.10 | none yet | 5, 16 | ADR-0026; legacy P5/P7 origin |

### Platform foundation (Phase 2)

Controls delivered by the Phase 2 platform code. Canonical design detail: `docs/security/threat-model.md` ("Phase 2 platform threats"), ADR-0012, ADR-0013, ADR-0017, ADR-0025, ADR-0026. Test paths cited are the mechanisms' own suites; their outputs are the EV-201–EV-219 evidence family.

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-051 | Runtime configuration is typed and fail-fast: boot refuses on missing or invalid configuration, and errors name the field and env var only — values are never printed | IMPLEMENTED [P2] | Engineering Owner | CC7.1, CC8.1 | 8.9 | EV-209, EV-214 (COLLECTED) | 2 | `packages/platform/src/config`; `ConfigurationError` carries the never-print-values rule in its contract |
| KAR-CTL-052 | Configuration secrets are opaque `SecretValue` holders, redacted on inspect and serialization; secrets are addressed by `karar-ref:*` references, never provider resource names; no direct `process.env` access outside the config loader and adapters | IMPLEMENTED [P2] | Security Owner | CC6.1 | 5.17, 8.12 | EV-209 (COLLECTED) | 2 | Enforced by the `no-direct-env-access` test and profile-secrecy tests (`packages/platform/src/config`) |
| KAR-CTL-053 | Database least privilege: `karar_migrator` (owns schemas, DDL only, carries no SUPERUSER/BYPASSRLS/CREATEDB/CREATEROLE) is separated from `karar_app` (per-table minimal DML, no DDL); denials proven live as 42501 | IMPLEMENTED [P2] | Security Owner | CC6.1, CC6.3 | 5.15, 8.2, 8.3 | EV-202 (COLLECTED) | 2 | Grant convention: no `GRANT ALL`, no PUBLIC; probed live by `packages/platform/src/db/contract.test.ts` |
| KAR-CTL-054 | Migrations are forward-only and checksum-verified with drift detection: a sha256 mismatch or a missing/renamed applied file hard-fails `migrate` and `verify`; from-zero database creation is a tested path | IMPLEMENTED [P2] | Engineering Owner | CC8.1 | 8.32, 8.9 | EV-201, EV-215 (COLLECTED) | 2 | `packages/platform/src/db/migrations.ts`; every migration file carries a `-- rollback:` block, executed by a human decision, never automatically |
| KAR-CTL-055 | Destructive database operations are double-guarded: `reset-local` refuses unless `KARAR_ENV=local` **and** the target host is loopback; there is deliberately no force flag | IMPLEMENTED [P2] | Engineering Owner | CC8.1, A1.2 | 8.32 | EV-214, EV-215 (COLLECTED) | 2 | `packages/platform/src/db/bootstrap.ts` — a guard that can be flagged away in the destroying command is not a guard |
| KAR-CTL-056 | The audit trail is append-only twice over: `karar_app` holds INSERT+SELECT only on `audit.audit_events`, and an immutability trigger raises on UPDATE/DELETE/TRUNCATE even for the owner; audit write failure surfaces as a typed error result, never swallowed | IMPLEMENTED [P2] | Security Owner | CC7.2, CC7.3 | 8.15, 5.33 | EV-207 (COLLECTED) | 2 | `modules/audit`; both denial paths (42501 and P0001) proven live. The Phase 2 mechanism behind KAR-CTL-039 |
| KAR-CTL-057 | Audit metadata is classification-guarded: secret-patterned keys are rejected outright, `SEALED` values are rejected outright, and `HIGHLY_SENSITIVE_FINANCIAL` values are stored redacted unless identifier/status-shaped | IMPLEMENTED [P2] | Security Owner | C1.1, CC7.2 | 8.15, 8.11 | EV-207, EV-208 (COLLECTED) | 2 | `modules/audit/application/audit-metadata-guard.ts` |
| KAR-CTL-058 | Event publication is transactional: outbox rows commit atomically with state; relays claim via `FOR UPDATE SKIP LOCKED`, mark publication exactly once, back off on failure, dead-letter at max attempts, and recover stale claims; lag and failure metrics are exposed | IMPLEMENTED [P2] | Engineering Owner | PI1.1, CC7.2 | 8.16, 8.25 | EV-203, EV-206, EV-212 (COLLECTED) | 2 | ADR-0012; concurrency proven with 2 relays × 200 events (`packages/platform/src/outbox`). Delivery is at-least-once by design; KAR-CTL-060 makes that safe |
| KAR-CTL-059 | Background jobs execute under single-claim leases with caller-supplied idempotency keys, priorities, bounded retries, dead-lettering, a payload-size cap, and graceful shutdown | IMPLEMENTED [P2] | Engineering Owner | PI1.1, A1.1 | 8.6, 8.16 | EV-205, EV-212 (COLLECTED) | 2 | ADR-0013; concurrency proven with 2 workers × 100 jobs (`packages/platform/src/jobs`) |
| KAR-CTL-060 | Event consumers are idempotent by receipts: a (consumer, event id) receipt makes redelivery of an already-processed event a no-op | IMPLEMENTED [P2] | Engineering Owner | PI1.1, PI1.3 | 8.25 | EV-203 (COLLECTED) | 2 | `packages/platform/src/outbox/receipts.ts`; receipts outlive the outbox rows they protect (`packages/platform/db/DATA_LIFECYCLE.md`) |
| KAR-CTL-061 | The six-class data classification is enforced at every platform surface that exists: event payload rules (`SEALED` identifier/status-only with **no exemption mechanism**; `HIGHLY_SENSITIVE_FINANCIAL` identifier-only without a declared owner/reason/reviewer exemption), logger redaction, and audit metadata all consume one classification vocabulary | IMPLEMENTED [P2] | Security Owner | C1.1, CC6.1 | 5.12, 5.13, 8.11, 8.12 | EV-204, EV-208 (COLLECTED) | 2 | `packages/platform/src/classification`; the enforcement mechanism KAR-CTL-033 names |
| KAR-CTL-062 | Readiness is truthful: `/readyz` on both api and worker (the worker on its own port) checks live PostgreSQL connectivity and migration status, and reports not-ready when a dependency fails | IMPLEMENTED [P2] | Operations Owner | CC7.1, A1.1 | 8.16 | EV-216 (COLLECTED), EV-434 | 2 | `apps/api/src/health`, `apps/worker/src/health-server.ts`; a readiness probe that cannot fail is decoration, not a control. 2026-08-18 (Phase 4): the probe set was **incomplete in a way that mattered, and the defect proves the control's own premise**. The rate-limit store was never connected before the server began listening; the client is lazy and holds no offline queue, so the first command lost the handshake and was rejected outright, and **every fail-closed identity policy — login, verification, password reset, MFA, invitations — answered 503 on the first request after boot** while readiness reported ready. The connection is now established during composition **before `listen()`**, and readiness probes the store with one real round trip inside a budget rather than reading a status field (`apps/api/src/health/readiness-probes.ts`). The regression test restarts real containers and is gated behind `KARAR_INTEGRATION=1`; it is not run by the default local gate, and the CI workspace lane now starts Redis alongside PostgreSQL so it can run at all (KAR-CTL-105; EV-434) |
| KAR-CTL-063 | Observability is classification-aware: OTel-compatible logs, traces, and metrics pass through the classification redaction vocabulary; `SEALED` is never loggable on any path | IMPLEMENTED [P2] | Engineering Owner | CC7.2, C1.1 | 8.15, 8.16, 8.11 | EV-208 (COLLECTED) | 2 | `packages/platform/src/observability`. Signals exist; alerting and on-call remain DEFERRED (KAR-CTL-041) — this control emits, it does not watch |
| KAR-CTL-064 | Key custody is contract-first: every encrypt/wrap result carries `KeyRef`/`KeyVersionRef` provenance; the four custody models of ADR-0017 are a closed enum; `KeyManagementProvider`/`EncryptionProvider` ports define the provider seam; identifiers derive from content, never from key material or ciphertext (rotation-vs-identifier rule) | DESIGNED | Security Owner | C1.1, CC6.7, A1.2 | 8.24 | EV-211 (COLLECTED) | 2 | `packages/platform/src/keys`; contract tests pin the design, but no enforcing provider runs — the only provider is test-only in-memory. **No cloud KMS, no production keys.** Implementation lands with KAR-CTL-035 at Phases 13/20 |
| KAR-CTL-065 | The sealed-integrity canary is contract-pinned: canary plaintext must carry the `KARAR-CANARY-` synthetic-marker prefix, structurally excluding customer-derived content from the one record that is decrypted on schedule | DESIGNED | Security Owner | CC7.2, A1.2 | 8.24, 8.16 | EV-211 (COLLECTED) | 2 | `packages/platform/src/keys/canary.ts`; the operating canary is a Phase 13 build and Phase 20 gate (KAR-CTL-035). Until it runs in production, key-loss detection is test-time only |

### Identity, tenancy, and access control (Phase 3)

Controls delivered by the Phase 3 identity, tenancy, authorization, consent, operating-entity, and control-plane code. Canonical design detail: `docs/security/threat-model.md` ("Phase 3 identity, tenancy, and access-control threats"), ADR-0021, ADR-0022, ADR-0024. Test paths cited are the mechanisms' own suites; their outputs are the EV-301–EV-317 evidence family. No HTTP surface exists this phase for role administration, kill-switch operation, or entity administration — those arrive with the control plane (ADR-0021).

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-066 | Password credential storage: argon2id with pinned, versioned parameters (memoryCost 131072, timeCost 2, parallelism 1; params_version 1) and rehash-on-login when parameters advance; verification and reset codes stored as HMAC digests with attempt caps and cooldowns; no plaintext credential material at rest | IMPLEMENTED [P3] | Security Owner | CC6.1 | 5.17, 8.5 | EV-301, EV-302 | 3 | `modules/identity/infrastructure/crypto/argon2-password-hasher.ts`, `modules/identity/infrastructure/crypto/node-credential-digester.ts`. Parameter sets are versioned in code, so a parameter change is a reviewed diff, not drift; no permission returns credential material (migration 0050 header) |
| KAR-CTL-067 | Access tokens are minimal and revocable: ES256-signed tokens carry {sub, sid, iss, aud, iat, exp, tv} only — no roles, permissions, or e-mail; authorization is re-derived from the database per request; the account token version (`tv`) invalidates outstanding tokens on password change, disable, or global revocation | IMPLEMENTED [P3] | Security Owner | CC6.1 | 5.16, 8.5 | EV-304 | 3 | `modules/identity/infrastructure/crypto/jose-token-signer.ts`, `modules/identity/application/use-cases/authenticate-request.ts`. A token cannot carry a stale grant because it carries no grant at all |
| KAR-CTL-068 | Refresh-token lifecycle: 32-byte random tokens stored only as sha256 digests; one-time rotation is atomic in SQL; tokens form families, and reuse of a rotated token revokes the family and its session and records the event; sessions are server-side rows revocable individually and in bulk | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.2 | 5.16, 5.17, 8.5 | EV-303, EV-304 | 3 | `modules/identity/application/use-cases/refresh-session.ts`, `modules/identity/application/use-cases/session-lifecycle.ts`; migration 0031. Reuse detection fires on the second presentation, not the first — the threat model carries that residual |
| KAR-CTL-069 | MFA foundation: TOTP enrolment, confirmation, challenge, and disable with secrets encrypted at rest through the platform `EncryptionProvider` (`KeyVersionRef` provenance); recovery codes stored as one-time sha256 hashes with attempt limits; a secret-leak regression suite asserts secrets never serialize outward | IMPLEMENTED [P3] | Security Owner | CC6.1 | 8.5 | EV-305 | 3 | `modules/identity/application/use-cases/mfa.ts`, `modules/identity/infrastructure/providers/platform-mfa-secret-cipher.ts`; migration 0032. Local runs encrypt under the local-only dev provider (KAR-CTL-078); KMS custody remains Phases 13/20 (KAR-CTL-035) |
| KAR-CTL-070 | Lockout and abuse throttling: lockout is derived by COUNT over the append-only `authentication_security_events` ledger per (account, IP digest), so engaging the lock erases nothing and the counter cannot reset; distributed per-key rate limits front every pre-auth entry point — fail-closed (503 DEPENDENCY_UNAVAILABLE) for login, verification, reset, and MFA, fail-open to a bounded in-process fallback for refresh, each mode declared per policy; client IP is honored only under explicit trusted-proxy configuration | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.6 | 8.5, 5.16 | EV-301 | 3 | `modules/identity/application/use-cases/login.ts`; `packages/platform/src/ratelimit/` (per-policy `onStoreFailure` modes); `packages/platform/src/http/trusted-proxy.ts`; migration 0033. Legacy AUTHN-11 (resettable counter) is the origin. 2026-08-18 (Phase 4): the fail-closed modes were found to be **failing closed at the wrong moment** — until the rate-limit store was connected before `listen()`, every policy declared fail-closed answered 503 on the first request after boot, which is the declared behaviour firing on a defect rather than on an outage. Fixed with KAR-CTL-105, and the regression is now proven by a suite that stops and restarts the real store rather than by a mock |
| KAR-CTL-071 | Principal context discipline: persistence touching principal-scoped tables runs inside `withPrincipalContext`, which binds tenant/user GUCs transaction-locally (`set_config(…, is_local => true)`) from server-side records, never client input, throws before any query on missing keys, and shadows absent optional keys; architecture test 9 blocks unscoped persistence paths and forbids session-scoped binds (`set_config(…, false)`) and `SET SESSION` on `app.*` GUCs | IMPLEMENTED [P3] | Engineering Owner | CC6.1, CC6.3 | 8.3 | EV-307, EV-308, EV-315 | 3 | `packages/platform/src/db/principal-context.ts`; `scripts/checks/architecture.mjs#checkTenantScoping`. Pooled-connection hygiene proven on both sanctioned pools (pg adapter and Prisma) |
| KAR-CTL-072 | RBAC is deny-by-default against a closed catalogue: 14 permissions seeded by migration and asserted equal to the code catalogue; no wildcards, structurally (grammar CHECK); 8 roles; the central PolicyService caches nothing beyond request scope; denials are audited; deliberately absent permissions deny by absence (`identity.account.disable`/`enable` documented but unseeded — no invoking surface; `amanat.content.read` absence test-pinned) | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.3 | 5.15, 8.2, 8.3 | EV-309 | 3 | `modules/authorization/domain/catalogue.ts`, `modules/authorization/application/policy-service.ts`; migrations 0050–0052 (0050's header records the unseeded pair) |
| KAR-CTL-073 | Privileged-role delegation is peer-gated: `authorization.role.assign` is PLATFORM_ADMIN-only, and assigning PLATFORM_ADMIN requires the actor to hold it; assignment writes are RLS-bound to the transaction's target principal; revoked assignments are immutable by trigger even for the owner; grants and revocations are audited with actor and reason | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.2, CC6.3 | 8.2, 5.18 | EV-309 | 3 | `modules/authorization/application/use-cases/assign-role.ts`, `modules/authorization/application/use-cases/revoke-role.ts`; migration 0052. No role-administration HTTP surface exists this phase (ADR-0021): abuse requires code execution, and equally no operator can grant or revoke over HTTP |
| KAR-CTL-074 | Invitation security: invitation tokens exist at rest only as sha256 digests; redemption is token-scoped RLS in a two-transaction pattern — the presented token's hash binds a transaction-local GUC exposing exactly one row, with no privilege elevation; membership INSERT is database-bound to the authenticated redeemer (self-scoped INSERT policies); per-invitation attempt caps kill guessing; the lifecycle is audited | IMPLEMENTED [P3] | Security Owner | CC6.1, CC6.2 | 5.17, 5.18, 8.3 | EV-306 | 3 | `modules/tenancy/application/use-cases/redeem-invitation.ts`, `modules/tenancy/infrastructure/providers/sha256-invitation-token-source.ts`; migrations 0043–0044 (RLS-04 lesson). The raw token is a bearer capability in flight; e-mail transport is out of scope this phase — the local mail sink never sends (KAR-CTL-078) |
| KAR-CTL-075 | Kill switches are restrict-only: the four Phase 3 switches (NEW_REGISTRATIONS, PASSWORD_LOGIN, SESSION_REFRESH, TENANT_INVITATIONS) can only deny an operation, never enable one; missing, inactive, or expired switches evaluate unrestricted; guarded reads fail CLOSED on store outage (503 DEPENDENCY_UNAVAILABLE); every state change increments a version counter by exactly one and auto-appends an append-only history via SECURITY DEFINER trigger; operating a switch requires `controlplane.killswitch.operate` with mandatory reason and actor, audited | IMPLEMENTED [P3] | Operations Owner | CC6.6, CC7.4 | 5.26, 5.29 | EV-310 | 3 | `modules/control-plane/domain/kill-switch.ts`, `modules/control-plane/application/use-cases/`, `modules/control-plane/presentation/http/kill-switch.guard.ts`; migration 0053. The guard mechanism is implemented and tested; mounting on registration/login/refresh/invitation routes happens at phase integration and is verified at the Phase 3 gate (KAR-RSK-019). No kill-switch HTTP operate surface exists this phase (ADR-0021) |
| KAR-CTL-076 | Consent is immutable, pinned evidence: grants pin `operating_entity_id` and `jurisdiction_ref` NOT NULL at creation (architecture test 21, which pins `data_protection_role_assignments` the same way); the only transitions are ACTIVE → WITHDRAWN (row preserved) and ACTIVE → SUPERSEDED (a re-grant inserts a new row), trigger-enforced with no DELETE grant; legal documents cannot publish without a recorded, reviewed classification (CHECK + typed error), and published versions are immutable even for the owner | IMPLEMENTED [P3] | Privacy Owner | P2.1, C1.1 | 5.33, 5.34 | EV-311, EV-414 | 3 | `modules/consent/domain/`, `modules/consent/application/use-cases/`; migrations 0064–0065. 2026-08-16 (Phase 3.5): the policy-pack pin arrived as promised — migration `0086` completes the pinning block, and test 21's Phase 3.5 deferral machinery was removed in favour of real enforcement (KAR-CTL-084). `reconsent_evaluations` is allow-listed rather than RLS-scoped: a decision record about a document version with no tenant/user columns; per-subject effects resolve via RLS'd `consent_grants` (migration 0065 header) |
| KAR-CTL-077 | Security events are recorded as evidence: authentication events (failures, locks, refresh reuse, pre-auth mutations) land in the append-only `authentication_security_events` ledger; role grants and revocations, authorization denials, kill-switch operations, invitation lifecycle, and consent decisions are audited with actor — and reason where the operation demands one | IMPLEMENTED [P3] | Security Owner | CC7.2, CC7.3 | 8.15 | EV-301, EV-309, EV-310 | 3 | `modules/identity/infrastructure/persistence/prisma-security-event-store.ts` (migration 0033); per-module audit-trail adapters over the Phase 2 append-only store (KAR-CTL-056), e.g. `modules/authorization/infrastructure/audit/record-audit-event-audit-trail.ts` and its peers in control-plane, tenancy, and users. Ledgers hold even against the migrator role (EV-312). Nothing watches these signals yet (KAR-RSK-003) |
| KAR-CTL-078 | Local-only providers fail closed: `LocalDevEncryptionProvider` and `LocalMailSink` refuse to construct outside `KARAR_ENV=local`, so a non-local deployment cannot silently encrypt under a development key or swallow outbound mail — boot fails loudly instead | IMPLEMENTED [P3] | Security Owner | CC7.1, CC6.7 | 8.9 | EV-313 | 3 | `packages/platform/src/keys/local-dev-encryption-provider.ts`, `packages/platform/src/notifications/local-mail-sink.ts`. The flip side is availability: no real mail or KMS provider exists, so flows needing them run only locally (KAR-RSK-020) |

### Jurisdiction, capability, and session binding (Phase 3.5)

Controls delivered by the Phase 3.5 jurisdiction, PolicyPack, capability, subject-policy, and session-binding code. Canonical design detail: `docs/security/threat-model.md` ("Phase 3.5 jurisdiction, capability, and tenant-binding threats"), ADR-0014, ADR-0015, ADR-0016, ADR-0021, ADR-0024, and `docs/architecture/jurisdiction-policy.md`, `capability-registry.md`, `tenancy.md`. Test paths cited are the mechanisms' own suites; their outputs are the EV-401–EV-429 evidence family. **No operator HTTP surface exists for any of it** — the four declared permissions (`jurisdiction.assignment.manage`, `jurisdiction.pack.activate`, `capability.availability.manage`, `capability.entitlement.manage`) are deliberately **unseeded**, so against the real `PolicyService` every mutating use case in these modules refuses today (ADR-0021; the control-plane surface and the seeding migration land at Phase 8).

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-079 | Jurisdiction assignment carries provenance and verification as **separate typed axes**, never collapsed into a boolean: source is one of USER_DECLARED / PROVIDER_VERIFIED / OPERATOR_ASSIGNED / CONTRACT_DERIVED, verification is UNVERIFIED / VERIFIED, and USER_DECLARED is CHECK-bound to UNVERIFIED so a self-selected country can never become a verified jurisdiction by any path; assignments are effective-dated end-only history, immutable by guard trigger even for the table owner; user assignments are RLS ENABLE+FORCEd on both principal GUCs; resolution returns a three-arm `EffectiveJurisdictionState` in which NONE and UNVERIFIED deny alike | IMPLEMENTED [P3.5] | Privacy Owner | CC6.1, CC6.3, P3.1 | 5.15, 8.3, 5.31 | EV-401 | 3.5 | `modules/jurisdiction/domain/assignment.ts`, `modules/jurisdiction/application/use-cases/user-assignments.ts`, `tenant-assignments.ts`; migrations 0072–0073. **`VERIFIED` records an operator's or provider's assertion, not an executed identity check** — no identity-verification provider exists this phase, so the strength behind the word is a Phase 8 control-plane question (KAR-RSK-022). 2026-08-18 (Phase 4): the model gained its **first runtime write path**, and it can only write the weakest pair — `POST /jurisdiction/self-declaration` records `USER_DECLARED` / `UNVERIFIED` with both values as **module-level constants rather than inputs**, the CHECK constraint binds the pair independently at the schema, and a declaration **cannot supersede a verified assignment**. It exists because both operator write paths are gated on deliberately unseeded permissions, which left bootstrap permanently reporting no jurisdiction. What a declaration changes is **which denial a subject sees, and nothing else** (KAR-CTL-102; EV-444) |
| KAR-CTL-080 | PolicyPacks are **pure reviewed code, never database rows**: a typed `PolicyPack` per (jurisdiction, version) with a DRAFT / PENDING_LEGAL_REVIEW / APPROVED / RETIRED lifecycle, a three-arm `PolicyDecision` union (DECIDED with its basis · PENDING_LEGAL_REVIEW · UNRESOLVED) so an undecided legal question is a typed state rather than a fabricated answer, and `validatePack`/`validatePackSet` structural validation that the activation use case runs **before any write**, refusing on any finding | IMPLEMENTED [P3.5] | Compliance Owner | CC3.2, CC8.1, P2.1 | 5.31, 8.26, 8.27 | EV-402, EV-415, EV-416 | 3.5 | `packages/jurisdiction-policy/src/policy-pack.ts`, `packages/jurisdiction-policy/src/decision.ts`, `packages/jurisdiction-policy/src/validation.ts`, `packages/jurisdiction-policy/src/packs/qa-v1.ts`. **No pack decides anything today.** `qa/v1` is DRAFT / PENDING_LEGAL_REVIEW with `approvalReference: null`, an EMPTY `clearedCapabilities`, and every decision slot an explicit PENDING_LEGAL_REVIEW carrying its open question. Approval is a legal act this repository cannot perform, and none has occurred (KAR-RSK-023, AC-018) |
| KAR-CTL-081 | An unapproved policy never governs a non-local environment: `canActivate` is a pure predicate evaluated before any write — a pack that is not APPROVED activates **only** in `local`, and a pack claiming APPROVED with no `approvalReference` is refused **everywhere**; the runtime activation use case validates the pack structurally and then re-applies the same predicate, so a denial leaves the ledger at zero rows | IMPLEMENTED [P3.5] | Compliance Owner | CC6.6, CC8.1 | 8.31, 8.32 | EV-402, EV-416 | 3.5 | `packages/jurisdiction-policy/src/lifecycle.ts`; `modules/jurisdiction/application/use-cases/pack-activation.ts`. Residual, carried honestly: the predicate reads the **process's own** environment identity, so a deployment mislabelled `local` would defeat it — which is why environment identity is boot-time typed configuration (`packages/platform/src/config`) and never a request input (KAR-RSK-023) |
| KAR-CTL-082 | The pack-activation ledger is append-only twice over and stores **metadata only, never pack content**: `karar_app` holds SELECT+INSERT only, and a statement-level immutability trigger raises on UPDATE/DELETE/TRUNCATE even for the table owner (so a zero-row update raises too); what a pack *says* is unreachable from the database because packs are reviewed code | IMPLEMENTED [P3.5] | Compliance Owner | CC7.2, CC7.3 | 8.15, 5.33 | EV-403 | 3.5 | Migration 0075; `modules/jurisdiction/domain/pack-activation.ts`. Same residual as every append-only table: a cluster superuser can drop the trigger (T5, EV-207, EV-312); off-host shipping is a later-phase control |
| KAR-CTL-083 | Every capability resolution records the **provenance it used**: the pack version, the availability row id and version, and the entitlement id and version, so two resolutions racing a change disagree explainably rather than silently. This is pinning for traceability, not serialization — no lock is taken and none is claimed | IMPLEMENTED [P3.5] | Engineering Owner | CC7.2, PI1.1 | 8.15, 8.25 | EV-419 | 3.5 | `modules/capability/domain/resolution.ts`, `modules/capability/application/use-cases/resolve-capability-availability.ts`. An operator-facing view of these pins is deferred with the Phase 8 control plane |
| KAR-CTL-084 | Legal-consequence evidence is schema-bound to the policy that produced it: migration `0086` pairs every version column with a **NOT NULL pin-state column** over a closed vocabulary (PINNED · NOT_APPLICABLE · PRE_POLICY_PACK / PRE_SUBJECT_POLICY_SELECTION), CHECKs tie PINNED to a non-null non-empty value, and a creation-cutoff CHECK refuses the historical PRE_* states for any row created from Phase 3.5 on; guard triggers cover the pin columns so a pin cannot be rewritten; the writing use cases take the version as **input** and neither module resolves policy itself | IMPLEMENTED [P3.5] | Privacy Owner | P2.1, C1.1, CC7.2 | 5.33, 5.34 | EV-414 | 3.5 | `packages/platform/db/migrations/0086_legal_consequence_pack_pins.sql`; enforced by `scripts/checks/architecture.mjs#checkPinning` (test 21, whose Phase 3.5 deferral machinery this migration retired). Pre-Phase-3.5 rows record PRE_POLICY_PACK / PRE_SUBJECT_POLICY_SELECTION — **an honest statement that no version was ever resolved, not a fabricated sentinel version**. `subject_policy_selections` was found to be a third legal-consequence table and is now declared as one. Residual: `created_at` is settable by the writing transaction, so the cutoff arm is dodgeable by a caller supplying a pre-cutoff timestamp — compensated by the guard triggers, the absent DELETE grant, and the required use-case input (KAR-RSK-029) |
| KAR-CTL-085 | Runtime jurisdiction settings are **restrict-only by shape, not by rule**: `JurisdictionRuntimeSettings` has no field, and `jurisdiction_settings` no column, capable of expressing an enablement — every one narrows what the code pack permits (disable list, AI suspension) — an absent row restricts nothing, and the resolver's merge is subtractive; `karar_app` is SELECT-only on the table, so the write path is a reviewed migration | IMPLEMENTED [P3.5] | Compliance Owner | CC6.1, CC8.1 | 8.9, 8.32 | EV-404, EV-417 | 3.5 | Migration 0074; `packages/jurisdiction-policy/src/resolution/effective-policy.ts`; property-tested in `modules/capability/__tests__/restrict-only.property.test.ts`. The shape argument holds only while the table keeps that shape — a future column with enabling semantics would defeat it silently, so this table is named explicitly in the Phase 8 review that would open its write path (KAR-RSK-024) |
| KAR-CTL-086 | The compile-time capability registry is the **ceiling**, and configuration cannot raise it: a closed `CapabilityId` union (TRANSACTIONS, BUDGETS, GOALS, INSIGHTS, AI_ADVISOR, ZAKAT, AMANAT — FUNDRAISING deliberately absent, with no id, no descriptor, and nothing able to reference it) with **three separated state dimensions** — lifecycle (product intent) · implementation (does the code exist) · per-environment deployment — because they legitimately disagree; adding or changing an entry is a reviewed code change, never a configuration change | IMPLEMENTED [P3.5] | Platform Owner | CC8.1, CC6.1 | 8.27, 8.32 | EV-405, EV-418 | 3.5 | `packages/capability-registry/src/index.ts`, `validation.ts`; architecture test 17's second tier holds the package framework-free by manifest and source-import scanning. **Every real capability is `NOT_IMPLEMENTED`, deployed nowhere, and declares an empty jurisdiction list.** Amanat is additionally HIDDEN and disclosure-bearing, so no client can learn it exists (KAR-RSK-008). Zakat carries a Sharia-review launch gate that no code can discharge — a non-engineering gate recorded in `modules/zakat/MODULE.md` and the roadmap, not a registry field (KAR-RSK-007, AC-010) |
| KAR-CTL-087 | Capability availability resolves through **eight deny-by-default gates whose order is itself the control**: gates 1–4 (descriptor · environment · jurisdiction and pack clearance · availability row) consume **no grant-like input** and run before gates 5–8 (entitlement · consent · licence · provider), so nothing a grant can say widens a ceiling denial; absence denies at every gate (a missing availability row is DISABLED, a missing entitlement denies, a missing processing basis denies, an unresolved option set denies, a store outage denies), a jurisdiction-specific row takes precedence over an environment-wide one, and the resolver never returns an untyped allow | IMPLEMENTED [P3.5] | Platform Owner | CC6.1, CC6.6 | 5.15, 8.2, 8.3 | EV-405, EV-406, EV-417 | 3.5 | `modules/capability/application/use-cases/resolve-capability-availability.ts`, `modules/capability/domain/denial-reason.ts`. Monotonicity is proven as a **property** over roughly 2,400 exhaustive plus 2,000 seeded randomized configurations, not asserted in prose (EV-417). The operator use case additionally refuses to *record* an allowing state for a NOT_IMPLEMENTED capability and audits the attempt as DENIED. Deny-by-default converts a configuration outage into unavailability — the monitoring consequence is KAR-RSK-003's, not this control's (AC-016, AC-020) |
| KAR-CTL-088 | Tenant capability entitlements gate but never price: `tenant_capability_entitlements` is RLS ENABLE+FORCEd on the tenant GUC with a `SECURITY DEFINER` trigger-written append-only history (`karar_app` holds no INSERT on the history at all, and `UNIQUE (row_id, version)` forbids skipped or forked history); revocation is a status, never a deleted row; every UPDATE must increment `version` by exactly one; the resolver validates the window **temporally at read time** rather than trusting a stored status, so a lapsed window denies while the row still says ACTIVE; the table carries **no plan, price, or subscription concept** — `source_ref` is an opaque seam a future subscription module fills through a port | IMPLEMENTED [P3.5] | Engineering Owner | CC6.1, CC6.2, CC6.3 | 5.15, 5.18, 8.3 | EV-407 | 3.5 | Migration 0077; `modules/capability/application/use-cases/manage-entitlements.ts`, `modules/capability/domain/entitlement.ts`. Both the availability and entitlement tables ship with **no rows**. Temporal validation depends on the injected instant reaching the use case — a wrong host clock moves every window together (KAR-RSK-025) |
| KAR-CTL-089 | Client exposure is decided in **exactly one place, deny-by-default**: a HIDDEN capability is omitted from client output in **every** state including a fabricated ALLOWED, and a denial whose reason is not client-surfaceable is omitted **entirely** rather than rendered as `available: false` with an internal reason — legal, jurisdictional, entity, licence, and not-yet-built grounds never reach a client, and PENDING_PROVIDER surfaces only where a descriptor opts in (no production descriptor does) | IMPLEMENTED [P3.5] | Security Owner | CC6.1, C1.1 | 8.11, 8.12 | EV-408 | 3.5 | `modules/capability/domain/client-view.ts`, `denial-reason.ts`; leak-regression suites drive hostile ports that try to attach extra fields at the edge. Residual: omission hides the reason, not the fact that a request took a different path — **response-timing equivalence is not asserted this phase** and is deferred with the T9 discipline (AC-019). 2026-08-18 (Phase 4): the rule acquired its first consumer, and the client was built to match it rather than to reinterpret it — capability navigation is a **compile-time allowlist**, so an id the server omits produces no destination, no count, and no readable state, and there is **deliberately no collection of unrecognised ids**, because such a collection would itself be a channel for the names it holds. The allowlist is empty, so the non-empty path is exercised only against synthetic fixtures (KAR-CTL-101; EV-447) |
| KAR-CTL-090 | Subject policy selections are universal metadata with **capability-owned content**: `subject_policy_selections` is classified CONFIDENTIAL, RLS ENABLE+FORCEd on **both** principal GUCs (a same-tenant different-user read returns nothing; a forged-identity INSERT is refused by WITH CHECK), immutable with trigger-bounded transitions so re-election inserts a new row and supersedes rather than editing, restrict-only against the pack's permitted option set (an option outside it is refused OPTION_NOT_PERMITTED; an unresolved option set fails closed; a pack version changing between validation and pin is detected as PACK_VERSION_MISMATCH with nothing written), temporally validated on read, and **audited by reference only — never option content**; it publishes no events and exposes no HTTP, and no staff, support, tooling, or analytics read path exists | IMPLEMENTED [P3.5] | Privacy Owner | C1.1, P3.1, P4.1 | 5.12, 5.13, 8.3, 8.11 | EV-409, EV-410 | 3.5 | Migration 0083; `modules/subject-policy/`. This table's isolation says nothing about the **content** an owning capability stores elsewhere — that protection arrives with the capability that owns it (KAR-RSK-026). A published event would be a side channel for a CONFIDENTIAL election, which is why the module publishes none by rule |
| KAR-CTL-091 | Session tenant binding is authorization, not client input: a binding moves null → value only, from a **server-side membership read**, never from the request; membership is re-verified at bind time; a switch atomically revokes the old session **and its refresh family** before issuing the new bound session, so no token survives pointing at the previous tenant; the switch runs verify → act → re-verify → compensate, revoking the replacement session when a membership vanishes inside the race window; arbitrary, revoked, expired-window, and disabled-tenant targets are denied uniformly without reaching the rebind seam; the first-party tenant comes from typed configuration, with **no magic UUID in domain code** | IMPLEMENTED [P3.5] | Security Owner | CC6.1, CC6.2, CC6.3 | 5.16, 5.18, 8.3, 8.5 | EV-411, EV-412, EV-413, EV-420 | 3.5 | `modules/identity/application/use-cases/session-tenant-binding.ts`; `modules/tenancy/application/use-cases/switch-tenant.ts`, `resolve-tenant-context.ts`; migrations 0080–0081 add read-only self and member SELECT arms so an unbound principal can list only their own memberships without any tenant GUC being fabricated. **Stated plainly: RLS cannot re-check membership** — a transaction that binds `app.tenant_id` from a session row sees that tenant's rows until the session is revoked, so the window between revocation and the next resolution is real, bounded by re-verification at every bootstrap read and by revocation on detection (KAR-RSK-030). Closes KAR-RSK-021. 2026-08-18 (Phase 4): the mechanism gained its **first real consumer**, which neither widens nor narrows the server-side race — the client presents choices and sends a selection, **never asserts a tenant**, and takes its binding from the server's own membership read; a switch explicitly invalidates the tenant-scoped providers rather than relying on a rebuild, so a screen cannot render the previous tenant's data after the session rotated (KAR-CTL-101; EV-443) |
| KAR-CTL-092 | The bootstrap response is minimized structurally: `GET /platform/bootstrap` and `POST /platform/tenant-binding` serialize a **closed field set picked name by name**, so hidden capabilities, Amanat's existence, unimplemented and pending-legal capabilities, licence internals, full PolicyPack content, raw consent evidence, internal audit or configuration data, and synthetic test capabilities are all outside the shape; the surface consumes the one client-safe capability projection **unenriched**, so the filter cannot drift into two implementations; enrichment ports return explicit nulls rather than fabricated defaults; the auto-bind side effect is declared in the OpenAPI contract rather than left to be discovered, and both outcomes are audited | IMPLEMENTED [P3.5] | Security Owner | CC6.1, C1.1 | 8.11, 8.12 | EV-408 | 3.5 | `modules/bootstrap/presentation/dto/bootstrap-responses.ts`, `modules/bootstrap/presentation/http/bootstrap.controller.ts`; leak regression in `modules/bootstrap/__tests__/leak-regression.test.ts`. A capability absent from the response is **not** one the client should render as unavailable — it is one the client must not know exists (KAR-RSK-028). 2026-08-18 (Phase 4): the closed field set was **widened deliberately and narrowed structurally in the same phase**. `capabilities` and `operatingEntity` became discriminated shapes — a resolution failure and an empty result are now different types on the wire, because a client cannot tell "you have nothing" from "we could not find out" — and a 503 `BOOTSTRAP_UNAVAILABLE` answers **all five** dependency failures with **one code and no `detail`**, so the response does not disclose which dependency failed. The new `OperatingEntity` summary crosses **four fields**, and the projection is enforced **by the SELECT rather than by a mapper**, so licence evidence, contract references, registration internals, role assignments, and administrative metadata never enter the process; a caller cannot name an entity or enumerate the register, only receive the one their own binding derives. `storageRef` was **removed** from the consent listing — a storage locator is not a client's business — and that omission is now asserted on real serialized bytes rather than held by a comment (KAR-CTL-104; EV-433, EV-447) |
| KAR-CTL-093 | The Phase 3.5 runtime tables change only through reviewed change, not through operation: `countries`, `jurisdictions`, and `jurisdiction_settings` carry **no `karar_app` write grant at all** — they change by reviewed migration only, with no use case, permission, or route to change them; capability descriptors are a reviewed compile-time constant; and all four declared permissions are **deliberately unseeded**, so deny-by-default means every mutating use case in these modules currently refuses against the real `PolicyService` | IMPLEMENTED [P3.5] | Engineering Owner | CC8.1, CC6.3 | 8.9, 8.32, 5.15 | EV-402, EV-405, EV-421 | 3.5 | Migrations 0070, 0071, 0074; `modules/capability/MODULE.md` and `modules/jurisdiction/MODULE.md` (Permissions sections). **Absence is the control here**, and it is temporary by design: the seeding migration and the operator surfaces arrive together at Phase 8 (ADR-0021), which is also when the settings write path is reviewed (KAR-CTL-085) |

### Client, mobile artifact, and build (Phase 4)

Controls delivered by the Phase 4 Flutter client, generated API client, backend contract hardening, and Android/iOS build pipelines. Canonical design detail: `docs/security/threat-model.md` ("Phase 4 client and mobile-artifact threats"), ADR-0007, ADR-0009, ADR-0016, ADR-0024, and `docs/architecture/flutter.md`. Test paths cited are the mechanisms' own suites; their outputs are the EV-431–EV-466 evidence family. **Phase 4 introduced no migration, no new table, no new permission, and no operator surface** — it is read, projection, presentation, and build work over the Phase 3.5 schema, and every new operation is self-scoped.

**How to read the proof class of a row below**, because it varies within this section in a way it did not in earlier ones. `CODE` means the mechanism exists with passing tests. `ARTIFACT` means an assertion read a **real build output** — a merged manifest, an unzipped APK, a packaged iOS bundle — which is stronger than source inspection and weaker than a device. `INFRASTRUCTURE` means the proof needs live PostgreSQL and Redis and cannot be produced by a bare checkout. **No row is proven on a device**, and the rows where that gap is the whole point say so in their own text.

| ID | Control | Status | Owner | SOC 2 TSC | ISO 27002:2022 | Evidence | Phase | Notes |
|---|---|---|---|---|---|---|---|---|
| KAR-CTL-094 | Mobile client structure is enforced rather than reviewed: feature-first Clean Architecture with dependencies pointing inward only, and a **scan** — not a convention — forbidding any file under `features/*/domain/` from importing Flutter, Riverpod, go_router, dio, secure storage, biometric plugins, the generated DTOs, `dart:convert`/`dart:io`/`dart:ui`, or any analytics or fingerprinting package; failures are values through a sealed `Failure` union paired with `Result` so a `switch` is exhaustiveness-checked, and composition happens through one mechanism with no service locator, no static singleton, and no global mutable instance | IMPLEMENTED [P4] | Engineering Owner | CC8.1, PI1.1 | 8.25, 8.27, 8.28 | EV-436 | 4 | `apps/mobile/test/core/architecture/layer_rules_test.dart`; contract stated in `apps/mobile/lib/README.md`. **The architecture runner does not reach this code** — `scripts/checks/architecture.mjs` scans TypeScript, so the Dart rules are a separate scan that the `mobile` CI lane executes (KAR-CTL-020). Ten feature folders exist and **every one is account, identity, or platform state**: there is no feature folder for a product capability, because no product capability exists |
| KAR-CTL-095 | The API client is generated from the contract and never hand-edited, and drift is detected contract-to-client on every run: `--check` regenerates in memory, needs no working tree, and runs as its own CI step ahead of the suite; the generator **refuses rather than guesses** — no invented response type where the contract gives no schema, no arbitrary choice between two schema-carrying 2xx responses, no union without a declared discriminator, and no wire value that cannot become a distinct Dart identifier | IMPLEMENTED [P4] | Engineering Owner | CC8.1, PI1.1 | 8.25, 8.27, 8.32 | EV-435 | 4 | `apps/mobile/tool/generate_api_client.dart`; `apps/mobile/lib/core/networking/generated/`; `apps/mobile/test/security/generated_client_sync_test.dart`. In sync at **35 operations / 98 schemas**. A companion test asserts the generated files still declare themselves generated, still carry matching digests, and still document the `--check` command, **so the CI step cannot quietly stop proving anything**. A defect this phase found and fixed makes the refusal rule concrete: identifier construction split on a fixed separator list, so the media type `text/markdown` carried its slash into emitted source that would not compile — every non-alphanumeric character is now a boundary, and three guards landed with the fix because a naming rule that maps many values onto one name is its own defect |
| KAR-CTL-096 | Client authentication surfaces preserve the server's own defences rather than undoing them at the edge: pre-authentication responses render a **receipt that does not distinguish a known address from an unknown one**, matching the server's enumeration defence; the MFA enrolment screen is **forbidden a golden baseline** by a scan, because a golden is a committed image of whatever was on screen and a setup key or recovery code must never be in one; and goldens are banned across the whole feature tree by the same scan | IMPLEMENTED [P4] | Security Owner | CC6.1, C1.1 | 5.16, 5.17, 8.5, 8.12 | EV-442 | 4 | `apps/mobile/test/features/authentication/`, `apps/mobile/test/features/mfa/`. Sign-in, registration, e-mail verification, password recovery and change, MFA enrolment, challenge, disable and recovery codes, and a session directory with individual and bulk revocation — each exercised in **both locales**, with every interactive control asserted to carry a name. The ban on goldens under `test/features` is a **source scan over the whole tree, not a convention** |
| KAR-CTL-097 | Device credential storage is single, namespaced, and fail-closed: exactly one secure entry holds the access and refresh tokens, their expiries, and the session id; platform options are **chosen with their reasons recorded beside them** rather than defaulted — `first_unlock_this_device` on iOS and macOS so the credential stays out of iCloud Keychain and off any restored backup, reset-on-error on Android so a key-material change forces re-authentication rather than leaving a half-readable store; every operation returns a `Result` in which a genuine absence and an unreadable store are **different values**; and only the key *name* is ever logged, never the value and never the platform error message, which can echo the entry | IMPLEMENTED [P4] | Security Owner | CC6.1, C1.1 | 5.17, 8.5, 8.12 | EV-437 | 4 | `apps/mobile/lib/core/security/token_store.dart`, `flutter_secure_store.dart`; `apps/mobile/test/core/security/secure_token_store_test.dart`. Non-sensitive preferences live in a **separate, deliberately unencrypted store whose key type refuses a credential-shaped name at construction**. Residual, stated rather than defended against: **a rooted or jailbroken device defeats platform storage, and root/jailbreak detection is deliberately not attempted** (KAR-RSK-040) |
| KAR-CTL-098 | Security-relevant local flags are held in a **dedicated fail-closed store with no in-memory fallback**: a closed enum of two keys (application-lock state, session-abandonment marker) so no credential-shaped key is expressible at all; every read distinguishes a value, a genuine absence, an unavailable store, and a corrupt one, and every write and removal distinguishes success, refusal, and unavailability; when the platform store will not open the port returns an implementation that **reports unavailability to every call** rather than an empty one that answers "absent"; and the state is loaded as its own explicit startup step **before the lock is evaluated and before any token is read** | IMPLEMENTED [P4] | Security Owner | CC6.1, CC7.1 | 8.9, 8.5 | EV-438, EV-439 | 4 | `apps/mobile/lib/core/security/local_security_state_store.dart`; `apps/mobile/test/core/security/local_security_state_store_test.dart`, `apps/mobile/test/core/storage/preferences_local_security_state_store_test.dart`. **This control exists because of a defect in its predecessor, and the defect is the argument for the control.** Both flags were kept in the ordinary preference store, which swallows a failed write and substitutes an in-memory store when the platform store will not open — defensible for a dismissed hint, and for the lock flag they compose into a **fail-open**: an unopenable store reads as absent, absent reads as `false`, and `false` reads as *the user never turned the lock on*. `KeyValueStore.writeBoolChecked`, the narrower fix attempted first, was **deleted** with it, so the ordinary store now offers no way to ask whether a write landed — the honest shape for a store that does not guarantee one |
| KAR-CTL-099 | Session continuation is single-flight and terminal-failure-closed: concurrent callers share **one** in-flight refresh, a caller whose token was already replaced takes the new one and issues nothing, an expired refresh chain ends the session **with no network call at all**, and the refresh request itself goes over a **separate raw transport with no session manager, no coordinator, and no retry policy**, which makes a refresh storm structurally impossible rather than merely unlikely; terminal failures end the session and wipe the credential while transient ones leave it intact; a 401 is replayed **once** and a second is authoritative; and **an unsafe request without an idempotency key is never replayed** | IMPLEMENTED [P4] | Security Owner | CC6.1, CC6.2, PI1.1 | 5.16, 8.5, 8.25 | EV-440 | 4 | `apps/mobile/lib/core/networking/`; `apps/mobile/test/core/networking/token_refresh_coordinator_test.dart`, `dio_api_transport_test.dart`. Ending a session is **idempotent**, so a failed refresh and a 401 arriving together cannot double-signal, and the in-memory credential is dropped **before** storage is wiped so a failed wipe still leaves the process credential-free. This is the client half of KAR-CTL-068's rotation and reuse detection; the theft window before a second presentation is unchanged and stays KAR-RSK-016's |
| KAR-CTL-100 | The device-local lock is not authentication and cannot become it: the biometric plugin is reached through **one** adapter that receives, stores, and transmits **no** biometric template, image, or derived representation and performs no custom biometric cryptography; **an unlock grants no session and never substitutes for signing in**; the failure mapping is fail-closed **including its default arm**, because the plugin's error list is explicitly open and an unseen code must not read as success; enabling the lock applies **only on a confirmed durable write**; and a disable that cannot be confirmed **retains the enabled state**, because the safe end of that failure is a lock the user must open | IMPLEMENTED [P4] | Security Owner | CC6.1 | 5.16, 5.17, 8.5 | EV-441 | 4 | `apps/mobile/lib/core/security/app_lock.dart`; `apps/mobile/test/features/session_management/platform_local_authenticator_test.dart`, `apps/mobile/test/app/routing/app_lock_cold_launch_test.dart`, `app_lock_recovery_test.dart`. Two shipped-configuration facts follow from the plugin and are asserted against a real build: the host activity is a `FlutterFragmentActivity`, and the launch and normal themes descend from `Theme.AppCompat` because **below API 28** the biometric prompt is drawn with an AppCompat dialog that throws under a non-AppCompat theme — `minSdk` is 24, so **API 24–27 ships inside that range**. **The prompt has never appeared on any device, on either platform** (KAR-RSK-031); a defect only running the application exposed — the lock screen's password fallback navigating into the redirect that returned it to the lock, trapping anyone whose authenticator had failed — is fixed by ending the session, and the new test presses the button rather than asserting it exists |
| KAR-CTL-101 | The client asserts no authority it was not given: tenant binding comes from the server's own membership read and the client only presents choices and sends a selection; a switch **explicitly invalidates the tenant-scoped providers** rather than relying on a rebuild, so a screen cannot render the previous tenant's data after the session rotated; and capability navigation is a **compile-time allowlist** in which an id the server omits produces no destination, no count, and no readable state, with **deliberately no collection of unrecognised ids** because such a collection would itself be a channel for the names it holds | IMPLEMENTED [P4] | Security Owner | CC6.1, CC6.3, C1.1 | 8.3, 8.11, 8.12 | EV-443, EV-447 | 4 | `apps/mobile/lib/features/tenant_selection/`, `apps/mobile/lib/features/platform_bootstrap/`; `apps/mobile/test/app/composition/feature_surface_test.dart`. Consumes KAR-CTL-089 and KAR-CTL-091 without re-deriving either. **The allowlist is empty**, which is correct rather than incomplete — nothing is implemented — and the consequence is that the resolved-and-non-empty path is exercised only against synthetic fixtures. **No financial value exists in any layer**: not in a screen, not in a fixture, not in a test, and a dedicated suite asserts none appears in any state including while loading |
| KAR-CTL-102 | Self-declared jurisdiction can only ever write the weakest pair: `USER_DECLARED` and `UNVERIFIED` are **module-level constants rather than inputs**, the schema constrains the pair independently, a declaration **cannot supersede a verified assignment**, the offered set and the accepted set share **one predicate** so the reference list and the write path cannot drift into disagreement, and a store failure returns 503 rather than an empty list — an empty list would read as "no jurisdiction may be declared", which is a different answer | IMPLEMENTED [P4] | Privacy Owner | CC6.1, CC6.3, P3.1 | 5.15, 5.31, 8.3 | EV-444 | 4 | `modules/jurisdiction/` (self-declaration use case and declarable-reference list); `GET /jurisdiction/declarable-references`, `POST /jurisdiction/self-declaration`. The client screen states in its **own outcome text** that the result remains unverified, and the domain type carries `isVerified` as a method that always returns false — present rather than absent, so a reader of the type cannot mistake a recorded declaration for a verified one. **A declaration changes which denial a subject sees and nothing else** (KAR-CTL-079). **The client's reference list is empty in this build**: the endpoint exists and the client provider still returns a constant empty list, so the screen always shows its unavailable notice |
| KAR-CTL-103 | Legal-document wording is served only where it exists, and never invented: the content endpoint is bound in every deployed environment to a source that **always refuses**, answering 409 with reason `NOT_RETRIEVABLE` — 409 rather than 404 deliberately, because the document exists and consent is still owed and only the wording is unavailable; an unknown document and another entity's document receive **byte-identical** answers so the catalogue is not an existence oracle; retrieved content whose SHA-256 does not match the published version is a 503 with **nothing served**; and the consent surface draws an action **only when the server said the action can work**, keeping three "nothing here" outcomes distinct — no applicable document, a failed read, and a document whose wording is unavailable | IMPLEMENTED [P4] | Privacy Owner | P2.1, P3.1, C1.1 | 5.33, 5.34, 8.12 | EV-445, EV-446 | 4 | `modules/consent/` (content endpoint and its refusing source); `apps/mobile/lib/features/consent/`. **No legal-document content exists in any deployed environment, and the LOCAL and TEST fixture is not a legal document** — inventing terms of service would be worse than admitting there are none. The fixture is **physically absent from a deployed artifact** rather than only refused at runtime (KAR-CTL-038, EV-446), and the environment gate still sits next to the bytes as a second, independent control. Response-timing equivalence is **not** asserted, the same deferral as T9 (KAR-RSK-028) |
| KAR-CTL-104 | Real serialized responses are validated against the contract that describes them: a suite drives the **composed application** — modules, guards, global exception filter, serializer, live PostgreSQL and live Redis — and checks status, `Content-Type`, and the returned bytes against the OpenAPI document, with two ledgers asserted **empty** so a regression is a failing test rather than a silent entry: no operation may describe its responses in prose alone, and no problem document may leave under any media type but `application/problem+json` | IMPLEMENTED [P4] | Engineering Owner | PI1.1, PI1.2, CC8.1 | 8.25, 8.29, 8.32 | EV-433 | 4 | `tests/conformance/`. **82 of 128** declared operation/status pairs validated, **0** media-type deviations, **0** prose-only operations. Both ledgers were non-empty when the suite was written — 25 pairs across five modules returned correctly shaped RFC 7807 bodies under `application/json` because the media type was set only by the error boundary and those 25 wrote to the reply object directly, and all 17 `/auth` operations described their bodies in prose with **no schema at all**, so the generated client decoded them as untyped JSON and no runtime check could hold the server to anything. The covered set is **named explicitly** at the end of the suite, so an operation that quietly stopped being exercised fails there. The contract validator implements a deliberate subset of JSON Schema and **throws on any keyword it does not implement**, so a schema cannot silently pass unchecked. Coverage is a real limit, not a rounding: **46 pairs are uncovered** and the suite's header enumerates them (KAR-RSK-039) |
| KAR-CTL-105 | A dependency that operations fail closed on is connected **before the server accepts traffic**, and readiness reports it: the rate-limit store connection is established during composition **before `listen()`**, and `/readyz` probes it with one real round trip inside a budget rather than reading a status field | IMPLEMENTED [P4] | Operations Owner | CC7.1, A1.1 | 8.16, 8.9 | EV-434 | 4 | `apps/api/src/main.ts`; `apps/api/src/health/readiness-probes.ts`, `readiness.service.ts`. **The defect this closes is the argument for the control** (KAR-CTL-062, 070): the store was never connected before listening, the client is lazy and holds no offline queue, so the first command lost the handshake and every fail-closed identity policy — login, verification, password reset, MFA, invitations — answered 503 on the first request after boot while readiness reported ready. The regression test **stops and restarts real containers**, is gated behind `KARAR_INTEGRATION=1`, and is deliberately not run by the default local gate; the CI workspace lane now starts Redis alongside PostgreSQL, without which every distributed rate-limiter assertion had been skipping since it was written |
| KAR-CTL-106 | A package for a deployed environment **refuses to exist** rather than shipping misconfigured: on both platforms the build fails at configuration time for any environment other than LOCAL unless given an HTTPS endpoint carrying no credentials whose host is not loopback, a private alias, IPv6 loopback in either notation, or a `.local`/`.localhost`/`.internal`/`.test` suffix; **absence of an environment or an endpoint is the failure, never the exemption**; and on iOS the check is the Runner target's last build phase, running after the plist is written and **before code signing** | IMPLEMENTED [P4] | Security Owner | CC6.1, CC6.6, CC8.1 | 8.9, 8.31, 8.32 | EV-448, EV-452 | 4 | `apps/mobile/android/app/build.gradle.kts`; `apps/mobile/ios/Scripts/verify_packaged_bundle.sh`; `apps/mobile/test/security/build_environment_guard_test.dart`. **Proven by execution, not by presence:** CI drives real Gradle through **nine refusal cases and one acceptance**, each refusal asserted **for its stated reason**. Three defects the phase found are why that matters — the guard originally ran only when dart-defines existed, making a direct Gradle invocation a total bypass; host extraction by `substringBefore(':')` made every IPv6 rule **unreachable code that a source-presence test reported as present**; and iOS had **no build-time guard at all** while Android refused the identical build. Carried openly: **`localhost.`, the expanded IPv6 loopback, `0.0.0.0`, and the `.internal` and `.test` suffixes still pass the Dart runtime configuration loader** — the two layers now disagree and the narrower one is the one that runs on a device (KAR-RSK-038) |
| KAR-CTL-107 | Android backup and device-to-device transfer are closed by **declaration of every mode**, not by one attribute: `allowBackup="false"` disables cloud backup only at targetSdk 31 and above and this build targets 36, and a backup mode whose section is **missing** from the rules resource is *fully enabled* rather than off — so all three manifest attributes are set and the data-extraction rules declare **every** mode and exclude **every** documented domain from each, with `<cross-platform-transfer>` carrying the `platform` attribute the resource is invalid without | IMPLEMENTED [P4] | Security Owner | CC6.1, C1.1 | 8.12, 8.13, 8.10 | EV-451 | 4 | Rules generated by `apps/mobile/android/app/build.gradle.kts` and asserted against a real build in `apps/mobile/test/security/platform_hardening_test.dart`. **The resource whose entire purpose is closing the transfer path is what once stopped a shippable artifact existing:** omitting `platform` does not make the section permissive, it makes the resource invalid, and `lintVitalRelease` refuses the assembly — which nothing caught for several commits because a debug assembly parses the same file happily and **the debug APK was the only Android artifact CI produced**. The honest limit of the claim is recorded in the control's own text: session tokens are Keystore-backed so a transferred copy would be ciphertext, but *"the copy that left the device is useless"* is a weaker claim than *"no copy left the device"*, and only the second is a control |
| KAR-CTL-108 | No shipped iOS artifact carries a transport exception: the localhost App Transport Security exception is **added** to a packaged bundle only when the configuration is Debug **and** the compiled environment is LOCAL, after the fragment is validated against an exact expected value; in every other configuration any exception found is **removed and the plist re-read to confirm the removal**, because a rewrite that reported success without taking effect would otherwise ship it; absence of a compiled environment is **refused, never treated as LOCAL**; and on every configuration the build fails if arbitrary loads are permitted in any form | IMPLEMENTED [P4] | Security Owner | CC6.6, CC6.7 | 8.20, 8.21, 8.24 | EV-453 | 4 | `apps/mobile/ios/Scripts/verify_packaged_bundle.sh`, `apps/mobile/ios/Runner/ATSLocalDevelopment.plist`; `apps/mobile/test/security/ios_packaged_bundle_test.dart`. **Every iOS artifact carried this exception until this phase.** The control is add-then-verify rather than strip-and-hope, and CI exercises it across four environments — a LOCAL simulator build whose packaged bundle is verified to **carry** the exception, and DEV, STAGING and PRODUCTION builds verified to carry **none**. Certificate pinning remains deliberately absent (EXC-002, KAR-RSK-013) |
| KAR-CTL-109 | An artifact's identity matches the environment it was compiled for, on both platforms and by one rule: four issued application identifiers per platform with a per-environment suffix, derived where a decoder already exists rather than duplicated, refusing any `PRODUCT_BUNDLE_IDENTIFIER` that is not one of the four, narrowing the packaged plist when the configuration default and the compiled environment disagree, and **re-reading to confirm on every branch**; a build with no compiled environment is **refused rather than defaulted** | IMPLEMENTED [P4] | Engineering Owner | CC6.1, CC8.1 | 8.31, 8.32 | EV-450, EV-452 | 4 | `apps/mobile/ios/Scripts/bundle_identity.sh`; `apps/mobile/android/app/build.gradle.kts` (`applicationIdSuffix`); the identity matrix is in `docs/phases/phase-04.md`. **Every iOS artifact carried the production bundle identifier until this phase**, whatever environment it was compiled for, while Android had derived a per-environment identifier from the start — and PRODUCTION is the one row where the wrong answer and the right answer are the same string, which is why the lane that never built DEV would not have caught it. One consequence is stated plainly: **`flutter run` and Xcode-IDE builds now require `--dart-define=KARAR_ENV`**, because a build told nothing about its environment is refused instead of silently becoming a production-identified artifact. **The direct artifact-to-artifact comparison covers one environment pair in CI, not four** (KAR-RSK-036) |
| KAR-CTL-110 | Mobile dependency pinning is asserted, not reviewed: every direct non-SDK dependency carries an **exact version with no caret and no range** beside a committed lockfile; the pinning suite fails on any inexact constraint, on a nested-block (`git:`, `path:`, `hosted:`) bypass that is not one of the three SDK-sourced entries, on the removal of any of the five security-critical packages named individually, on any `dependency_overrides` section, and on a lockfile that does not record hosted sources with checksums; every Flutter step in both workflows runs `flutter pub get --enforce-lockfile`; and because that flag enforces whatever the lockfile *says*, **host exclusivity and per-package checksums are asserted separately** | IMPLEMENTED [P4] | Engineering Owner | CC7.1, CC8.1 | 5.21, 8.32, 8.8 | EV-455 | 4 | `apps/mobile/test/security/dependency_pinning_test.dart`; `apps/mobile/pubspec.yaml` and its lockfile. The suite opens with a guard that the parser found dependencies at all, **so a parsing regression cannot make the rest vacuous**. Two regressions are why each half exists: `local_auth` had drifted to a caret range with nothing failing, and the lockfile check asserted that pub.dev **appeared** rather than that it was the only host — an audit that redirected one package to a private mirror left the suite green. **One workspace override exists and is pinned exact**: `deepmerge-ts` at `8.0.1` in `pnpm-workspace.yaml` against `GHSA-ggr8-5vv4-36mx` / `CVE-2026-40345`, exact rather than ranged because a range under a security override would let a future lockfile refresh pull an unreviewed release in under the authority of a security fix. Owner Engineering Owner; removal trigger is a Prisma release whose own dependency moves to a patched line, at which point the override must be **removed rather than carried** |
| KAR-CTL-111 | Shipped artifacts are inspected as artifacts, not inferred from source: the merged Android manifest's permission set is compared **exactly** — not as a subset, so a permission *disappearing* is also a finding — against a real build; the built APK's network policy is read with `aapt2` and the unzipped APK scanned for credential material; the packaged iOS bundle's transport posture, effective identifier, and purpose strings are read from the bundle; and the assertions are gated on an environment variable that turns a **missing artifact into a failure rather than a skip** | IMPLEMENTED [P4] | Security Owner | CC7.1, CC8.1 | 8.8, 8.29, 8.32 | EV-449, EV-450, EV-451, EV-452, EV-456 | 4 | `apps/mobile/test/security/platform_hardening_test.dart` (`KARAR_VERIFY_ANDROID_ARTIFACT`), `ios_packaged_bundle_test.dart` (`KARAR_VERIFY_IOS_ARTIFACT`). **Before the gate variable existed the artifact assertions lived on a lane that builds nothing and skipped every time, which is precisely how they were passing.** The fourth permission in the merged manifest — an application-defined, `signature`-level permission contributed by `androidx.core` that restricts androidx's runtime-registered receivers to same-key code — had **never been reviewed** before this phase; it grants no platform capability, and because its name carries the environment's applicationId suffix it is matched by shape rather than by literal. These lanes were **not** required checks when this row was written; they became required at the Phase 4 PR close, which is what moved KAR-CTL-113 to IMPLEMENTED |
| KAR-CTL-112 | User-facing text and interaction sit under mechanical gates rather than review: every message lives in one catalogue per language with **exact parity in both directions**, a translator description on every English message, matching placeholders, and any English plural required to be plural in Arabic with the `few`, `many`, and `other` arms present; text direction is **never set by hand** and the test harness deliberately refuses to accept one, so an RTL tree is proven rather than requested; a source scan requires every numeric generated message to pass through the single formatter and a prose scan forbids a literal Arabic-Indic digit in any message; and WCAG 2.1 contrast is **computed** — 4.5:1 for text, 3:1 for component boundaries — over both palettes, with a 48px tap-target floor expressed as a minimum constraint and asserted at 1.0x and 2.0x text scale | IMPLEMENTED [P4] | Engineering Owner | PI1.1, CC2.3 | 8.25, 8.29 | EV-458 | 4 | `apps/mobile/lib/l10n/arb/`; `apps/mobile/test/l10n/`, `apps/mobile/test/core/architecture/numeral_rules_test.dart`, `apps/mobile/test/shared/design_system/`. **The gates were being evaded by construction rather than by carelessness:** 335 user-facing messages sat in six per-feature Dart catalogues, outside the parity, description, placeholder, and plural gates entirely, and were consolidated into the ARB files. Two live defects came out of that — the Arabic catalogue was shipping **two numeral scripts at once** on one screen, and the product had **two Arabic names**, one of them the legacy system's, on the launcher name, the legal-language notice, and the iOS Face ID prompt. Every interactive control carries a name, a sensitive subtree leaves the semantics tree when the application backgrounds, and errors publish as live regions. Not claimed: **no widget is ever rendered in dark mode** — the dark palette is verified as colour arithmetic and never as a rendered tree |
| KAR-CTL-113 | Mobile changes are merge-blocked by CI: analysis, generated-client drift, the localization gate and the Flutter suite, plus real Android and iOS artifact builds and the assertions read from them, all as **required status checks** | IMPLEMENTED [P4] | Engineering Owner | CC8.1, CC7.1 | 8.32, 8.29 | EV-463 (COLLECTED), EV-464 (COLLECTED) | 4 | The lanes exist and run on every PR — `mobile` (analysis, drift, localization gate, suite), `mobile-android` (debug APK, release assembly, nine Gradle refusal cases, merged-manifest comparison, `aapt2` network policy, APK credential scan, unsigned-artifact assertion), `mobile-ios` (simulator and device builds across four environments with packaged-bundle verification), and `mobile-supply-chain` in the Security workflow. *(As recorded at the gate: only `mobile` was a required check, so the artifact assertions and the mobile secret scan could not stop a merge.)* **2026-08-18, at the Phase 4 PR close: DESIGNED → IMPLEMENTED.** The row's own statement names analysis, drift, the localization gate and the Flutter suite plus real Android and iOS artifact builds as required status checks. `mobile-android` and `mobile-ios` were added to branch protection once both had run to success on the PR head, taking the required set from 8 to 10 with every prior check preserved and admin enforcement, force-push and deletion settings re-read afterwards and unchanged. `mobile-supply-chain` was added immediately afterwards, once it had run to success on two heads, taking the required set to **11**; the gap this row previously recorded is closed. The residual is narrower rather than absent: the runs establishing the lanes pass predate the protection change, so the first head at which all eleven gate is the next one. |
| KAR-CTL-114 | Signing fails closed on absence: **no signing material exists in this repository**, an Android release signing configuration is created only when all four pieces of material are present so an incomplete release emits an **unsigned artifact rather than falling back to the debug key**, iOS builds run without code signing, and the packaged-bundle phase **refuses the build** when a development team or provisioning profile is set and identifier narrowing would be required, because naming the owner of an identifier requires a real Apple Team ID this repository does not hold and must not invent | IMPLEMENTED [P4] | Engineering Owner | CC6.1, CC8.1 | 8.31, 8.32, 5.15 | EV-449, EV-452 | 4 | `apps/mobile/android/app/build.gradle.kts` (`signingConfigs`, no debug fallback); `apps/mobile/ios/Scripts/verify_packaged_bundle.sh`. CI asserts the release artifact is **unsigned rather than debug-signed**, because no signing material exists here and a signature would mean the debug key had been substituted; the **absence** of an iOS development team and provisioning profile is itself asserted by test. **This control governs refusal, not signing.** Custody, approval, and provenance of an actual signed build are KAR-CTL-115 and do not exist |
| KAR-CTL-115 | Release signing is governed: signing keys are custodied per the key-management rules, a named role approves each store submission, the identity that signs is separated from the identity that authors the change, and every published build is traceable to a reviewed commit and a recorded approval | DEFERRED | Platform Owner | CC6.1, CC6.7, CC8.1 | 8.24, 5.15, 8.32 | none yet | 20 | No signing material, no Apple Team ID, no store account, and no published build exist, so there is nothing to custody and nothing to approve — the mechanism that holds meanwhile is KAR-CTL-114's refusal. Activates before any signed device build is produced, and the separation leg is bound to the same trigger the [control-owners](control-owners.md) table already carries for production key operations (EXC-001, KAR-RSK-032) |
| KAR-CTL-116 | Register traceability is enforced mechanically: every EV, KAR-CTL, and KAR-RSK id cited anywhere in `docs/` resolves to a defining row in its register, every repository path cited by an evidence row exists on disk, and every quoted evidence status and control status matches its authoritative register — as a **merge-blocking check**, not a sweep somebody remembers to run | DESIGNED | Compliance Owner | CC2.2, CC4.1 | 5.36, 5.37 | in-repo: hand-run sweep recorded in [phase-compliance-gate.md](phase-compliance-gate.md) (Phase 3.5 record, item 18) | 4 | **The sweep works and is not automated, which is the whole gap.** It was run by hand at the Phase 3.5 gate with zero forks across 76 EV, 93 KAR-CTL, and 30 KAR-RSK ids, and that gate recorded the corrective action to graduate it into the architecture suite — owner Engineering Owner, **target Phase 4** — precisely because a hand-run sweep had by then been the fix for the same class of defect twice. **It was not added at Phase 4**: architecture test 26 still covers assurance-claim traceability only, and the runner stands at the same 24 active tests. Recorded as a slipped corrective action rather than absorbed; the continual-improvement entry (CI-006) is the Compliance Owner's to update |

## Tally (v0.6)

Re-derived at Phase 4 by counting the rows above, not by carrying the previous version's figures forward.

| Status | Count |
|---|---|
| DESIGNED | 17 (16 carried from v0.5, plus KAR-CTL-116 — DESIGNED on the same reading that keeps KAR-CTL-025 there: a status is honest to its own control statement, and both statements require a merge-blocking mechanism that does not block) |
| IMPLEMENTED | 83 (6 [C1]-contingent, 15 [P2]-contingent, 16 [P3]-contingent, 15 [P3.5]-contingent, and **21 [P4]-contingent**: KAR-CTL-094–112, 113 and 114. **The [P4] marker is unresolved** — no Phase 4 gate has executed and no CI run exists against a Phase 4 head, so any [P4] control whose named check did not run reverts to DESIGNED at the gate) |
| DEFERRED | 15 (14 carried, plus KAR-CTL-115 release-signing governance at Phase 20) |
| EXCEPTION | 1 |
| OPERATING / EVIDENCED / NOT_APPLICABLE | 0 |

116 controls (18 + 82 + 15 + 1 = 116). Changes to this matrix go through PR review like any other change; the status column is updated only with a pointer to what changed (a CI run, a settings export, a review record).
