# Risk Register

**Status:** ACTIVE register · **Owner:** Security Owner · **Version:** 0.7 · **Date:** 2026-08-22 · **Review:** every phase gate (EV-008)

**v0.7 (2026-08-22, Phase 5 review and gate; extended the same day after the independent reviews):** **KAR-RSK-042–050 added** — 042–045 from the closeout verification and **046–049 from the two independent review passes**: a refused request that performs 22 database round trips first, a rate-limit window pruned with the application's clock, a source-store AAD that binds the subject and nothing else, and a client that cannot record a second identical purchase. Originally — the client that has never been seen to reach a screen, the picker nobody can reach on any device today, verification controls that could pass without verifying, and a CI lane that stayed broken because no pull request was open to run it. **No row was closed.** **031, 032 and 033 (the device gap) were re-examined and HELD, with 042 recording that the gap now has a demonstrated consequence rather than only an absence of proof.** **KAR-RSK-011 is recorded as OVERDUE** — its protected evidence store passed its Phase 4 deadline undischarged and is carried here as a slipped treatment with its owner and reason, not re-dated to a later phase. Nothing became OPERATING or EVIDENCED; nothing is deployed.

**v0.6 (2026-08-18, Phase 4):** **KAR-RSK-031–041 added** with the Phase 4 client, artifact, and build surface. **One row re-scored: KAR-RSK-013 no certificate pinning, 6 Moderate → 8 Moderate** — Phase 4 is the revisit point EXC-002 named, and a client that now holds real credentials on a real device raises the impact of an intercepting proxy from 3 to 4 while the likelihood stays 2. **No risk closed.** Notes dated on KAR-RSK-001, 002, 003, 004, 010, 011, 016, 020, and 027 where Phase 4 changed their facts. **Phase 4 was IN PROGRESS when this version was written** — no gate had executed, so this is the phase review rather than the gate record. The gate has since executed (PASS_WITH_DOCUMENTED_DEFERRED_ITEMS) and Phase 4 is merged; the gate-record half remains as recorded there.

**Three re-score commitments falling due at Phase 4 were discharged by re-examination rather than by movement, and each says so in its own row.** KAR-RSK-016 (refresh replay) named "client-side token custody rules land with the Flutter network layer (Phase 4)" — they landed, and the theft window before a second presentation is untouched, so the score holds. KAR-RSK-027 (binding/rotation abuse) named "re-score at 4 with a real client" — the client arrived and neither widens nor narrows a server-side race, so the score holds. KAR-RSK-013 named a Phase 4 revisit — that one **did** move. A commitment to re-score is discharged by recording the re-examination and its outcome; leaving it unmentioned is how a register goes quiet.

**v0.5 (2026-08-16, Phase 3.5 close):** the gate executed and **confirmed KAR-RSK-021's closure** rather than reopening it (gate record v0.6). No row was opened, closed, or re-scored at the gate itself. **Phase 3.5 is COMPLETE; Phase 4 is NOT STARTED.**

**v0.4 (2026-08-16, Phase 3.5 review):** KAR-RSK-021 **CLOSED** on the delivered session-binding mechanism with its adversarial and concurrency tests — its own stated closure condition (EV-411, EV-412, EV-420; KAR-CTL-091). KAR-RSK-022–030 added with the Phase 3.5 mechanisms. Notes dated on KAR-RSK-010, 016, 017, and 020 where Phase 3.5 changed their facts. No re-score.

**v0.3 (2026-08-16, Phase 3 review):** KAR-RSK-016–020 added with the Phase 3 mechanisms; KAR-RSK-009 note dated (the `EncryptionProvider` seam is now exercised by MFA secrets under the local-only dev provider). No re-score, no risk closed.

**v0.2 (2026-08-15, Phase 2 review):** KAR-RSK-003 note revised (signals exist, alerting does not); KAR-RSK-009 and 010 updated with Phase 2 deltas (010 re-scored 12 → 9); KAR-RSK-014 and 015 added. No risk closed.

Scored per [risk-methodology.md](risk-methodology.md) — residual, with current gates and compensating controls in place, over a horizon of the next two phases. Treatments and target phases are expanded in the [treatment plan](treatment-plan.md).

---

## Open risks

| ID | Risk | Category | L | I | Score | Treatment | Owner | Target phase | Status |
|---|---|---|---|---|---|---|---|---|---|
| KAR-RSK-001 | **Key-person concentration.** One person holds every role, credential, and unwritten context; incapacity stalls the project and could strand accounts and evidence | Governance | 3 | 5 | 15 High | MITIGATE — documentation-first discipline (everything canonical in-repo), credential recovery paths verified, role structure ready for handover; residual ACCEPTED until team growth. 2026-08-18 (Phase 4): re-read, unchanged, and one concentration point grew — **the mobile signing material and store accounts that do not exist yet will be held by the same person**, and the roadmap gate that separates production key operations from change authorship is the only structural answer recorded for it (KAR-CTL-115). Phase 4's eleven parallel agent workstreams are a technical division of labour directed by one maintainer and change nothing here (EXC-001) | Platform Owner | SoD trigger (≥3 eng) | OPEN |
| KAR-RSK-002 | **No independent review of security-relevant changes.** Author and approver are the same person; a reasoning error survives to merge | Change management | 4 | 3 | 12 High | MITIGATE — EXC-001 compensating controls: merge-blocking CI, architecture tests, independent-reviewer-agent step in the phase process. 2026-08-18 (Phase 4): re-read, unchanged, and the mechanical half of the compensation **thinned relative to the new surface** rather than holding steady — three of the five CI lanes this phase added are not required checks, so the Android release assembly, the merged-manifest comparison, the APK credential scan, the packaged-bundle assertions, and the mobile secret scan cannot block a merge (KAR-CTL-113, KAR-RSK-037). The architecture runner does not reach the Dart client at all (KAR-CTL-094). **The phase's own record is the strongest argument for this row:** three defects passed every static form of review it ran and were found only by assembling the release, pressing the control, and asserting exclusivity | Engineering Owner | Team ≥ 2 | OPEN |
| KAR-RSK-003 | **No alerting or on-call pre-production.** Defects, misconfigurations, or CI compromise would be discovered by inspection, not by anyone being told | Operations | 3 | 3 | 9 Moderate | ACCEPT until runtime exists. 2026-08-15 (Phase 2): the detection *substrate* now exists — structured classification-aware logs/traces/metrics, truthful `/readyz`, outbox lag and DLQ metrics (KAR-CTL-058, 062, 063) — but it runs only in local/test execution and **nothing watches or pages**; the risk is unchanged in substance and stays OPEN. Alert routing and on-call remain KAR-CTL-041, Phase 20 gate. 2026-08-18 (Phase 4): the readiness surface got **more truthful and no more watched** — `/readyz` now probes the rate-limit store with a real round trip after a defect in which readiness reported ready while every fail-closed identity policy answered 503 (KAR-CTL-105). A probe nobody polls is a signal nobody receives; score and status unchanged | Operations Owner | 17–20 | OPEN |
| KAR-RSK-004 | **Supply-chain compromise** via npm, pub.dev, Docker Hub, or a base image — malicious package, typosquat, or poisoned update entering the toolchain | Supply chain | 3 | 4 | 12 High | MITIGATE — lockfile pinning, reviewed updates, SCA, SBOM, secret-scanned CI (KAR-CTL-025–028); digest pinning to verify at Phase 1 gate. 2026-08-18 (Phase 4): **the surface and the controls both grew, and the row holds at 12 because they roughly cancel.** A second package ecosystem entered scope with the Flutter client, and pub advisories have no baseline here so the mobile supply-chain lane is report-only and additionally does not gate a merge. Against that: exact pins with no ranges, `--enforce-lockfile` in both workflows, **host exclusivity** (pub.dev and only pub.dev) and per-package checksums asserted separately, and a pinning suite that fails on nested-block bypasses and on the removal of any named security-critical package (KAR-CTL-110; EV-455). Two real regressions motivated each half — `local_auth` had drifted to a caret range with nothing failing, and the lockfile check proved a host *appeared* rather than that it was the only one, so an audit redirecting one package to a private mirror left the suite green. One advisory was acted on rather than reported: `deepmerge-ts` pinned exact at 8.0.1 against `GHSA-ggr8-5vv4-36mx` / `CVE-2026-40345` | Security Owner | 1 ongoing | OPEN |
| KAR-RSK-005 | **Secret leakage via CI misconfiguration** — over-scoped tokens, secrets echoed into logs, or workflow injection from untrusted input | CI / secrets | 3 | 4 | 12 High | MITIGATE — least-privilege CI credentials (KAR-CTL-009), secret scanning (026), secrets-handling rules (036); workflow review at Phase 1 gate. 2026-08-18 (Phase 4): five CI jobs and one Security job were added and re-read against these rules — **no workflow references a repository secret**, the mobile supply-chain job runs its secret scan **before Flutter is installed** so a scan cannot be affected by what a package resolution pulled in, and the SBOM job keeps dependency-snapshot submission off so the workflow token stays read-only. **One pin was wrong, and finding it took a sweep rather than a reading:** `actions/download-artifact` was SHA-pinned to a commit whose comment claimed `v6.0.0`, and that commit is a README merge rather than the release — corrected in `be0d35a` after all fourteen third-party pins were resolved against the tags they name, thirteen of which matched (EV-466). Score unchanged; the sweep is now a per-gate check rather than a one-off | Security Owner | 1 ongoing | OPEN |
| KAR-RSK-006 | **Data residency undetermined.** Where customer data may lawfully live and flow is an open legal question; a late adverse answer reshapes architecture and vendors | Legal / regulatory | 3 | 4 | 12 High | MITIGATE — provider-portable design (DeploymentProfile, jurisdiction-scoped KEKs) keeps answers cheap; legal opinion is a Phase 20 gate; no residency claim is made (AC-015) | Platform Owner (legal counsel pending) | 20 | OPEN |
| KAR-RSK-007 | **Zakat Sharia review absent.** No Sharia scholar has reviewed the Zakat methodology; outputs could be religiously incorrect or be mistaken for a fatwa | Legal / Sharia | 2 | 4 | 8 Moderate | MITIGATE — external review gates Zakat launch (roadmap non-engineering gate); outputs never represented as a fatwa (AC-010) | Platform Owner (external reviewer pending) | 9 launch gate | OPEN |
| KAR-RSK-008 | **Amanat legal clearance absent.** The sealed-obligation product has no per-jurisdiction legal clearance; building it before clearance risks rework or non-launch | Legal | 2 | 5 | 10 High | MITIGATE — Phase 14 gated on clearance; ships `PENDING_LEGAL_REVIEW` until cleared; disclosure-not-access model documented for counsel | Platform Owner (legal counsel pending) | 14 gate | OPEN |
| KAR-RSK-009 | **Sealed-data key custody unimplemented until Phases 13/20.** Between sealed-vault build and custody approval, the ENC-2 failure class (irrecoverable, undetectable key loss) exists as a design risk | Data protection | 2 | 5 | 10 High | MITIGATE — hard gates: approved `KeyCustodyStrategy` and running canary before any production `SEALED` data (KAR-CTL-035, ADR-0017). 2026-08-15 (Phase 2): the rotation, custody, and canary **contracts now exist in code and are pinned by tests** (KAR-CTL-064, 065 — DESIGNED; key-version provenance structural, `KARAR-CANARY-` prefix guarded); implementation and the operating canary remain Phase 13/20, so score and status are unchanged. 2026-08-16 (Phase 3): the `EncryptionProvider` seam is exercised in earnest for the first time — identity MFA secrets encrypt through it with `KeyVersionRef` provenance, under `LocalDevEncryptionProvider`, which refuses to construct outside local (KAR-CTL-069, 078); custody and the operating canary remain Phase 13/20, score and status still unchanged | Security Owner | 13, 20 | OPEN |
| KAR-RSK-010 | **Documentation drift.** The docs corpus is large and precedes the code; published promises can diverge from what gets built — the legacy's most consequential finding class (P1) | Documentation | 3 | 3 | 9 Moderate | MITIGATE — Assurance Claim Registry linkage (KAR-CTL-006), architecture test 26 (ACTIVE since Phase 2), docs checks in CI (023), phase-gate reconciliation. 2026-08-15: re-scored L4 → L3 — the claims-linkage test now runs merge-blocking, so unnoticed divergence needs the linkage to be missing too, not a single oversight. 2026-08-16 (Phase 3.5): test 26 strengthened — a claim's cited repository paths must now **resolve on disk** and its evidence ids must be well formed, so a claim pointing at a deleted or renamed file fails the build instead of reading as evidence (EV-422). Not re-scored: the test still cannot read whether the evidence *supports* the claim, which is the half that produced P1 | Compliance Owner | 2 onward | OPEN |
| KAR-RSK-011 | **GitHub as single point of dependence** — SCM, CI, and interim evidence store in one vendor account; suspension or compromise halts delivery and could orphan evidence | Vendor / availability | 2 | 4 | 8 Moderate | MITIGATE — full-clone recoverability (KAR-CTL-046), per-phase evidence export, evidence-store decision by Phase 2 gate. **2026-08-18 (Phase 4): the evidence-store half of this treatment has now slipped three gates and passed its own final deadline, and that is recorded as a finding rather than as another carry.** The Phase 2 gate deferred it to "before Phase 4 or before any external audit"; the Phase 3.5 gate marked it **now falling due**; Phase 4 is here and no protected store exists. **112 evidence rows across four phases** live in run URLs with finite vendor retention and in-repo references, in one vendor account, with no organizational access control and no reviewer other than the collector — which is also why exactly one row in the register is `REVIEWED` — EV-007, the branch-protection export, whose subject can be re-read from the GitHub API rather than from a document this project wrote — and why no other row can become one under this arrangement. **Not re-scored, deliberately:** the event this row models is vendor loss or compromise, and that is no more likely than it was; what changed is that the mitigation deadline lapsed. Held at 8 Moderate with the lapse carried in the treatment plan as a slipped step (methodology §Process 4). Owner: Compliance Owner | Operations Owner | 2 | OPEN |
| KAR-RSK-012 | **Accidental content in git history.** Files not meant for the repository can be committed and persist in history; one benign incident predates Phase 1 (`Archive.zip`, present in git history at commit `cc9b0d7`; untracked and git-ignored as of Phase 1) | Data hygiene | 3 | 3 | 9 Moderate | MITIGATE — secret scanning (026), synthetic-data rule (038), `.gitignore` discipline, repo-hygiene check at each phase gate; disposition recorded at Phase 1 close: history deliberately not rewritten, file untracked and ignored | Engineering Owner | 1 close | OPEN |
| KAR-RSK-013 | **No certificate pinning in v1.** Mobile clients rely on platform trust stores; a compromised CA or intercepting proxy is not additionally defended against | Mobile security | 2 | 4 | 8 Moderate | ACCEPT — carried from threat model §4 (challenge C11); recorded as EXC-002; revisit at Phase 4 and again pre-launch. **2026-08-18 (Phase 4): revisited on schedule — this row's own Phase 4 trigger — and re-scored I 3 → 4, 6 → 8, with the acceptance standing.** The reason for the move is that the thing being protected changed: until this phase there was no client, no credential on any device, and nothing for an intercepting proxy to sit in front of. There is now a client that holds a real refresh token in platform-backed storage and exchanges it over a transport, so interception costs a session rather than a request. **The likelihood does not move and the reason it does not is worth stating: no endpoint exists for any environment**, so the only packages this repository can build are LOCAL ones and there is no traffic to intercept — L is held at 2 and is re-examined the moment a DEV endpoint exists. Compensating controls refreshed and now code rather than design: one secure entry with `first_unlock_this_device` on iOS and macOS and reset-on-error on Android (KAR-CTL-097), single-flight refresh over a separate raw transport with terminal failures wiping the credential (KAR-CTL-099), URIs logged with every query value replaced and DTOs printing type names only (KAR-CTL-094), and no shipped artifact carrying a transport exception with arbitrary loads failing the build in any configuration (KAR-CTL-108). Pinning's own failure mode — bricked clients on rotation — is unchanged and is still the reason for the acceptance | Platform Owner | revisit 4 (done); 20 | ACCEPTED |
| KAR-RSK-014 | **Queue and backlog resource exhaustion.** A hostile or defective producer floods the outbox or job queue, exhausting storage, connections, or worker capacity (added 2026-08-15, with the queues themselves) | Operations | 2 | 3 | 6 Moderate | MITIGATE — largely delivered with the mechanism: bounded retries with backoff then dead-letter, job payload-size cap, single-claim `SKIP LOCKED` leases, stale-claim recovery, paged scans (KAR-CTL-058, 059); residual is a poison generator filling the DLQ *inside* limits, whose mitigation is alerting on DLQ depth/backlog — which does not exist yet (KAR-RSK-003) | Engineering Owner | 2 (caps); 20 (alerting) | OPEN |
| KAR-RSK-015 | **Schema/migration drift.** An environment's schema silently diverges from the migration history — edited applied files, out-of-band DDL, or partial application (added 2026-08-15, with the migration runner) | Data platform | 2 | 3 | 6 Moderate | MITIGATE — largely delivered with the mechanism: sha256 checksums with drift detection hard-failing `migrate`/`verify`, forward-only history, DDL possible only as `karar_migrator`, from-zero rebuild proven (KAR-CTL-053, 054); residual is drift introduced with migrator-level access between verify runs — review owns that, and `/readyz` reports migration status (KAR-CTL-062) | Engineering Owner | 2 ongoing | OPEN |
| KAR-RSK-016 | **Refresh-token theft and replay.** A stolen refresh token grants session continuation until rotation or expiry; reuse detection fires on the second presentation, not the first (added 2026-08-16, with the session mechanism) | Identity / access | 2 | 4 | 8 Moderate | MITIGATE — largely delivered with the mechanism: one-time atomic rotation, token families, reuse detection revoking the family and its session and recording the event, sha256-only storage (KAR-CTL-068; EV-303); residual is the window before a second presentation — client-side token custody rules land with the Flutter network layer (Phase 4), reuse-signal alerting with monitoring (Phase 20, KAR-RSK-003) . 2026-08-16 (Phase 3.5): a tenant switch now revokes the session **and its refresh families** atomically before issuing the replacement, so a stolen refresh token cannot survive a switch pointing at the previous tenant (KAR-CTL-091; EV-420). Score unchanged — the theft window before any switch is untouched. **2026-08-18 (Phase 4): the client-side custody rules this row named for Phase 4 landed, and the score still does not move.** Delivered: one namespaced secure entry with platform options chosen rather than defaulted, an unreadable store and a genuine absence kept as different values, only key names reaching logs (KAR-CTL-097); single-flight refresh in which concurrent callers share one in-flight call, an already-replaced token issues nothing, an expired chain ends the session with **no network call at all**, and the refresh goes over a separate raw transport with no coordinator or retry policy so a storm is structurally impossible (KAR-CTL-099; EV-437, EV-440). **What none of that touches is the risk:** a token stolen from a compromised device is still valid until it is rotated or reused, and reuse detection still fires on the second presentation. Re-score commitment discharged by re-examination; alerting on reuse signals remains Phase 20 | Security Owner | 3 (mechanism); 4, 20 (residual) | OPEN |
| KAR-RSK-017 | **Cross-tenant data exposure.** A missing filter, forged tenant claim, or leaked pooled-connection context reads or writes another tenant's rows — the platform's core isolation promise (added 2026-08-16, with tenancy) | Data protection | 2 | 5 | 10 High | MITIGATE — RLS ENABLE+FORCE failing closed on unset GUCs, transaction-local principal context bound from server-side records, architecture tests 9/22 merge-blocking, adversarial suites asserting on non-empty two-tenant data including pooled-connection hygiene on both sanctioned pools (KAR-CTL-011, 071; EV-307, 308, 316); residual: RLS is per-row, not per-column, and allow-listed tables rest on compensating grants reviewed per table. 2026-08-16 (Phase 3.5): the surface grew to **48 tables (22 ENABLE+FORCE, 33 allow-listed, 7 both)** and test 22 gained anti-boilerplate rules forcing each allow-list reason to state why no tenant or subject predicate fits and to describe its actual compensating controls; the session binding is a **new source of tenant context**, and its own residual is carried separately as KAR-RSK-030 (EV-421, 423). Score unchanged | Engineering Owner | 3 ongoing; re-score 17+ | OPEN |
| KAR-RSK-018 | **Privileged-role escalation.** A compromised code path or insider grants PLATFORM_ADMIN or scatters role assignments beyond intent (added 2026-08-16, with RBAC) | Access control | 2 | 4 | 8 Moderate | MITIGATE — deny-by-default closed catalogue with no wildcards structurally, PLATFORM_ADMIN-only assignment with the peer-delegation rule, assignment writes RLS-bound to the target principal, revoked assignments immutable, grants and revocations audited with actor and reason (KAR-CTL-072, 073; EV-309); no HTTP surface for role administration exists until the control plane (ADR-0021), so abuse currently requires code execution | Security Owner | 3 ongoing; control-plane phase | OPEN |
| KAR-RSK-019 | **Kill-switch guards unmounted until phase integration.** The guard mechanism exists and is tested, but until the lead's composition wiring mounts it on registration/login/refresh/invitation routes, an activated switch restricts nothing at the HTTP layer (added 2026-08-16, with the mechanism) | Operations | 2 | 2 | 4 Low | MITIGATE — temporary by construction: the lead wires the guards during phase integration, before the Phase 3 PR; the Phase 3 gate verifies the mounting and closes this row on that verification (gate checklist item 3); exposure meanwhile is nil in substance because nothing is deployed anywhere (KAR-CTL-075; EV-310). Mounting LANDED 2026-08-16 before the PR: consumer-declared gates on register/login/refresh and invitation issue/redeem, pinned by mount tests and the composed error-boundary test (`apps/api/src/errors/error-boundary.composed.test.ts`); only the gate verification remains. Gate verification EXECUTED 2026-08-16: mounts proven in the closing clean-clone run — row closed per its own plan (gate record v0.4) | Engineering Owner | 3 close | CLOSED (2026-08-16, Phase 3 gate) |
| KAR-RSK-020 | **Fail-closed dependencies deny service.** Deliberate deny-over-silently-open choices concentrate availability cost: kill-switch store outage 503s guarded operations; rate-limit store outage 503s login/verification/reset/MFA (refresh alone fails open to a bounded in-process fallback); local-only mail/encryption providers refuse to construct outside `KARAR_ENV=local`, so flows needing them cannot run outside local until real providers land (added 2026-08-16) | Operations | 3 | 2 | 6 Moderate | ACCEPT (by design) — each failure mode is declared per policy in code, not incidental (KAR-CTL-070, 075, 078; EV-310, 313); revisit when real environments exist: mail/KMS provider selection at Phase 17+, outage alerting with monitoring at Phase 20 (KAR-RSK-003). 2026-08-16 (Phase 3.5): the pattern extends to policy and capability resolution — an availability, entitlement, or policy store outage **denies** rather than defaulting, and the only shipped provider source is `NoProvidersConfigured`, so every provider-requiring capability resolves `PENDING_PROVIDER` (KAR-CTL-087). Score unchanged, and the operational cost is nil in substance while nothing is deployed and no capability is available. **2026-08-18 (Phase 4): this risk materialized for the first time, from a defect rather than from an outage, and the shape of it is worth keeping.** The rate-limit store was never connected before the server began listening; the client is lazy and holds no offline queue, so the first command after boot lost the handshake and **every policy declared fail-closed — login, verification, password reset, MFA, invitations — answered 503**, while readiness reported ready. The declared behaviour was correct and fired on the wrong cause. Fixed by connecting during composition before `listen()` and by making readiness probe the store with a real round trip (KAR-CTL-105; EV-434). Score unchanged: the lesson is that a fail-closed dependency needs its **connection** proven at startup, not only its outage behaviour proven in a test | Operations Owner | revisit 17+, 20 | OPEN |
| KAR-RSK-021 | **Tenant-bound API surface dormant pending session tenant binding.** Sessions are issued with `tenant_binding = null` and no binding mechanism exists in Phase 3, so the ten tenant-bound endpoints (users, tenancy except redemption, consent) answer 401 for every caller while their contracts are published (added 2026-08-16, from independent review) | Delivery | 3 | 1 | 3 Low | MITIGATE — fail-closed by construction (an unbound session reaches nothing tenant-scoped); the dormancy is documented in phase-03 Known limitations, both MODULE.md files, and the OpenAPI description so no consumer is misled; the binding mechanism (bind-at-login or explicit tenant selection) is Phase 3.5 scope; closure = mechanism implemented with adversarial and concurrency tests, row closed at the Phase 3.5 gate. **Closure condition met 2026-08-16:** the binding mechanism landed as KAR-CTL-091 — `BindSessionTenant` (first bind, no rotation) and `RebindSessionTenant` (switch with full session and refresh-family rotation), binding sourced from a server-side membership read and never from the request, verify → act → re-verify → compensate on the switch, migrations 0080/0081 adding read-only self and member SELECT arms so an unbound principal can list only their own memberships, and the first-party tenant resolved from typed configuration — with the adversarial and concurrency tests the closure condition named (EV-411 binding authorization, EV-420 rotation and concurrency, EV-412 membership outliving the binding). The ten endpoints are reachable once a session carries a binding; **no route contract changed to make that true**. The residual the mechanism does not remove is carried forward as its own row, KAR-RSK-030. Closure recorded on the executed verification; the Phase 3.5 gate confirms it or reopens this row with a reason and an owner | Engineering Owner | 3.5 | **CLOSED (2026-08-16, Phase 3.5)** |
| KAR-RSK-022 | **Jurisdiction misassignment, or an unverified assignment treated as verified.** Policy resolution keys on jurisdiction; a wrong or over-trusted assignment applies the wrong legal ceiling to a real principal. `VERIFIED` currently means *an operator or provider asserted it* — no identity-verification provider exists (added 2026-08-16, with the jurisdiction model) | Legal / regulatory | 2 | 4 | 8 Moderate | MITIGATE — source and verification are separate typed axes with `USER_DECLARED` CHECK-bound to `UNVERIFIED`, resolution exposes the verification state rather than collapsing it, `NONE` and `UNVERIFIED` deny alike, assignments are end-only history immutable even for the owner, and every assignment and ending is audited (KAR-CTL-079; EV-401). Residual: the strength behind the word `VERIFIED` is a Phase 8 control-plane question, and no operator surface exists to make an assignment today — the permission is unseeded | Privacy Owner | 8 (verification strength); re-score when a provider exists | OPEN |
| KAR-RSK-023 | **A `DRAFT` or unapproved PolicyPack governing a non-local environment.** An unapproved pack in force would let engineering-authored assumptions stand in for legal decisions — precisely the failure the pack machinery exists to prevent (added 2026-08-16, with PolicyPacks) | Legal / regulatory | 1 | 5 | 5 Moderate | MITIGATE — `canActivate` refuses a non-`APPROVED` pack outside `local` and refuses `APPROVED`-without-`approvalReference` everywhere, evaluated before any write; the activation use case validates structurally first and leaves the ledger at zero rows on refusal; the ledger is append-only and stores metadata only; packs are reviewed code, not rows (KAR-CTL-080, 081, 082; EV-402, 403, 415, 416). Residual, stated plainly: **the predicate reads the process's own environment identity, so a deployment mislabelled `local` would defeat it** — bounded today by boot-time typed configuration and by there being no deployment at all. Likelihood is 1 because no non-local environment exists to reach | Compliance Owner | 17+ (first non-local environment) | OPEN |
| KAR-RSK-024 | **A capability exposed above its ceiling by configuration.** A database row, setting, or entitlement used to make code available that is not implemented, not deployed, or not cleared by policy (added 2026-08-16, with the capability registry) | Access control | 1 | 5 | 5 Moderate | MITIGATE — the compile-time registry is the ceiling and changes only by reviewed code; gates 1–4 consume no grant-like input and run before any grant; jurisdiction settings have no column able to express an enablement; the write path refuses to record an allowing state above the descriptor ceiling and audits the attempt as `DENIED`; monotonicity is proven as a property over ~2,400 exhaustive plus 2,000 seeded randomized configurations (KAR-CTL-085, 086, 087; EV-404, 405, 417). Residual: **the shape argument holds only while the tables keep that shape** — a future column with enabling semantics would defeat it silently, so `jurisdiction_settings` is named explicitly in the Phase 8 review that would open its write path | Platform Owner | 8 (settings write path); ongoing per schema change | OPEN |
| KAR-RSK-025 | **Entitlement or availability misconfiguration.** A forged, stale, or wrongly-windowed row grants or withholds a capability against intent; temporal validation rests on the instant injected into the use case (added 2026-08-16, with entitlements) | Access control | 2 | 3 | 6 Moderate | MITIGATE — RLS ENABLE+FORCE on the tenant GUC, a `SECURITY DEFINER` trigger-written append-only history with `UNIQUE (row_id, version)`, revocation as a status never a deletion, `version` incrementing by exactly one per UPDATE, and windows validated temporally at read time so a lapsed window denies while the row still reads `ACTIVE`; both tables ship with **no rows**, and the managing permissions are unseeded (KAR-CTL-088, 093; EV-407). Residual: a wrong host clock moves every window together (the same residual as the Phase 2 clock note), and misconfiguration in the deny direction shows up as unavailability that nothing currently watches (KAR-RSK-003) | Engineering Owner | 8 (operator surface); 20 (alerting) | OPEN |
| KAR-RSK-026 | **Subject-policy content disclosure.** A subject's elected policy option is a sensitive personal fact (a Zakat methodology election, for example, can reveal religious observance); a cross-principal read, an audit metadata leak, or a published event would disclose it (added 2026-08-16, with `SubjectPolicySelection`) | Data protection | 2 | 4 | 8 Moderate | MITIGATE — the table is CONFIDENTIAL, RLS ENABLE+FORCEd on **both** principal GUCs, immutable with supersession, audited by reference and pin only (never option content), publishes no events **by rule**, exposes no HTTP, and grants no staff, support, tooling, or analytics read path; a leak-regression suite drives the audit boundary (KAR-CTL-090; EV-409, 410). Residual: this table's isolation says nothing about the **content** the owning capability stores elsewhere — that protection arrives with the capability that owns it, and any future reader must arrive as its own audited, purpose-limited surface | Privacy Owner | 9 (first owning capability) | OPEN |
| KAR-RSK-027 | **Tenant-binding or session-rotation abuse.** A caller induces a binding to a tenant they are not a member of, or exploits the switch path's rotation to strand or duplicate authority (added 2026-08-16, with session binding) | Identity / access | 2 | 4 | 8 Moderate | MITIGATE — binding moves null → value only from a server-side membership read, never from request input; membership is re-verified at bind time; a switch atomically revokes the old session and its refresh family before issuing the new one, so no token survives pointing at the previous tenant; arbitrary, revoked, expired-window, and disabled-tenant targets are denied uniformly without reaching the rebind seam; the concurrent-revocation race revokes the replacement session; every denial is audited (KAR-CTL-091; EV-411, 420). Residual: the switch narrows a race window rather than eliminating it, and the auto-bind side effect on `GET /platform/bootstrap` is a declared contract fact rather than a hidden one. **2026-08-18 (Phase 4): re-scored as this row required, and the outcome is that it holds at 8.** The real client arrived and consumes the binding surface **without re-deriving any of its authority** — it presents choices and sends a selection, never asserts a tenant, takes its binding from the server's own membership read, and explicitly invalidates the tenant-scoped providers on a switch so a screen cannot render the previous tenant's data after the session rotated (KAR-CTL-101; EV-443). A well-behaved client neither widens nor narrows a server-side race, and the residual stays exactly where it was. Re-score commitment discharged by re-examination | Security Owner | 3.5 (mechanism); re-score at 4 with a real client | OPEN |
| KAR-RSK-028 | **Bootstrap over-disclosure.** The one authenticated client surface aggregates identity, binding, jurisdiction, entity, policy, and capability state; a field added carelessly leaks a hidden capability's existence, licence internals, pack content, or raw consent evidence (added 2026-08-16, with the bootstrap surface) | Data protection | 2 | 3 | 6 Moderate | MITIGATE — the response is a **closed field set picked name by name**, not a filtered internal object; the surface consumes the single client-safe capability projection unenriched so the filter cannot drift into two implementations; hidden capabilities are omitted in every state and non-actionable denial reasons are omitted entirely; enrichment ports return explicit nulls rather than fabricated defaults; a leak-regression suite drives hostile ports that attach extra fields at the edge (KAR-CTL-089, 092; EV-408). Residual: **omission hides the reason, not that a request took a different path** — response-timing equivalence for hidden capabilities is deliberately not asserted this phase, and the risk re-scores when a real client consumes the surface at Phase 4 | Security Owner | 4 (client); T9 timing discipline deferred | OPEN |
| KAR-RSK-029 | **The pin creation-cutoff CHECK keys on a caller-settable `created_at`.** Migration `0086`'s cutoff arm refuses the historical `PRE_*` pin states for rows created from Phase 3.5 on, but the writing transaction supplies `created_at`, so a caller passing a pre-cutoff timestamp could record a legal-consequence row claiming to predate the machinery (recorded honestly by the security workstream, 2026-08-16; carried here unchanged) | Data protection | 2 | 3 | 6 Moderate | MITIGATE — compensating controls are the guard triggers covering the pin columns (a pin cannot be rewritten once written), the absent DELETE grant, and the writing use cases taking the version as a **required input** so a legitimate path cannot omit it; architecture test 21 blocks the merge on any missing or unjustified pin (KAR-CTL-084; EV-414). Residual accepted for now: closing it needs a database-supplied creation timestamp independent of the caller — the `recorded_at` witness pattern the audit store already uses (KAR-CTL-056) — which is a schema change for a later phase, not a Phase 3.5 patch. **Also open and recorded rather than answered:** `subject_policy_selections` carries no operating-entity pin, so whether an election survives an entity migration is a question for the owning module | Privacy Owner | later phase (independent creation witness); 9 (entity-migration question) | OPEN |
| KAR-RSK-030 | **A bound session outliving the membership that justified it.** Once a transaction binds `app.tenant_id` from a session row, RLS shows that tenant's rows for the life of the transaction — **RLS cannot re-check membership** — so the window between a membership revocation and the next resolution is real (added 2026-08-16, with session binding; the residual KAR-RSK-021's closure does not remove) | Data protection | 2 | 4 | 8 Moderate | MITIGATE — membership is re-verified on **every** bootstrap read and the session is revoked when it has gone; a bound session whose tenant is no longer a usable choice reports the need to reselect rather than a working tenant; membership removal is a status transition the roster policy reads, not a deleted row; the adversarial suite proves the raw data-reach fact directly rather than asserting the absence of one (KAR-CTL-091; EV-412, 421). **Proven residual, stated rather than mitigated away:** the window is bounded by re-verification frequency and by revocation on detection, not by the database. Shortening it further means shorter sessions or per-statement membership checks, both of which are performance and design decisions for the phase that has real traffic | Engineering Owner | re-score at 4 (real client) and 17+ (real environments) | OPEN |
| KAR-RSK-031 | **The device-local lock has never been exercised on a device.** The biometric prompt has not been shown on any real Android or iOS handset — not on an API 24–27 device, where the AppCompat theme requirement actually bites, and not on any iOS device where Face ID would be invoked (added 2026-08-18, with the application lock) | Mobile security | 3 | 3 | 9 Moderate | MITIGATE — everything that can be proven without a device is proven: the adapter receives, stores and transmits no biometric representation and performs no custom biometric cryptography, an unlock grants no session, the failure mapping is fail-closed **including its default arm** because the plugin's error list is explicitly open, enabling applies only on a confirmed durable write and an unconfirmable disable retains the enabled state, the host activity type and `Theme.AppCompat` parents are asserted by source scan, the merged manifest's biometric permissions are asserted against a real build, and the Face ID purpose string and both localized variants are asserted present and non-empty in the packaged bundle (KAR-CTL-100, 111; EV-441, 450, 454). **None of that proves a prompt appears**, and one defect this phase found is the argument for the row: the lock screen's password fallback navigated into the redirect that returned it to the lock, **trapping anyone whose authenticator had stopped working** — every unit assertion passed throughout, and the widget test asserted the button existed. Impact is 3 rather than higher because the lock is a local convenience over an already-authenticated session, not an authentication boundary | Security Owner | first device-testing pass | OPEN |
| KAR-RSK-032 | **No signed build exists on either platform, and no signing material exists anywhere in the project.** Distribution, install-time behaviour, update paths, and everything that only a signed artifact can exercise are untested end to end (added 2026-08-18, with the build pipelines) | Mobile delivery | 4 | 3 | 12 High | MITIGATE — the absence is made to fail closed rather than papered over: the Android release signing configuration is created only when all four pieces of material are present, so an incomplete release emits an **unsigned artifact rather than falling back to the debug key**, and CI asserts the artifact is unsigned **rather than debug-signed** because a signature here would mean the debug key had been substituted; iOS builds run with no code signing and the **absence** of a development team and provisioning profile is itself asserted by test (KAR-CTL-114; EV-449, 452). Likelihood is 4 because the condition holds today and every phase that does not address it keeps holding. What the mitigation cannot do is discover what only signing discovers, so the treatment is a scheduled first signed build rather than a stronger refusal. Custody, approval, and provenance of a signed build are KAR-CTL-115, DEFERRED and not pretended | Engineering Owner | pre-release; custody gates at 20 | OPEN |
| KAR-RSK-033 | **No real Apple Team ID exists, so cross-platform transfer is configured but unverified on a device.** The Android data-extraction rules name the iOS application by bundle identifier, and whether that element identifies the application it claims to is a property of two installed applications under one owner — which needs a team identity this repository does not hold and must not invent (added 2026-08-18, with the identifier separation) | Mobile security | 3 | 3 | 9 Moderate | MITIGATE — the chain is verified as far as it can be and the break is named rather than bridged: the Android and iOS identifiers agree per environment, are derived from one rule, and that rule is asserted character-for-character equal to the Gradle suffix table, so per-environment identity holds transitively for all four; the two artifacts are additionally compared **artifact to artifact** in CI for one environment pair (KAR-CTL-107, 109; EV-450, 451, 452). **The build fails closed rather than guessing an owner:** the packaged-bundle phase refuses when a development team or provisioning profile is set and narrowing would be required. Residual: no supported-device export or import test has been run, so the transfer exclusion is a declaration verified against a resource rather than against a device pair. Closure needs a team identity and two devices, neither of which is an engineering decision | Security Owner | first device-testing pass; needs an Apple Team ID | OPEN |
| KAR-RSK-034 | **Server-side revocation is absent for the compound credential-abandonment failure.** When a session is abandoned but **neither** the credential deletion **nor** the durable invalidation can be confirmed, nothing local can guarantee the credential is dead — only revoking the session server-side can (added 2026-08-18, with the local security-state store) | Identity / access | 2 | 4 | 8 Moderate | MITIGATE — the client refuses to present a clean state rather than claiming one: it enters a typed `SECURITY_RECOVERY_BLOCKED` state, reaches **no protected content**, offers an explicit retry, and holds an in-process latch so the credential cannot be used for the remainder of the process (KAR-CTL-098; EV-438, 439). **The guarantee is bounded and the code says so** — that is the honest shape of a local control, not a shortfall in it. Closure is server-side revocation on abandonment, which is not in this phase; until then the residual is a credential that may survive on a device the application has stopped trusting, bounded by refresh rotation, token expiry, and the account-level token version (KAR-CTL-067, 068). Likelihood is 2 because it needs a secure store that refuses **both** a delete and a durable write | Security Owner | the phase that adds abandonment revocation | OPEN |
| KAR-RSK-035 | **The golden baselines are not CI-enforced and have never run on Linux.** Four baselines — two design-system compositions in two locales, light mode only — are rasterised on macOS arm64 under a zero-tolerance comparator, and CI excludes them (added 2026-08-18, with the design system) | Delivery | 3 | 2 | 6 Moderate | ACCEPT for now, with the reason recorded rather than assumed: a zero-tolerance image comparator that has never run on the target runner is as likely to produce a permanently red lane as to catch a regression, and **two in-repo comments contradict each other about whether a Linux runner would fail, neither having been tested**. Compensating: `make test` now matches CI exactly, golden exclusion included, so the local gate and CI no longer disagree about which tests exist; goldens run deliberately through `make test-golden`; and goldens are **banned outright under `test/features`** by a source scan, because a golden is a committed image of whatever was on screen and an MFA setup key or recovery code must never be in one (KAR-CTL-096, 112). What is unprotected is visual regression in the design system, whose impact is presentation rather than security | Engineering Owner | first CI job running Flutter tests on Linux | OPEN |
| KAR-RSK-036 | **The direct artifact-to-artifact identity comparison covers one environment pair, not four.** The Android lane produces LOCAL and PRODUCTION and the iOS lane holds whichever environment it built last, so only where both platforms built the same environment does the comparison run against two real artifacts (added 2026-08-18, with the cross-platform comparison) | Mobile security | 2 | 3 | 6 Moderate | MITIGATE — the transitive chain is real and is stated as transitive rather than presented as direct: per-environment iOS identity **is** verified for all four against the shared rule, and that rule is asserted character-for-character equal to the Gradle suffix table, so identity holds for all four by construction while only one pair is checked artifact against artifact (KAR-CTL-109; EV-452). The gap has a known, costed fix — building all four Android variants on the macOS lane, at the price of a full Android toolchain on the expensive runner — which is a scheduling decision rather than an unknown. Residual: a defect that broke the rule for exactly one unchecked environment would pass CI | Engineering Owner | next mobile CI change | OPEN |
| KAR-RSK-037 | **The mobile artifact and supply-chain CI lanes run without gating a merge.** *(Statement as recorded when the risk was opened; the condition was resolved at the Phase 4 PR close — see the note column.)* `mobile-android`, `mobile-ios`, and `mobile-supply-chain` were not required status checks, so the release assembly, the merged-manifest comparison, the APK credential scan, the packaged-bundle assertions, and the mobile secret scan can all be red on a mergeable pull request (added 2026-08-18, with the lanes) | Change management | 3 | 3 | 9 Moderate | MITIGATE — `mobile` (analysis, generated-client drift, localization gate, suite) **is** required and covers the code path; what is unguarded is the artifact evidence, which is exactly the class of proof this phase added because static inspection missed three defects. **This is the legacy's INFRA-07 shape re-appearing on a new surface** — gates that block runs rather than merges — which is the failure KAR-CTL-016 exists to prevent, so it is recorded as a control status (KAR-CTL-113, DESIGNED) rather than as a note. The fix is a branch-protection settings change plus the corresponding update to `docs/operations/repository-security-settings.md`, both outside the compliance workstream's write scope. Likelihood is 3 because a single oversight on a red lane suffices | Engineering Owner | next branch-protection review | OPEN **2026-08-18, at the Phase 4 PR close: the condition this row describes no longer holds.** All three lanes are now required status checks on `main` (required set 8 → 11), so the release assembly, the merged-manifest comparison, the APK credential scan, the packaged-bundle assertions and the mobile secret scan all gate a merge. The residual is narrower rather than absent: the runs that established the lanes pass **predate** the protection change, so the first head at which they actually gate is the next one. Re-scored at the Phase 5 gate once a merge has been blocked or allowed under the new set. |
| KAR-RSK-038 | **The Dart runtime loopback rule is narrower than the build-time rule that protects it.** `localhost.`, the expanded IPv6 loopback, `0.0.0.0`, and the `.internal` and `.test` suffixes pass the runtime configuration loader, which checks exact equality over four hosts plus two suffixes and was not widened when the build-time guards were (added 2026-08-18, from the guard work) | Mobile security | 2 | 2 | 4 Low | MITIGATE — exposure is bounded by the layer that did move: **a package carrying such an endpoint cannot be built for a deployed environment**, because the build-time guard refuses it on both platforms with the bracket unwrap and the trailing-dot strip (KAR-CTL-106; EV-448, 452). The residual is that **the two layers now disagree and the narrower one is the one that runs on a device**, so the belt holds while the braces do not. Closing it is a loader change plus the matching test cases — small, known, and deliberately not done under a release-blocking remediation round. Recorded rather than folded into the guard's own row, because a control whose two layers disagree should say so | Security Owner | next mobile change | OPEN |
| KAR-RSK-039 | **Runtime response conformance covers 82 of 128 declared operation/status pairs.** Forty-six declared pairs are validated against no real response, so the contract's `additionalProperties: false` declarations and response schemas are enforced against the server for the mobile-consumed surface and not beyond it (added 2026-08-18, with the conformance suite) | Delivery / integrity | 2 | 3 | 6 Moderate | MITIGATE — the uncovered set is **enumerated in the suite's own header rather than left as a percentage**, and it is not all of one kind: most are simply outside the mobile-consumed surface the suite was scoped to, and within the seventeen `/auth` operations it does cover, exactly three declared statuses are **not reachable in process** for reasons the header records — two need a code stored as a digest and delivered only by e-mail, and one would require failing the shared rate-limit store underneath every other test in the run. The covered set is asserted explicitly, so an operation that quietly stopped being exercised fails there, and the validator **throws on any JSON Schema keyword it does not implement** so a schema cannot silently pass unchecked (KAR-CTL-104; EV-433). What must not happen is a new surface landing outside the suite; the treatment is extension **as** each surface is written rather than after | Engineering Owner | 5 (with the first substantial response surface) | OPEN |
| KAR-RSK-040 | **A compromised device defeats the client's storage and capture protections, and neither is defended against.** Root and jailbreak detection is deliberately not attempted, and `FLAG_SECURE` and its iOS equivalent are not implemented, so screenshots and screen recording are not prevented (added 2026-08-18, with the client) | Mobile security | 2 | 3 | 6 Moderate | ACCEPT, with both halves deliberate rather than overlooked. Root and jailbreak detection is an arms race that produces false confidence and breaks legitimate users, and the platform keystore is the boundary being relied on; what is done instead is to keep the exposed surface single and small — one namespaced secure entry, `first_unlock_this_device` on iOS and macOS so the credential is out of iCloud Keychain and off restored backups, reset-on-error on Android (KAR-CTL-097; EV-437). For capture, the sensitive-screen wrapper covers its subtree and **removes it from the semantics tree** whenever the application is not resumed, and **states its own limit in source** rather than implying more; goldens are banned across the feature tree so a setup key or recovery code cannot end up in a committed image (KAR-CTL-096). Re-score when real customer data exists on a device, which is not the case in any environment today | Security Owner | re-score at first real data; revisit 20 | OPEN |
| KAR-RSK-041 | **Client-side dependency on two platform vendors whose behaviour the project cannot observe or version.** Backup and transfer semantics, keystore behaviour, biometric prompt behaviour, and transport-security enforcement are decided by Android and iOS releases, not by this repository, and change under a shipped application without a code change here (added 2026-08-18, with the mobile client) | Vendor / platform | 3 | 3 | 9 Moderate | MITIGATE — the project's answer is to assert against what actually ships rather than against documentation: the merged manifest is compared **exactly** so a dependency that starts contributing a permission fails the build, the data-extraction rules declare **every** backup mode because an omitted section is fully enabled rather than off, the packaged bundle's transport posture and identifier are read from the artifact, and the plugin that owns the biometric prompt carries its own dependency record with a named owner and review triggers (KAR-CTL-107, 108, 110, 111). **The residual is the part that cannot be asserted:** a platform release changing behaviour under an installed application is invisible to every check here, and with no device testing and no signed build there is no channel through which such a change would be noticed (KAR-RSK-031, 032). `compileSdk` is pinned to a dependency's floor with its removal trigger recorded, so the pin is a constraint rather than a preference | Security Owner | ongoing; re-score at first device pass | OPEN |
| KAR-RSK-042 | **The mobile client does not present a screen a person can act on, and the cause is now located rather than guessed.** Two distinct defects. **(a) A liveness gap in platform I/O, FIXED**: the startup sequence awaits two storage reads before the network, and neither was bounded — a `Future` that never completes is neither a value nor a throw, so the fail-closed machinery below could not run because nothing reached it. Both are now bounded by one policy with a typed failure that fails closed, and instrumented on an iOS 26.5 simulator the startup machine demonstrably reaches a terminal state (`configLoading → sessionRestoring → unauthenticated`) and the router redirects to the sign-in gate. It did not before. **(b) The screen does not follow the state machine, OPEN**: after the redirect the Flutter UI thread produces no further frames — the content area is byte-identical across a 40-second window, with the status bar excluded — so what a person sees is the last rasterised frame, a spinner that never resolves. `tool/startup_smoke.sh` fails on this today. *(Scope note: the earlier record of this row said the sequence was "suspended in one of two secure-storage reads". That was the best available reading then and is superseded: the reads are bounded and the sequence completes.)* (added 2026-08-22; re-scoped the same day) | Product / engineering | 4 | 4 | 16 High | MITIGATE — (a) delivered with 13 tests including a mutation that removes the bound. (b) reproduce on a second runtime (Android emulator, then a device) to separate an iOS-specific render stall from application logic, fix, and keep `tool/startup_smoke.sh` red until it passes. **No client-behaviour claim may be made until (b) is closed** | Engineering Owner | **Phase 5 — this is a demonstrated Phase 5 client failure and is not deferred to Phase 6** | OPEN |
| KAR-RSK-043 | **The statement-import picker cannot be exercised end to end by anyone, on any device, today.** The whole financial surface is gated on the client-safe bootstrap reporting `TRANSACTIONS` available; the registry deploys it in no environment and declares it in no jurisdiction, so the availability resolver denies before any later gate and no build against any real environment can reach the picker through the UI. The device smoke test the roadmap contemplates is therefore not merely unperformed for want of hardware — it is unperformable without a purpose-built harness that bypasses the capability gate, which would prove something other than the product (added 2026-08-22, Phase 5 verification) | Product / verification | 5 | 3 | 15 High | MITIGATE — the honest closure is a deployed environment with the capability available, which is Phase 6+ work. Until then the picker's two native implementations stay verified by unit test and artifact assertion only, and no document may describe the import path as verified on a device. Recorded rather than worked around: a harness build that forces the gate open would produce evidence about the harness | Platform Owner | Phase 6+ (first deployed environment) | OPEN |
| KAR-RSK-044 | **A verification control can pass without having verified anything, and two did.** `phase5-ingestion-not-mounted-early` scanned zero files at Phase 5 and was counted among the architecture headline's passes; `phase-status-consistency` exempted the entire `docs/compliance/` directory and so could not see eleven stale current-state assertions in the canonical compliance corpus. Both were fixed in Phase 5 with self-tests and mutation proofs, and the risk the pair demonstrates is structural rather than local: a green check is trusted in proportion to nobody re-deriving what it examined (added 2026-08-22, Phase 5 verification) | Assurance | 3 | 4 | 12 Moderate | MITIGATE — every checker that can go quiet now fails on a zero-scan inside its own window, `NOT_APPLICABLE` is a third status excluded from the pass count, and the docs gate derives capability state, control tally, gate existence and phase status mechanically. KAR-CTL-116's merge-blocking register sweep remains the outstanding leg and is still DESIGNED | Compliance Owner | Phase 6 | OPEN |
| KAR-RSK-045 | **A CI lane can be broken for an arbitrary number of commits when no pull request is open to run it.** The readiness recovery step — the only step that stops and restarts the application's dependencies — could not start at all from commit `a64ad7d` onward, because a `globalSetup` path resolved against the process working directory. Phase 5 ran 101 commits with no pull request, so no required check reported it; it was found by executing CI's own command locally during verification (added 2026-08-22, Phase 5 verification) | Assurance | 4 | 3 | 12 Moderate | MITIGATE — the property is now asserted by a test in the repository (`vitest.config.test.ts`) so it fails in the workspace lane rather than only in the lane it breaks. The broader treatment is to open the phase pull request early enough that CI runs against the branch as it develops, rather than once at the end | Engineering Owner | Phase 6 | OPEN |
| KAR-RSK-046 | **A rate-limited request performs 22 database round trips, including a row UPDATE, before it is refused.** The guard's own header says a refused request does no work, which is true of the use cases and false of everything in front of the limiter: ten queries in principal enrichment — one of them `UPDATE public.sessions SET last_seen_at` — and twelve in capability resolution, all before the budget is consulted. An admitted read costs 26, so exceeding a budget saves about 15% of database cost. A principal whose capability is UNAVAILABLE is never rate limited at all and pays the full twelve-query resolution per 403, at unbounded frequency. There is no IP-keyed or pre-authentication limit anywhere on the surface (added 2026-08-22, Phase 5 closeout review) | Availability | 3 | 4 | 12 Moderate | MITIGATE — a capability-resolution cache with an explicit invalidation, and an IP-keyed limit ahead of principal enrichment. Neither is a Phase 5 change: the first needs an invalidation design that cannot serve a stale availability answer, and the second is an edge control. **The guard's header is corrected meanwhile**, because a comment that overstates a control is worse than no comment | Engineering Owner | Phase 6 | OPEN |
| KAR-RSK-047 | **The rate-limit window is pruned with the application's clock, so one skewed instance shortens it for the whole fleet.** The Lua script prunes with `ZREMRANGEBYSCORE key 0 (now - window)` where `now` is `Date.now()` from the calling process; Redis's own `TIME` is never consulted. Verified against live Redis: an instance 20 seconds ahead reduced a five-entry window to one entry and admitted a request the correct-clock path had just refused — and the pruning is destructive, so the correct-clock instance then admitted too. At a drift of one window the ceiling collapses entirely. The limiter's header claims N instances share one window; that is true for concurrency and false for time (added 2026-08-22, Phase 5 closeout review) | Availability / abuse | 2 | 4 | 8 Moderate | MITIGATE — take the timestamp from Redis `TIME` inside the script, so one clock governs one window. Deliberately not changed at Phase 5: it alters the script every limiter shares, and the closeout is not the moment to re-time the whole surface. Compensating meanwhile: one process, one clock, no fleet | Engineering Owner | Phase 6 | OPEN |
| KAR-RSK-048 | **The encrypted source store's AAD binds the subject and nothing else, and no adapter can bind more.** `open`, `verify` and `erase` receive a `StoredSource` descriptor carrying neither the import id nor the media type, so the import half of the documented binding cannot be rebuilt by ANY implementation — S3 with KMS included — and the stored AAD authenticates the object against itself for everything but the subject prefix. Verified: an object replayed under a different import of the SAME subject decrypts; cross-user and cross-tenant are refused. `statement_import_sources.object_ref` carries no unique constraint, so two imports can name one object. The exploit precondition is row-level write — which is precisely what an AEAD binding exists to survive (added 2026-08-22, Phase 5 closeout review) | Security | 2 | 4 | 8 Moderate | MITIGATE — widen the port so `open`/`verify`/`erase` receive the import id and media type, then bind them. Structural rather than local: it changes the port, every adapter and every caller. The subject arm — the half that was missing and was fixed earlier in Phase 5 — holds and is tested | Security Owner | Phase 6 | OPEN |
| KAR-RSK-049 | **A person who buys the same thing twice cannot record the second purchase from the client.** The platform is correct: dedup compares an occurrence index rather than requiring a next ordinal, so two identical transactions are never collapsed, and the second is accepted at `occurrenceOrdinal: 2`. The client cannot get there — `api_transactions_repository.dart` never sets the field, the domain repository has no such parameter, and neither `DUPLICATE_TRANSACTION` nor `OCCURRENCE_ORDINAL_NOT_NEXT` appears anywhere in `apps/mobile/lib`, so the 409 falls through to a generic conflict. The ledger silently understates spending (added 2026-08-22, Phase 5 closeout review) | Product | 4 | 3 | 12 Moderate | MITIGATE — plumb the ordinal through the client repository and render the two refusals as what they are: an offer to record the second occurrence. Not a Phase 5 change because the client surface is unreachable on any runtime today (KAR-RSK-042), so shipping a flow nobody can reach would be work with no evidence behind it | Engineering Owner | Phase 6 | OPEN |
| KAR-RSK-050 | **A security-relevant screen assertion was wrong, and platform-divergent, and had never run on CI.** Two screens assert that nothing longer than a masked tail reaches the display. Both stripped every non-digit from a rendered string and required fewer than nine digits left — which treats digits scattered through a sentence as one number, so `"True as of Mar 1, 2026 12:00 PM"` reduces to nine digits and a freshness timestamp is reported as a possible card number. It passed on macOS and failed on Linux, because the two platforms' ICU data render the same instant with different date patterns. **Phase 5 ran 116 commits with no pull request, so the `mobile` lane never executed on any Phase 5 head**; the draft pull request opened as the CI harness reported all three cases in its first run (added 2026-08-22, Phase 5 draft-PR CI) | Assurance | 4 | 2 | 8 Moderate | MITIGATE — **delivered**: `longestDigitRun` counts a contiguous run, allowing a single space or hyphen between groups because that is how a card number is printed, and every real detection is kept while dates, grouped amounts and masked tails are not. The general treatment is KAR-RSK-045's: open the phase pull request in the first working session, so a platform-divergent assertion is found on the day it is written | Engineering Owner | Phase 6 (process half) | OPEN |

## Notes

- **KAR-RSK-012:** the archive incident involved no secrets or personal data as far as inspection shows; it is recorded because the *mechanism* (accidental commit, permanent history) is the risk, and pre-production is the cheap time to fix the habit. This register does not itself remove the file; that action belongs to the Platform Owner at Phase 1 close.
- Risks 006, 007, 008 are **not engineering-closable** — their treatments are legal/external engagements the roadmap already names as non-engineering gates.
- Nothing here is scored against a production environment, because none exists; Phase 17/19/20 gates force rescoring at each step onto real infrastructure.
- **Phase 2 review, 2026-08-15:** every row re-read against the Phase 2 platform deliverables. Deltas: 003 note revised (signal substrate exists, watching does not), 009 note dated (contracts designed, gates unchanged), 010 re-scored 12 High → 9 Moderate (test 26 active), 014 and 015 added with their mechanisms. All other rows reviewed, unchanged. **No risk closed at this review.** The gate-record half (attendee, sign-off) lands with the Phase 2 gate report per the EV-008 pattern.
- **Phase 3 review, 2026-08-16:** every row re-read against the Phase 3 identity/tenancy/access-control deliverables. Deltas: 016–020 added with their mechanisms; 009 note dated (provider seam exercised, custody gates unchanged); KAR-RSK-013's compensating-control facts refreshed in EXC-002 (short-lived tokens with server-side revocation moved from design to code). All other rows reviewed, unchanged; no re-score, no closure — KAR-RSK-019 is expected to close at the Phase 3 gate on the guard-mounting verification, not before it. The gate-record half (attendee, sign-off) lands with the Phase 3 gate report per the EV-008 pattern.
- **Phase 3 gate, 2026-08-16 (record in phase-compliance-gate.md v0.4):** every row reviewed at close; KAR-RSK-019 CLOSED on the executed mounting verification; KAR-RSK-021 target fixed to Phase 3.5 with its closure condition; no other status or score changed. Outcome PASS_WITH_DOCUMENTED_DEFERRED_ITEMS.
- **Post-review addendum, 2026-08-16 (independent review):** KAR-RSK-021 added (tenant-bound surface dormant pending session tenant binding — the review found the residual undocumented); KAR-RSK-019 updated in place: guard mounting landed before the PR, gate verification remains the closing act.
- **Phase 3.5 gate, 2026-08-16 (record in phase-compliance-gate.md v0.6) — HISTORICAL, as recorded at that gate:** every row reviewed at close. **KAR-RSK-021 CLOSED — confirmed at the gate**, on the verification its own closure condition named (gate scope item 7): server-side membership sourcing, bind-time re-verification, switch-time session **and refresh-family** rotation, verify → act → re-verify → compensate, uniform denial of arbitrary/revoked/expired/disabled targets, and the first-party tenant from typed configuration, all proven by the adversarial and concurrency suites (EV-411, 412, 420). The row was **not** reopened. Its surviving residual stays separate as KAR-RSK-030. No other status or score changed; no risk was opened or closed at the gate beyond that confirmation. Outcome PASS_WITH_DOCUMENTED_DEFERRED_ITEMS. **Phase 3.5 is COMPLETE and Phase 4 is NOT STARTED** — no row below is scored against Phase 4 work, and the several rows naming Phase 4 as a re-score point are stating when they will next be re-examined, not claiming anything about it.
- **Phase 3.5 review, 2026-08-16:** every row re-read against the Phase 3.5 jurisdiction, PolicyPack, capability, subject-policy, and session-binding deliverables. Deltas: **KAR-RSK-021 CLOSED** on its own stated closure condition (mechanism plus adversarial and concurrency tests — EV-411, 412, 420); 022–030 added with their mechanisms; notes dated on 010 (test 26 strengthened), 016 (switch rotates refresh families), 017 (48-table inventory, anti-boilerplate allow-list rules, binding as a new context source), and 020 (policy/capability stores deny on outage; the only provider source is `NoProvidersConfigured`). All other rows reviewed, unchanged; **no re-score**. Three risks were considered and deliberately *not* opened as new rows because an existing row already carries them: the Sharia gate on Zakat (KAR-RSK-007, now visible as a registry-level launch gate), Amanat's legal clearance (KAR-RSK-008, now visible as a HIDDEN, disclosure-bearing capability that no pack clears), and the single-maintainer reality (KAR-RSK-001/002, EXC-001 — multiple agent workstreams are technical, not organizational). The gate-record half (attendee, sign-off) lands with the Phase 3.5 gate report per the EV-008 pattern.

- **Phase 4 review, 2026-08-18:** every row re-read against the Phase 4 client, contract-hardening, and build deliverables. Deltas: **031–041 added** with their mechanisms; **013 re-scored 6 Moderate → 8 Moderate** on its own stated Phase 4 revisit, with the acceptance standing and the reason for holding L at 2 recorded in the row; notes dated on 001, 002, 003, 004, 005, 010, 011, 016, 020, and 027. **No risk closed, and no risk was closed by the arrival of a mechanism** — 016 and 027 both named Phase 4 as a re-score point and both were re-examined and held, which is how a re-score commitment is discharged when the answer is "unchanged". **This is the phase review, not the gate record** *(HISTORICAL, as written on 2026-08-18 before the Phase 4 gate ran)*: at the time it was written no Phase 4 gate had executed, so the attendee-and-sign-off half was to land with the gate per the EV-008 pattern, and nothing in it may be read as a gate outcome. The Phase 4 gate has since executed — see the bullet below.
- **Three Phase 4 findings were considered and deliberately *not* opened as new rows**, because an existing row already carries them and a duplicate row is register noise rather than register coverage: the **absent protected evidence store** (KAR-RSK-011, whose treatment now carries the lapsed deadline as a slipped step rather than a new risk), the **single-maintainer reality across eleven agent workstreams** (KAR-RSK-001/002 and EXC-001 — agent workstreams are a technical division of labour, not separation of duties), and the **`deepmerge-ts` advisory** (KAR-RSK-004, where it is recorded as an advisory acted on rather than reported).
- **Two Phase 4 residuals are carried in the control matrix rather than here, and the distinction is deliberate:** the client's empty capability-navigation allowlist and its empty jurisdiction reference list are **correct behaviour with nothing to be exposed to** — nothing is implemented and no jurisdiction is approved — so they are limitations of what can be tested, recorded in KAR-CTL-101 and KAR-CTL-102, and not risks with an owner and a treatment. Recording them here would dilute what a risk row means.
- **Phase 4 gate, 2026-08-18 (record in phase-compliance-gate.md v0.7/v0.8 and its post-merge record):** every row reviewed at close. **No risk was opened, closed, or re-scored at the gate**, and none of the Phase 4 rows moved on the merge — a mechanism arriving and a mechanism operating are different questions, and nothing operates. The gate's outcome was `PASS_WITH_DOCUMENTED_DEFERRED_ITEMS` with eleven deferred items; the risk consequences of those items are carried by the rows they name rather than duplicated here. **KAR-RSK-011's protected evidence store reached and passed its stated deadline at this gate and was not discharged** — carried into Phase 5 as an overdue treatment, which the Phase 5 review below records rather than rolls forward again.
- **Phase 5 review, 2026-08-22 — the FIRST pass, before the independent reviews ran; the additions it names are 042–045 and the full Phase 5 set is 042–049 (see the version note at the head of this file):** every row re-read against the Phase 5 financial platform, the six financial modules, the CSV ingestion path and the Flutter financial surface. **Deltas: 042–045 added.** No row was closed. **031, 032 and 033 (the device gap) are re-examined and HELD, and Phase 5 sharpened rather than reduced them**: the phase produced a native document picker on both platforms whose two implementations have still never run against a real document provider, and the Phase 5 verification session additionally reproduced a startup hang on a live iOS runtime (KAR-RSK-042), so the device gap now has a demonstrated consequence rather than only an absence of proof. **013 was re-examined and held.** Notes dated on 011 (overdue, see below), 016, 020 and 027.
- **KAR-RSK-011 is OVERDUE and is recorded as a slipped treatment rather than rolled forward silently.** Its protected evidence store was targeted at the Phase 4 gate, arrived there undischarged, and arrives here the same way. It is not re-dated to a later phase by this review: the deadline passed, the treatment plan carries the slip with its owner and reason, and the Phase 5 gate records it as a nonconformity. A passed deadline does not become "ongoing" because nobody wrote it down.

## Closed risks

| ID | Risk | Closed | Closing event |
|---|---|---|---|
| KAR-RSK-019 | Kill-switch guards unmounted until phase integration | 2026-08-16 (Phase 3 gate) | Guard mounting verified on the closing clean-clone run — register/login/refresh and invitation issue/redeem carry the consumer-declared gates, proven by the mount tests and the composed error-boundary test. Recorded in phase-compliance-gate.md v0.4, item 3. The row above stays in the register with its full history |
| KAR-RSK-021 | Tenant-bound API surface dormant pending session tenant binding | 2026-08-16 (Phase 3.5) | The session-binding mechanism delivered as KAR-CTL-091 with the adversarial and concurrency tests its closure condition named — EV-411 (binding authorization), EV-420 (rotation and concurrency), EV-412 (membership outliving the binding), all COLLECTED on the executed suite of 2026-08-16. **The closure was recorded here on that executed verification, and the Phase 3.5 gate has now confirmed it** (gate record v0.6, gate scope item 7) rather than reopening it. The residual the mechanism does not remove is carried forward as KAR-RSK-030, not folded into the closure |

Closed rows are never deleted: the row above the fold keeps its full text and its `CLOSED` status, and this table is the index of closing events.
